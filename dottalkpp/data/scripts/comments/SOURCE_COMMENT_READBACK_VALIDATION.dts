* SOURCE_COMMENT_READBACK_VALIDATION.dts
* Status: STABLE_CANONICAL_SCRIPT / READ_ONLY
* Purpose: Validate source-comment tables from the canonical COMMENTS lane.

SET ECHO OFF
ECHO ===== SOURCE COMMENT READBACK VALIDATION START =====
SETPATH DBF COMMENTS
SETPATH INDEXES INDEXES/COMMENTS
SETPATH LMDB LMDB/COMMENTS
WORKSPACE OPEN DBF
WORKSPACE

ECHO ===== COUNTS =====
SELECT SRCFILE
COUNT
SELECT SRCBLOCK
COUNT
SELECT SRCLINE
COUNT
SELECT SRCUSAGE
COUNT
SELECT SRCCLASS
COUNT
SELECT SRCDISP
COUNT
SELECT SRCALIAS
COUNT
SELECT MEMO_LINES
COUNT

ECHO ===== STRUCTURES =====
SELECT SRCFILE
STRUCT
SELECT SRCBLOCK
STRUCT
SELECT SRCLINE
STRUCT
SELECT SRCUSAGE
STRUCT
SELECT SRCCLASS
STRUCT
SELECT SRCDISP
STRUCT
SELECT SRCALIAS
STRUCT
SELECT MEMO_LINES
STRUCT

ECHO ===== SMALL SAMPLES =====
SELECT SRCFILE
LIST
SELECT SRCBLOCK
LIST
SELECT SRCUSAGE
LIST
SELECT MEMO_LINES
LIST

ECHO ===== SOURCE COMMENT READBACK VALIDATION END =====
