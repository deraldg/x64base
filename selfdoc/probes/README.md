# SelfDoc probes

This directory is reserved for small SelfDoc probes.

Bootstrap policy:

- Do not move existing scripts here during bootstrap.
- Do not write DBFs from bootstrap.
- Do not wire probes into CMDHELPCHK yet.
- Add only explicit, reviewed probes in later phases.

Intended future use:

- tiny inspection scripts
- non-mutating catalog checks
- experimental report generators
- reproducible diagnostics