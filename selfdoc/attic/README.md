# SelfDoc attic

This directory is reserved for retired or superseded SelfDoc tooling.

Bootstrap policy:

- Do not move existing scripts here during bootstrap.
- Do not delete existing scripts during bootstrap.
- Do not treat attic contents as active pipeline inputs unless a later manifest says so.

Intended future use:

- superseded probes
- old one-off helper scripts
- historical notes about replaced tooling