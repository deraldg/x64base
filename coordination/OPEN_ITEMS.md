# Open items -- the rung below a lane

Small, real, deferred work. Too small for an AIF lane, an SDLC gate, or a PDLC
project; too real to lose. One line each.

**Why this file exists (2026-08-17).** The coordination ladder had a documented
lightest rung -- quips (`AI_SESSION_COORDINATION_PROTOCOL_V1.md`) -- but quips
live in a gitignored inbox and do not survive a fresh clone, so parking anything
there loses it. Above that, the next rung was a claimed AIF lane, which is heavy
for "check a DNS record some day". Between the two there was nothing, so small
items went into chat and evaporated.

**Why it nags rather than merely records.** AIF-006's own warning text carries
the measurement: an obligation with no gate held at **33 percent** compliance
while gated obligations held **83-94**. A register nobody surfaces is a register
that decays. `tools/coordination/check_open_items.py` runs in the pre-push gate
and prints any row whose NEXT LOOK date has passed. It never blocks -- these are
deferred by choice, and a gate that blocks on them would teach people to delete
rows instead of doing them.

## How to use it

- **Add** a row. Take the next id. Set NEXT LOOK to when you actually want to be
  reminded, not tomorrow. A date far out is a legitimate answer.
- **Snooze** by moving NEXT LOOK. That is not cheating; it is the point. But the
  RAISED date never changes, so an item you have snoozed six times shows it.
- **Close** by deleting the row and, if it produced anything durable, saying so
  in the commit message. No closed-items graveyard here -- git already has one.
- **Promote** to an AIF lane if it grows. Claim a number, cite this id, delete
  the row.

## WHERE it gets done

The column that stops this becoming a code-only list. An item whose work happens
at a registrar or a hosting panel can NEVER be closed by a commit, and a register
that assumes otherwise silently mis-files every infrastructure item it holds.

`repo` -- a change in this tree - `site` -- the x64base-site tree -
`registrar` / `host` / `dns` -- an external control panel -
`decision` -- needs a ruling before any work starts

## Items

| id | raised | next look | where | item |
| --- | --- | --- | --- | --- |
| OI-001 | 2026-08-17 | 2026-09-15 | dns | `derald.com` serves an unrelated third party over an invalid certificate. Registry checked 2026-08-17: NOT lapsed -- registered 2004, expires 2027-06-24, GoDaddy, `NS23/NS24.DOMAINCONTROL.COM`, all four client locks set, last changed 2026-07-08 (two days before `c244300da` retired it as a support host). Check the `@` and `www` A records, whether a hosting or forwarding product is still attached, and the certificate binding. Also sweep for other places that still point at the name. |
| OI-002 | 2026-08-17 | 2026-10-01 | repo | `src/bindings/` is dead code. Nothing `add_subdirectory`s it, and `src/CMakeLists.txt` excludes it twice with "hard safety: never compile src/bindings into dottalkpp". Its comment reads `# After add_library(pydottalk MODULE ...)` -- describing a target it does not create. Pre-dates the move to `bindings/pydottalk/`; last touched 2026-04-28. It is a SECOND `pydottalk` definition and it briefly misled this session into thinking it was the live one. Delete it, or leave a one-line note saying what it was. |
| OI-005 | 2026-08-17 | 2026-09-08 | decision | **Command-shell execution from Python: how, and against which binary.** The starter README lists it as an unproven milestone, and the superseded `src/bindings/pydottalk.cpp` (OI-002) already implements `Session::run(table, commands)` and `Session::export_csv(table, fields, for_clause, out_path)` -- two capabilities the current module does not have. Three routes, and the choice decides OI-002: (a) link the shell into the `.pyd`, which drags the ~400-TU CLI back in and defeats the lean build, and gives the module its own per-process identity/catalog state that nobody logged into; (b) subprocess `dottalkpp.exe --script`, which is what `datarun.ps1` already does, keeps the process boundary, inherits the CLI's real init and permission path, and uses the documented `SET ALTERNATE` capture rule; (c) a lean data-only CLI, which is a new target and a new command subset to maintain. Prior art favours (b): `dottalkpp` and `dottalk_bbsd` already share the STORE with no IPC between them, so a third process is the established pattern rather than a new one. |
| OI-006 | 2026-08-17 | 2026-09-22 | repo | **The ROOT build has no `CMAKE_POSITION_INDEPENDENT_CODE` and would fail on Linux.** Measured 2026-08-17: the property is set NOWHERE in this repository. On ELF, every object linked into a shared module must be position-independent, and `xbase`/`memo`/`xindex` are static libs built without it. The lean pydottalk build hit this and now sets it; the root build does not, so `cmake -DBUILD_PYDOTTALK=ON` on Linux/WSL fails at LINK with `relocation R_X86_64_TPOFF32 against xbase::trigger_hooks::(anonymous)::g_suppress_depth ... recompile with -fPIC`. Windows never surfaces it because a DLL carries no such constraint. NOT urgent -- nobody builds the root on Linux today -- but it is a landmine for the first person who tries, and the diagnostic names a thread-local three libraries deep rather than the missing property. One line in the root CMakeLists, or per-target on the three libs; decide which, since setting it globally also applies to the CLI executable. |
| OI-007 | 2026-08-17 | 2026-11-03 | repo | **Re-measure `DOTTALK_INDEX_MODE` once pydottalk exposes an index API.** Today the three modes ship a byte-identical module (665336 B, sha256 `54cb15eb...`, all three) because `module.cpp` references the index zero times, so the static `libxindex.a` resolves nothing and the linker discards it, `lmdb_backend.o` included. Confirmed against the AI portal on 2026-08-17: indexing has NEVER been tested with pydottalk -- the portal names it once, as a closeout filename; the four `launcher_pydottalk` proof runs are 356-389 B with zero index hits; the only registered pydottalk proof is APPEND BLANK, which is CRUD. The binding is CRUD-only by construction. THE TRIGGER for this row is the first `setOrder`/`seek`/tag binding added to `module.cpp`: at that moment `lean-none` should start FAILING to link, the modes stop being interchangeable, and on Windows the `.pyd` may acquire a runtime dependency on `lmdb.dll` (vcpkg builds lmdb as a DLL on x64-windows) that must then ship beside it. Settle the DLL half any time with `dumpbin /dependents pydottalk.cp312-win_amd64.pyd`. Evidence: `proof.build.index_mode_changes_nothing_shipped`. |
| OI-008 | 2026-08-17 | 2026-09-14 | decision | **`status.showUntrackedFiles=no` is set in this clone, so the prescribed safety check cannot see new files.** CLAUDE.md, `AI_TIER1_SEED_V1.md` and `AI_README.md` all told sessions to run `git status --short` between add and commit; with this config a bare status reports NOTHING for a file just created, so an unstaged new document is indistinguishable from a clean tree. Found 2026-08-17 when the check reported clean while five new files sat unstaged. Prose corrected to `-uall` in all three places, so the check now works as written. **The config itself is still an open decision.** Turning it off makes the house check honest by default, but every session then sees a large untracked backlog on every status: `scripts/`, `scripts/ps1/`, `smoke/`, `terminals/` and a stray `ws_proxy.txt` at the repo root (a captured status listing, not source). Note the seed already says sessions will see "hundreds of untracked files", which is the doctrine describing a state the prescribed command could not show. Decide: unset the config and triage the backlog, or keep it and rely on `-uall` everywhere. **FOUR MEASURED COSTS, found within the hour by running `-uall` once.** (a) `src/cli/cmd_smtp.cpp` is untracked and 0 bytes, and `src/CMakeLists.txt:92` globs `src/cli` with `file(GLOB_RECURSE ... CONFIGURE_DEPENDS)` -- so anything dropped there is compiled into the CLI whether git knows it or not. Harmless while empty; the moment it has content the build includes a file the repository does not have. (b) `scripts/build/` is a complete CMake build tree (`CMakeCache.txt`, `CMakeFiles/`, `_deps/`). The pre-push gate hard-blocks build trees and reported `hard-block: 0` on every commit today -- correctly, because it inspects the STAGED INDEX. (c) `scripts/MCC - Build full relations graph (CNX-backed).dts` carries a literal em-dash in its FILENAME, against the house rule; `house-style` also passed, because it checks added documentation LINES, not names. (d) `PYDOTTALK_SDLC_CHARTER_v0.md` asserted "no `pyproject.toml`" while one sits untracked at the repo root -- an absence claimed from a status that cannot report presence, corrected the same day. **The pattern in (a)-(c) is one rung up from the config**: three gates that are green, correct, and unable to distinguish "clean" from "outside my scope". Whatever is ruled on the config, that scope question is the more general defect and belongs to AIF-118. **TRIAGED 2026-09-01, AND THE ROW'S OWN URGENCY EVIDENCE IS STALE. THE SAFETY ARGUMENT FOR UNSETTING THE CONFIG HAS EVAPORATED.** This row's headline cost (a) was `src/cli/cmd_smtp.cpp` untracked and 0 bytes inside `src/CMakeLists.txt`'s `GLOB_RECURSE`, so "the moment it has content the build includes a file the repository does not have". Re-measured: the file is 15,852 bytes AND TRACKED, and **`src/` now carries ZERO untracked files of any kind**. Nothing untracked is on the compiled surface, which is the only part of this backlog a bare `git status` could have hidden to any consequence. **THE BACKLOG IS 7,449 FILES AND IS DOCUMENTATION AND DATA, NOT SOURCE:** 3,064 `.md`, 1,127 `.json`, 559 `.csv`, 896 database files (`.dbf`/`.cdx`/`.dtx`/`.dbt`), 1,028 scripts (`.ps1`/`.dts`/`.py`), 62 `.hpp`. Concentrated in `docs/datadict` 1,914, `dottalkpp/data` 1,540, `dottalkpp/docs` 1,206, `docs/maintenance` 860, `docs/manuals` 663. **EXACTLY 22 OF THE 7,449 ARE BUILD ARTEFACTS** -- all of them cost (b)'s `scripts/build/` CMake tree -- and they are now gitignored, which is the whole of the disposable fraction. **THE TWO HALVES WERE CONFLATED AND THAT IS WHY THIS SAT FIFTEEN DAYS.** "Is the house check honest?" was ANSWERED when the prose moved to `-uall` in all three places; "should 7,449 files be tracked?" is a documentation-and-data question already owned by OI-011, OI-013 and OI-019. **RECOMMENDATION: KEEP THE CONFIG.** Unsetting it puts a 7,449-line status in front of every session to solve a problem that is no longer in the compiled surface, and the doctrine already warns sessions they will see hundreds of untracked files. The steward's decision is still the steward's; what changed is that it is no longer a safety decision. **ONE REAL WIDOW FOUND AND CLOSED IN THE SAME COMMIT:** `include/identity/org_schema.hpp`, 10,399 bytes, dated 2026-08-14, untracked and NOT ignored, in a directory where the other EIGHT headers are all tracked. It is not merely an omission -- `labtalk/ai_portal/AI_GLOSSARY_V1.md:380` names it as the **Home** for the `namespace standing` term, so a tracked document cited a file the repository did not have. That is OI-013's "urgent kind" test met, and a header needs no glob to be included. ASCII-clean, carries a valid `@dottalk.file v1` banner reading `status: review-needed`, and documents the dangling OrgUnit scope. Tracked 2026-09-01. **THE REMAINING 61 UNTRACKED `.hpp` ARE NOT THE SAME CASE** -- 59 sit under `dottalkpp/docs`, one under `dottalkpp/bin`, one under `docs/messaging`; none is on an include path. NEXT LOOK moved to 2026-09-14 for the config decision alone. |
| OI-010 | 2026-08-17 | 2026-09-29 | decision | **`main` carries TWO build documents and `development` carries one; decide which is canonical before a third diverges.** Measured 2026-08-17: `origin/main` has both `BUILDING.md` and `BUILDING.md` under `docs/getting-started/`; `development` has only the root one, and the public README links the `docs/getting-started/` copy. This is the shape `CLAUDE.md` names citing AIF-082 6.8 -- "two shims that restate will diverge, and have" -- and they already have: the root copy asserted that `DOTTALK_PRODUCT` and `windows-lean-*` presets were "not on the public repository yet" while main's own `CMakePresets.json` carries 28 occurrences of those names. That claim is corrected in `development` as of this row, but the `docs/getting-started/` copy on main was NOT inspected and may repeat it. Decide: one canonical page with the other reduced to a pointer, or a deliberate split (quickstart vs reference) with a stated boundary. Note the reference tier now exists as `docs/manuals/developer/dev/dev-21-build-system.md` (DEV-21, written as DEV-19 here until 2026-08-21: the chapter was renumbered on the day it was authored, for the reason OI-011 records, and this citation kept the pre-rename number. The cited-paths gate reported it as MISSING at every commit that touched this file), so a three-way split is the live risk. Cannot be closed in `development` alone; the second file exists only on `main`, which makes this a promotion-scope call. **ITEM 1 IS DONE, MEASURED 2026-08-25 -- and a stale git ref hid it for nine days.** `origin/main:BUILDING.md` is BYTE-IDENTICAL to `HEAD:BUILDING.md` (sha f1ba8ade207b1a4f, 138 lines, carrying "Corrected 2026-08-17"): the 2026-08-21 promotion `9470a50d9` already took it, so the corrected root page has been public for four days. **ITEM 2 REMAINS** -- `BUILDING.md` under `docs/getting-started/` is still on `main` and needs removal in a rebuild-review-commit window, NOT a promotion run. THE THREE PREMISES THIS ROW CARRIED WERE ALSO CHECKED AND ARE ALL FALSE: the getting-started copy does NOT repeat the presets claim (47 lines, zero mentions of DOTTALK_PRODUCT or windows-lean); the public README does NOT link it (it links the SITE and the SIBLING `POSIX_WSL_QUICKSTART.md`, and `git grep` across main finds no reference at all); and it is NOT a quickstart tier -- `docs/getting-started/` is 4 files on main and THE SAME THREE on development, so it is one orphan, which is `PROMOTION_PROCESS.md` section 6's classification verbatim. **THE METHOD FAILURE IS THE DURABLE PART:** the first pass read `origin/main` without checking that the ref was last fetched 2026-08-16. The three premises survived re-measurement by LUCK (that promotion did not touch them); what the stale ref hid was item 1, in the document recommending it as work. A remote ref is a CACHED ANSWER and has a freshness stamp -- `git log -1 --format=%ad origin/main`. Record: `docs/maintenance/OI010_EXAMINED_V1.md` sections 11-14. **CITATION RESPELLED 2026-08-30, NOT SUPPRESSED.** `cited-paths` reported this row's `docs/getting-started/` copy as MISSING -- correctly by its own rule, and misleadingly as to meaning: **the file's absence on `development` IS THIS ROW'S SUBJECT.** It exists on `main`. The line-suppression marker was the wrong instrument because it suppresses a LINE and this line also cites `docs/maintenance/OI010_EXAMINED_V1.md` and `docs/manuals/developer/dev/dev-21-build-system.md`, both tracked and both of which must keep being audited -- the same trap the AIF-135 register row hit the same morning, where the fix was spelling rather than suppression. So the path is written as a filename and a directory rather than one token: neither half matches the path scanner, both halves still read, and the branch is now named, which the original citation never did. **AND THE MARKER IS DESCRIBED RATHER THAN NAMED IN THIS ROW, WHICH IS NOT FUSSINESS.** The first draft of this note wrote the marker literally while explaining it, and that DISARMED THIS ROW -- both tracked paths above silently stopped being audited, measured immediately after. It is the exact hazard `check_cited_paths.suppressed_lines()` documents in its own docstring: *the marker suppresses any line containing it, INCLUDING A LINE THAT ONLY NAMES IT*, with the R126 ruling's self-disarming sentence as the recorded precedent. Reproduced here within a minute of reading that docstring. **The gate was right and a reader would have been wrong**, which is the third instance today of a measured absence being mistaken for a defect -- and the disarming is a fourth, where a measured presence silenced a check nobody would have noticed going quiet. |
| OI-011 | 2026-08-17 | 2026-09-07 | repo | **THE DEVELOPER MANUAL IS ALMOST ENTIRELY UNTRACKED: 21 chapters on disk, ONE in git.** Measured 2026-08-17 with `git ls-files docs/manuals/developer/dev/`: the only tracked chapter is `dev-20-semantic-field-hooks-and-pronouns.md`. Every other chapter -- `dev-00-evidence-rules` through `dev-19-help-meta-crosswalk-and-manual-generation`, twenty files including `dev-02-build-and-runtime-layout` -- exists on disk only. They are not ignored (they show under `-uall`); they were simply never added, and `status.showUntrackedFiles=no` (OI-008) meant no `git status` ever mentioned them. **This is authored prose, not generated output**: `dev-00` sets evidence rules and `dev-17` sets contributor rules, which is not what a generator emits. Consequences: a fresh clone gets one chapter of the developer manual; nothing in CI or the gates can validate them; and the work is one `git clean -fd` from gone with no history to recover from. NEAR DATE ON PURPOSE -- this is the largest measured cost of OI-008 and the cheapest to fix if it is simply an oversight. Decide first whether the twenty are (a) intended to be tracked and never were, (b) drafts deliberately held back, or (c) superseded by `manualgen` output; only (a) is a simple `git add`. Found while adding a build chapter, which had to be renumbered from `dev-19` to `dev-21` because an untracked `dev-19-help-meta-crosswalk-and-manual-generation.md` already held that number and no tracked listing could show it. **WIDENED 2026-08-17, same day: the chapter count understated this badly. The WHOLE canonical manuals home is adrift -- `docs/manuals/` holds 9,641 files on disk and 14 in git.** Per subtree, on-disk / tracked: `anchors` 2/0, `assets` 21/8, `command_reference` 3/0, `developer` 9,598/6, `dist` 14/0, `student` 1/0, `user` 1/0. Inside `developer`: `manualgen` 9,536/3, `proofs` 32/0, `dev` 23/2, `generated` 3/0, `04_selfdoc` 2/0, plus loose `DEVELOPER_MANUAL_DRAFT_COMBINED.md` and `README.md`. **The decisive measurement is that NONE of it is gitignored** (`git check-ignore` returns nothing for `manualgen`, `dist` or `user`), so this is untracked by OMISSION, not by policy -- nobody decided to exclude it; the exclusion happened. Split the ruling in two, because the two halves are different questions: (1) the AUTHORED tiers are small and nearly all absent -- `command_reference`, `dist`, `student`, `user`, `anchors`, `proofs` and the `dev` chapters total under 100 files and are the real loss risk; (2) `manualgen` is ~9.5k MDO-* status artifacts that are plausibly REGENERABLE output, in which case they want a `.gitignore` entry rather than a `git add`, and saying so turns an accident into a decision. Do (1) first; it is the cheap half and the one where a `git clean` costs work that cannot be rebuilt. **PROGRESS 2026-08-18 on half (2), and one claim above is CORRECTED.** The `manualgen` bulk was measured: 11 GB, 96% in two directories (`backups` 5.5 G, `accepted_catalogs` 5.1 G), both dominated by five 1 GiB SPARSE LMDB preallocations -- `data.mdb` files of exactly 1,073,741,824 bytes holding a few KB, and git does not preserve sparseness. **No `.gitignore` change is needed and the earlier "NONE of it is gitignored" is wrong at the level that matters**: `**/*.mdb` (:224), `**/*.cdx.d/` (:218), `docs/**/backups/` (:323) and `manualgen/**/*.csv` (:341) already cover it, leaving residual exposure of 38 files / 0.07 MB in `accepted_catalogs`. `check-ignore` returning nothing for the `manualgen` DIRECTORY was read as covering its contents; it does not. That misreading is not cosmetic -- `docs/**/backups/` is exactly why twelve governance attestations (gate acceptance, canonical-apply authorization, human review decisions) sat unreachable: no `git add`, however broad, could have taken them, so the tree could not report its own gap. They are rescued to `docs/maintenance/manualgen_acceptance_evidence/`, flattened per maintainer instruction with run ids in the filenames and full original paths in a MANIFEST; verified 0 of 12 still ignored. Nine were expected and twelve landed -- two basenames had distinct siblings, including four 786-byte overlay plans in THREE different contents. Sources are NOT deleted; removing the 11 GB stays a separate maintainer step so the rescue can be reverted alone. Half (1), the ~72 authored files, is still open. **HALF (1) LARGELY CLOSED 2026-08-24, and this row's own headline was STALE.** It reads '21 chapters on disk, ONE in git'. Measured today: `docs/manuals/developer/dev/` holds 25 files with FOUR tracked -- `dev-19-external-references`, `dev-20`, `dev-21` and `dev-22` landed after this row was written. 20 numbered chapters plus one stray `.txt` were untracked, 53 KB in total. **The three-way question this row poses is answered, and the answer is (a) intended to be tracked and never were.** The decisive measurement is not the file count: `docs/manuals/developer/README.md` is TRACKED and links four chapters -- `dev-04`, `dev-05`, `dev-09`, `dev-13` -- and ALL FOUR were untracked, so a fresh clone got a manual index whose every chapter link was broken. Nobody holds back a draft while linking it from the committed index. Corroborating: `dev-00` opens with hand-written YAML front matter (`page_id`, `status: DRAFT_PATCHED`, `last_verified`, `evidence_classes`), which is editorial metadata and not what a generator emits, so (c) superseded-by-manualgen is out too. The 20 numbered chapters are now tracked. **WHAT IS DELIBERATELY NOT IN THAT COMMIT, so it is a decision and not an oversight:** (i) `DotTalk++x64base Field Types & Validation System.txt` -- 7,258 bytes, 454 lines carrying non-ASCII, a stray `.txt` rather than a numbered chapter, and a filename containing spaces and an ampersand. It would fail the house-style gate on sight and it may be imported third-party material; it wants its own look. (ii) THE `dev-19` COLLISION IS STILL LIVE and is now collision IN GIT rather than on disk alone: `dev-19-external-references.md` and `dev-19-help-meta-crosswalk-and-manual-generation.md` are both tracked and both claim chapter 19. That duplicate number is exactly what forced the `dev-21` renumber this row describes, so it is the next thing to rule on -- renumbering is a rename plus every citation, which is a decision, not housekeeping. **CLEARED 2026-08-24 on the steward's instruction.** SENIORITY DECIDED IT: `dev-19-help-meta-crosswalk-and-manual-generation.md` dates from 2026-07-08 and is the file that forced the `dev-21` renumber; `dev-19-external-references.md` dates from 2026-08-20 and took a number already in use -- the same mistake, in the other direction. So the NEWCOMER moved, to **DEV-23**, which is max+1 over a fully occupied 00-22 and not a gap (gaps are not free real estate; the same rule `next_aif.py` states for AIF numbers). THE COLLISION WAS IN THE IDENTITY METADATA TOO, not just the filename: BOTH files declared `page_id: DEV-19`, so renaming the file alone would have left the identical defect one layer down, which is the shape this house keeps finding. Heading and `page_id` moved with the file. Citations: exactly one outside this register -- `labtalk/ai_portal/AIF120_APPLICATION_UI_DSL_SEED_V1.md:30` -- updated. `docs/maintenance/REPO_HYGIENE_TRIAGE_CHARTER_2026-08-04.md:156` cites the INCUMBENT and is unaffected. `docs/manuals/developer/README.md` links neither. (iii) The other authored tiers named above -- `command_reference` 3/0, `dist` 14/0, `student` 1/0, `user` 1/0, `anchors` 2/0, `proofs` 32/0 -- are untouched. ONE CONTENT EDIT WAS MADE AND IS NAMED HERE RATHER THAN LEFT TO BE DISCOVERED: `dev-15-selfdoc-pipeline.md` line 148 carried a single U+2019 in "project's", which the house-style gate blocks; it was normalized to an ASCII apostrophe. One character, no meaning changed. NEXT LOOK moved to 2026-09-07 for the remainder (i)-(iii) rather than left stale. **HALF (2) RESIDUAL RE-MEASURED 2026-08-25 and it is now SMALLER and NAMED.** The 2026-08-18 figure of "38 files / 0.07 MB in `accepted_catalogs`" has moved: of 39 files under `docs/manuals/developer/manualgen/accepted_catalogs/`, `manualgen/**/*.csv` now takes 15, leaving **24 files / 58,873 bytes untracked and NOT ignored** -- 13 x64 DBFs across TWO accepted catalogs (`man_catalog_v1` 8 tables, `manstar_native_reference_v1` 5 tables, the second one absent from every prior inventory here), 5 CDX indexes, 4 MDO acceptance summaries, `man_catalog_v1_manifest.json`, and the tree `README.md`. Surfaced by a WIDOW advisory naming MANHASH.dbf alone, which understates it: MANHASH is not special, NONE of the tree is tracked, and only MANHASH raised an advisory because only MANHASH got cited by a document. **The case for (a) track-them is now evidenced rather than assumed:** a Phase 6 read of the catalog on 2026-08-25 found the published developer manual DRIFTED from its accepted hash 4h 26m after promotion and unnoticed for 90 days -- the accepted catalog is the only thing in the tree that can detect that, and a `git clean -fd` currently costs all of it with no history. 58 KB, explicit paths, gated and manifest-carrying. See `docs/maintenance/lanes/full_stack_documentation/runs/DOCFLUSH-20260812-001/manualgen_phase/PHASE6_CATALOG_VERIFICATION_V1.md`. |
| OI-012 | 2026-08-17 | 2026-09-08 | site | **The published flavor/index table tells students x64 can only use CDX, and that is two errors in one row.** `x64base-site` `content/docs/engine/dbf-flavors-and-indexes.mdx:25`: the V64 VFP/x64 row gives `SET ORDER` default `.cdx`, accepted containers `.cdx`, and the note "CDX is the command-facing container; LMDB is the physical backend". (1) **`.cnx` is missing from V64's accepted containers** -- `docs/manuals/assets/diagrams/index_order_cdx_lmdb_v1.svg` records "CNX ... v64 accepted 2026-08-09", so the table predates the acceptance. (2) **"LMDB is the physical backend" is only half** -- the same diagram states "CDX's backend depends on RESIDENCE: LMDB on disk, native in vRAM", and the code agrees: `include/xindex/cdx_backend.hpp:78` returns `maintainsIncrementally() = true` while `include/xindex/cdx_native_backend.hpp:64` returns `false` with the comment "native CDX does not incrementally maintain on mutation in v1 ... just mark the container stale". The vRAM native CDX-V64 is the minidb fallback and is unmentioned. NET EFFECT: V32 is documented with two containers and V64 with one, when V64 now has the most options. NOT a site-wide blindness -- CNX appears on 25 site pages; this is one table row plus its echoes. CHECK THE ECHOES WHEN FIXING: `content/docs/engine/cdx-lmdb-indexing.mdx:69` (the whole page is named for the two-layer model), `content/docs/engine/architecture.mdx:150`, `content/docs/dev/third-party-acknowledgements.mdx:80`, and `docs/cases/CASE_ENG_010_INDEX_NAVIGATION_CDX_LMDB.md`, which that page cites as stating the teaching rule. PRESERVE WHILE EDITING: INX and IDX are different things and must not be merged -- INX is a single-tag on-disk format (`orderstate::isInx` sits beside `isCnx`/`isCdx`), IDX is `category: education`, "memory-only educational index lab" per `src/cli/cmd_idx.cpp`. Also missing everywhere: WHEN the index moves (direct write vs at-COMMIT vs `SET INDEXTXN` bulk txn); see `AIF119_M2_INDEX_BINDING_PROPOSAL_V1.md`. Public and teaching-facing, hence the near date. **FIXED 2026-09-01 IN `x64base-site`, THREE FILES.** (1) The V64 row of `content/docs/engine/dbf-flavors-and-indexes.mdx:25` now reads accepted containers `.cdx` or `.cnx`, and its note replaces "LMDB is the physical backend" with the residence rule: LMDB on disk, native CDX-V64 in RAM, plus the auto-resolution condition (`.cnx` only when a `.cnx` exists and no `.cdx` does) and the reassurance that the DEFAULT IS UNTOUCHED -- because a student reading that a second container is accepted will otherwise wonder whether theirs changed. (2) `architecture.mdx:150` carried the same unqualified half and now names residence. (3) `third-party-acknowledgements.mdx:80` is a CREDIT page, so it was QUALIFIED RATHER THAN REWRITTEN -- "LMDB is the physical backend on disk", with one parenthetical for the RAM path, because diluting the acknowledgement to teach indexing would serve neither. **ONE NAMED ECHO NEEDED NOTHING AND THAT IS WORTH RECORDING:** `cdx-lmdb-indexing.mdx:69` was ALREADY CORRECT -- it bolds "**on disk**" and states the CNX auto-resolution condition verbatim. The drift was never site-wide; the page named for the two-layer model had the model right and the SUMMARY TABLE elsewhere had it wrong, which is the ordinary direction for this failure: the condensed restatement loses the qualifier. **THE INX/IDX PRESERVATION THIS ROW ASKED FOR IS NOW EXPLICIT** rather than merely respected -- a sentence under the table states that INX is a single-tag on-disk container and IDX is a memory-only educational lab, so the next editor is told not to merge them instead of being trusted to know. NOT DONE, and out of this row's scope as written: `docs/cases/CASE_ENG_010_INDEX_NAVIGATION_CDX_LMDB.md` lives in `ccode`, not the site, and WHEN the index moves (direct write vs at-COMMIT vs `SET INDEXTXN`) is still unwritten everywhere -- that is AIF119_M2's, not a table row's. Site changes are UNCOMMITTED in `D:\dev\x64base-site` on `codex/lean-sites-publish`, a clone with ZERO hooks installed (OI-024), so nothing gated them. |
| OI-003 | 2026-08-17 | 2026-10-01 | repo | `build.ps1` has no way to build the module alone: line 164/170 hardcode `--target dottalkpp pydottalk`, so `-WithPyDotTalk` always builds the CLI too. The lean route now exists (`build_pydottalk.ps1` -> `bindings/pydottalk` standalone), so this is no longer blocking -- but anyone reaching for `build.ps1 -WithPyDotTalk` still gets the heavy build with no switch to say otherwise. Either add `-PyOnly`, or make the wrapper the documented entry point and say so in `build.ps1`. |
| OI-013 | 2026-08-21 | 2026-09-05 | repo | **`docs/gui` is 14 files on disk and 1 tracked, and the one is tracked only by accident.** `GUI_BUILD_AND_RUN_V1.md` reached git on 2026-08-21 as a side effect of a rename commit -- it showed as `create mode`, meaning the GUI build-and-run instructions had never been committed. The other 13 remain untracked and are not ignored. Same shape as the `docs/ui` widow (closed in `1a40c97a7`) and the `src/gui/wx/res` widow (closed in `686c7e14e`), where `main_frame.cpp` included a header that was not in the repository and a fresh clone could not compile the Workbench. Check whether any of the 13 is cited by tracked source or by the build before deciding; that is what made `res/` urgent rather than tidy. **MEASURED 2026-08-26, AND IT IS THE URGENT KIND. `src/cli/cmd_maint.cpp` CITES THREE OF THE UNTRACKED THIRTEEN** -- lines 163-165 print `docs/gui/GUI_SYNC_DEVELOPMENT_WORKFLOW_V1.md`, `docs/gui/UNIFIED_GUI_CORE_V1.md` and `docs/gui/GUI_LOCALIZATION_MESSAGE_CONTRACT_V1.md` as `MAINT GUI` output. That is a RUNTIME widow, not a documentation one: the shipping binary tells a user to read three files a fresh clone does not have, which is strictly worse than the `res/` case (that one failed loudly at compile time; this one succeeds and misdirects). Eight further TRACKED documents cite the set -- `docs/contracts/CONTRACT_REGISTRY_V1.md` (rows 57-58 register two of them as active contracts, by path), `CONTRACT_LIFECYCLE_V1.md`, `SELF_DOC_APPS_INDEX_v1.md`, `SELF_DOC_SUBSYSTEM_MATRIX_v1.md`, `docs/ui/CORE_UI_PRINCIPLES_V1.md`, `docs/database/DATABASE_SAFETY_CONTRACT_V1.md`, `VALUE_LOCALE_COLLATION_CONTRACT_V1.md`, and `labtalk/reports/selfdoc/x64base_engine_feature_crosswalk_v1.md`. So the contract REGISTRY lists two contracts whose text is not in the repository. **PRE-COMMIT CHECKS ARE CLEAN:** zero machine-absolute paths (so this does not feed OI-017), zero non-ASCII, and every `docs/*.md` path the thirteen themselves cite resolves on disk (so committing them opens no new widow). ~84 KB, ~2400 lines, authored 06-29 to 07-03 and untracked since. **METHOD WARNING, because two earlier passes of this same check returned the OPPOSITE answer:** `grep -l -F "$stem" $(cat tracked.txt)` over 5413 tracked files overflows the argument list, and with stderr sent to `/dev/null` the failure is indistinguishable from "no matches" -- it reported all 13 as orphans, twice, once with the `.md` suffix and once without. A single ripgrep pass over the tree found 23 citing files immediately. Do not run the per-file loop form; it is the AIF-118 shape wearing a shell idiom. |
| OI-014 | 2026-08-21 | 2026-09-05 | decision | **`PROMOTION_PROCESS.md` asserts something about this tree that is false, and the sentence is load-bearing.** Lines 79 and 123 both say `messaging` / `metadata` / `sandbox` are "versioned in development, never published". Measured 2026-08-21: `dottalkpp/data/messaging` is 4 files on disk, **0 tracked**, and NOT gitignored -- untracked by omission. Two of the four are `.dbf` tables. The claim is what explains why those lanes are absent from `PROMOTE.manifest`: they are supposedly safe because they are versioned. If they are not versioned, the manifest is not protecting them from publication -- nothing is protecting them from loss. Either `git add` the four, or correct the sentence. Do not leave a governing document asserting a safety property that does not hold. |
| OI-015 | 2026-08-21 | 2026-09-29 | repo | **The staging build cannot cover the GUI lane, so promotion step 3 structurally misses it.** `C:\x64base\build\CMakeCache.txt` carries `DOTTALK_WITH_WX:BOOL=OFF`, so the 2026-08-21 staging build compiled the engine and all ten test executables and produced no `dottalk_wb.exe` at all. That build is still worth running -- it proves `main`'s engine source compiles from a clean tree -- but `PROMOTION_PROCESS.md` step 3 reads as though building staging validates what was promoted, and for anything behind `DOTTALK_WITH_WX` it cannot. The `wb` rename was proven on `D:` instead, which is where the two real defects were caught. Either enable the switch for promotion builds, or say plainly in step 3 which lanes staging does not cover. **WIDENED 2026-08-24: THE DEVELOPMENT SIDE HAD THE SAME HOLE AND IT WAS NOT WRITTEN DOWN ANYWHERE.** This row records that STAGING cannot cover the GUI lane. It could not cover it on `D:` either: `src/CMakeLists.txt:476` gates `gui/core` behind `DOTTALK_WITH_GUI`, which defaulted OFF, so a plain `build.ps1` did not COMPILE `src/gui/core` at all. MEASURED on `508325324`: a build reported "Built OK" while `session.obj` was a full DAY older than `dottalkpp.exe`, immediately after a commit that rewrote 132 call sites in `session.cpp`. **THE GATE IS AT CONFIGURE TIME**, which is why the obvious workaround does not work either -- `cmake --build build --config Release` over the whole tree built 25 targets and walked straight past the one that had changed, because the previous configure had excluded it from the solution. THE COST WAS SILENCE, NOT SLOWNESS: ctest went **18 -> 20** the moment the GUI was switched on, and the two that appeared -- `dottalk_gui_core_async_smoke` and `dottalkpp_gui_match_count_test` -- had not been failing, they had not been RUNNING, and nothing in a green summary said so. Same shape as `REGRESSION ALL` passing ten specs over a commit that rewrote the free-slot allocator without once exercising it. **FIXED 2026-08-24 on the steward's ruling ("-WithGui should be the default"):** `build.ps1` now follows its own idiom for a feature that is ON -- an opt-OUT `-NoGui`, like `-NoIndex` and `-NoTV` -- with one named `$GuiEnabled` that both the configure line and the target list read. `-WithGui` is still accepted and still forces it on, redundant rather than wrong, because scripts and habits spell it. `-NoWx` was deliberately NOT added: wxWidgets is a heavier dependency and `DOTTALK_WITH_WX` stays opt-in, which means **THE HALF THIS ROW ORIGINALLY RECORDS IS STILL OPEN** -- `dottalk_wb` is still behind an opt-in switch at both ends, and promotion step 3 still cannot validate it. |
| OI-016 | 2026-08-21 | 2026-09-05 | repo | **`.github/workflows/copilot-setup-steps.yml` is now live on the PUBLIC repository and neither author read it.** It went out in the 2026-08-21 promotion (`9470a50d9`) as a `create mode`, having reached `main` for the first time. A workflow on a public repo runs on GitHub's infrastructure against the owner's account and can be triggered by events the repo does not control. NEAR DATE deliberately: this is the only item from that promotion that executes rather than merely being read. Read it, confirm the trigger and permissions are what you want public, and either keep it deliberately or remove it from `PROMOTE.manifest`. **EXAMINED 2026-08-28, THE DUE DATE. TRIGGER CONFIRMED SAFE; THE PERMISSIONS HALF IS NARROWED AND THE DATE MOVED TO 09-05.** The file was read against the PUBLISHED artifact (md5 e142f5d8, identical to origin/main at 9470a50d9). Its whole trigger block is `on: workflow_dispatch:` -- no push, no pull_request, no schedule, no pull_request_target -- so it is the ONLY one of the four published workflows an outside event cannot start, and this row's stated worry ("triggered by events the repo does not control") is FALSE for it. THE PERMISSIONS HALF COULD NOT BE ANSWERED BY READING THE FILE, which is why reading it did not close the row: with no `permissions:` block the job takes the REPOSITORY DEFAULT, a SETTING rather than a fact in the repo. Practice was split -- ci.yml pinned `contents: read`, release.yml pinned `contents: write`, this file and build-windows.yml inherited. FIXED FOR THIS FILE in 45dfb6b46, which pins `permissions: contents: read` (all checkout + apt-get needs) with a comment saying why. KEEP IT: publication was never accidental -- PROMOTE.manifest:52 is `.github/workflows/*.yml` and 23617ec67 added it deliberately; removing it from the manifest would un-publish ci.yml too. WHAT REMAINS AND IS WHY THE DATE MOVED RATHER THAN THE ROW CLOSING: read Settings -> Actions -> General -> Workflow permissions and confirm the default is what you want public. If it reads "Read and write", build-windows.yml still runs write-capable on `push: [main, dottalkpp-core]`. That is a browser check nobody but the owner can make. ALSO FOUND, NOT THIS ROW'S SCOPE: this file's header claims it mirrors ci.yml's ubuntu-core and says "keep the two in sync" -- they already differ in two ways (this installs `cmake`, adds setup-python@v5), inside two weeks, with the invariant held by a prose comment and nothing checking it. And build-windows.yml was found UNTRACKED-yet-PUBLISHED (OI-018's shape, live) and is the ONLY CI compiling DOTTALK_WITH_TV and DOTTALK_WITH_INDEX; tracked in 1860a0fb1, draft row at claude/OI020_DRAFT_BUILD_WINDOWS_UNTRACKED_AND_TESTS_CANNOT_FAIL.md, number NOT claimed. |
| OI-017 | 2026-08-21 | 2026-10-06 | repo | **71 machine-absolute paths across 14 files are already published in `main`.** Measured by the overlay guard on 2026-08-21. NOT introduced by that promotion -- every one of the 14 was already in `main` and the overlay re-copied identical content; zero of them appear in that run's 30-file change set. Worst offenders are functional rather than prose: `dottalkpp/data/cdxdemo.dts`, `dottalkpp/data/workspaces/manual.dtschema`, `dottalkpp/data/schemas/mcc_x64_output.txt`, `dottalkpp/help/find_repo_string.ps1` and `restore_dd058_backup.ps1` all carry `D:\code\ccode` and will not work for anyone who clones. `docs/gui/GUI_BUILD_AND_RUN_V1.md` was the fifteenth and was cleaned in `cac02a8b5`, which is the worked example. The guard already names them on every overlay, so the list is free; the work is sterilising them in `development` so the next promotion carries the fix. **STILL OPEN FOR THE THREE WORST FILES, re-measured 2026-08-25 against CURRENT main.** `9470a50d9`'s own message says "the data scripts carry relative paths instead of `D:\code\ccode`", and it did sterilise some -- but not these: `dottalkpp/data/cdxdemo.dts` main 4 / dev 4, `dottalkpp/data/schemas/mcc_x64_output.txt` main 4 / dev 4, `dottalkpp/help/find_repo_string.ps1` main 1 / dev 1. **WHAT CHANGED IS THAT THEY ARE NOW FIXABLE:** all three were tracked in development by `f6a8d71a6` (cited-path widow triage, 2026-08-25). You cannot sterilise in `development` what `development` does not track, which is why this row and OI-018 were stuck together. **PARTIALLY RESOLVED 2026-08-25, and TWO OF THE THREE ARE NOT DEFECTS.** Characterised before editing, because a blanket replace would have been wrong in both directions. (1) `dottalkpp/help/find_repo_string.ps1` -- FIXED. The one occurrence was a `.EXAMPLE` in comment-based help; it now reads `-Root .`, which is runnable and matches the second example that already did. Zero machine paths remain in the file. (2) `dottalkpp/data/cdxdemo.dts` -- NOT A DEFECT AND MUST NOT BE STERILISED. It is a DEFECT DEMONSTRATION: line 3 says `WILL FAIL BECAUSE IT WILL CONCATENATE THE ABSOLUTE PATH TO THE DEFAULT PATH DATA`, lines 5-7 record the resulting `warning: path does not exist` in comments, and line 9 then uses the direct `WORKSPACE OPEN <abs>` form that works. The absolute path is the SUBJECT MATTER; removing it destroys what the script demonstrates. (3) `dottalkpp/data/schemas/mcc_x64_output.txt` -- NOT A SCRIPT. It is a CAPTURED CLI TRANSCRIPT: `. SCHEMAS LOAD WORKSPACES\MCC_x64` is the prompt echo and the four paths are what the tool PRINTED. Editing it would falsify a record of what happened. **SO THIS ROW'S FRAMING IS WRONG FOR TWO OF ITS OWN HEADLINE FILES** -- they are described as "functional rather than prose" that "will not work for anyone who clones", but one is a demo whose point is the failure and the other does not run at all. **THE REAL QUESTION FOR BOTH IS PUBLICATION, NOT STERILISATION:** both match `PROMOTE.manifest` and therefore publish to the public repository. A scratch defect-demo and a captured error transcript on the public repo is a manifest-scope decision, not a path-hygiene one. The remaining 11 files of the original 14 are unexamined and may split the same way. |
| OI-018 | 2026-08-21 | 2026-09-12 | decision | **The promotion overlay publishes from the FILESYSTEM, not from git, so `main` carries files that have no history in the source of truth.** Measured 2026-08-21 while citing OI-017: `dottalkpp/data/cdxdemo.dts`, `dottalkpp/data/schemas/mcc_x64_output.txt` and `dottalkpp/help/find_repo_string.ps1` are each **0 tracked in `development` and 1 present in `main`**. `rebuild-staging.ps1` globs `PROMOTE.manifest` matches with `Get-ChildItem` and applies `.gitignore` as a deny guard, so a file that is untracked AND not ignored sails straight through to the public repository. This is the inverse of a widow and worse: a `git clean` in `development` would delete the only original while the published copy survives, with no commit anywhere showing where it came from. It also explains part of OI-017 -- the worst local-path leakers are files nobody tracks, so nobody reviews them. Two candidate fixes: have the overlay refuse an untracked path (cheap, and it turns this into a loud failure), or require the manifest to match only tracked files. Decide which, because the current behaviour means `PROMOTE.manifest` is not actually the allow-list the process says it is. **HALF STALE AS OF 2026-08-25.** This row's decisive measurement was that `dottalkpp/data/cdxdemo.dts`, `dottalkpp/data/schemas/mcc_x64_output.txt` and `dottalkpp/help/find_repo_string.ps1` are "0 tracked in development and 1 present in main" -- the inverse-of-a-widow case where the overlay publishes from the FILESYSTEM with no history in the source of truth. **ALL THREE ARE NOW TRACKED**, by `f6a8d71a6`, so the next promotion publishes them from git. That is the outcome this row wanted, reached as a SIDE EFFECT of a cited-path widow triage rather than by deciding the question. **THE DECISION THIS ROW POSES IS STILL UNMADE** -- whether the overlay should REFUSE an untracked path, or the manifest match only tracked files. Three files being fixed by accident does not answer it; `rebuild-staging.ps1` still globs the filesystem, so the next untracked-and-unignored file sails through exactly as before. |
| OI-019 | 2026-08-24 | 2026-09-07 | repo | **SIX OF THE FIFTEEN DEFAULT-SUITE REGRESSION SPECS ARE NOT IN THE REPOSITORY, and `cmd_regression.cpp` -- which IS tracked -- registers them by name.** Measured 2026-08-24 by parsing `kRegressionSpecs` and checking each script with `git ls-files`: 54 specs registered, **15 untracked**, of which **6 are in the default suite** -- `NONDESTRUCTIVE`, `INDEX_X32`, `INDEX_X64`, `X64_METRICS`, `LANGUAGE`, `RELJOIN`. All 15 exist on disk and **none is gitignored**, so this is omission, not policy. Wider: `dottalkpp/data/scripts` holds 332 `.dts` on disk and **87 tracked**. **CONSEQUENCE: a fresh clone cannot run 40% of its own default regression suite.** `REGRESSION ALL` would fail to resolve six scripts, and nothing in the tree would explain why -- the registration is tracked, the target is not. Everything validated in the 2026-08-24 session (the allocator move, four spec promotions, WORKSPACE PURGE) was validated against a suite that is substantially absent from the repository. This is OI-011's shape pointed at the TEST SUITE rather than the manual, and it is worse in one respect: the citing artifact is ENGINE SOURCE, not prose. **SPLIT IT, because the two halves are different questions.** (1) THE 15 REGISTERED SPECS ARE NOT A JUDGMENT CALL -- tracked code names them, so either they are in the repo or the registration is a lie. Verified stageable: all 15 present, none ignored, 55 KB total, and all ASCII in CONTENT (four carry CRLF, which a `[^ -~]` scan reports as non-ASCII and which `text=auto` normalizes in the blob; `.gitattributes:13` asks that `.dts` be checked for SHA-pinning before renormalizing and none of the four computes or asserts a hash, and no tracked file pins them by digest -- checked). (2) The other ~245 `.dts` need the OI-011 triage: intended-and-never-added, scratch, or generated. Do (1) first; it is the half where the cost is measurable and the answer is not a matter of taste. FOUND while reading the `cited-paths` gate output on the OI-011 commit, which named `x64_matrix_metrics_boundary_canary.dts` as a widow cited by `dev-08` and `dev-16`. One widow was the visible edge of six. |
| OI-021 | 2026-08-30 | 2026-09-29 | lane | **AIF-108, the Memo Object Challenge, is ASLEEP FOR 30 DAYS by owner ruling** (*"we can reset the memo challenge for later, make it sleep for 30 days"*). It is a test-design lane -- *chartered: NO engine change proposed; this lane produces tests and a protocol for receiving tests* -- holding 100 test ideas opened as a governed challenge to external AI agents through the house BBS. **WHY IT IS BEING SLEPT RATHER THAN CLOSED:** ten of its recursion ideas were blocked on memo-sidecar carriage, that carriage landed 2026-08-12, and the row still read BLOCKED eighteen days later -- so the lane has been accruing an obligation nobody was told about. Sleeping sets a date; closing would lose the ten ideas. **WHAT IS NOT ASLEEP:** the engine defects found while answering the owner's two-level memo question are GRANDFATHERED INTO AIF-120 (R133) and stay live there. The two files that cite AIF-108 for engine behaviour -- `include/dottalk/minidb_hydrate.hpp` and `src/cli/cmd_workspace.cpp` -- are re-pointed at AIF-120 in the same commit, because a sleeping test lane must not be the cited authority for shipped code. **ON WAKING:** re-read AIF-120's amendment first; several of the ten ideas may already be answered by the probe. **OI-020 IS DELIBERATELY SKIPPED** -- it is drafted in `claude/OI020_DRAFT_BUILD_WINDOWS_UNTRACKED_AND_TESTS_CANNOT_FAIL.md` and not yet rowed. Writing over a number someone has already drafted against is how AIF-146 was burned. |
| OI-022 | 2026-08-30 | 2026-09-20 | repo | **`dottalk_bbsd` is ENABLED and OUTSIDE the standard build, so nothing proves it links.** `DOTTALK_BUILD_BBSD` defaults ON and the target configures on every run, but `build.ps1` builds SEVEN targets and this is not one of them -- measured 2026-08-30 from its own banner (`dottalkpp`, `dottalk_gui_core`, `dottalk_gui_core_async_smoke`, and the four tests). **A CHANGE THAT BREAKS IT PASSES THE HOUSE BUILD IN SILENCE**, which is the mirror of the standing rule that a single-target build never proves the other six: a target outside the seven is never proven at all. Found while moving `fields_mgr.cpp` into `xbase.lib`, an edit that removed a source from THIS target's list and could only be verified with `cmake --build build --config Release --target dottalk_bbsd`. It linked -- but only after the owner **stopped the running daemon**, which had its own exe open (LNK1104 is cannot-open-OUTPUT; an unresolved symbol would have been LNK2019). **AND THAT MAY BE THE REASON IT IS OUTSIDE THE SEVEN, WHICH WOULD MAKE THE OMISSION A DESIGN CHOICE NOBODY WROTE DOWN:** you cannot relink a running executable, so a daemon in the default target list would fail the house build for anyone with it up. Offered as a HYPOTHESIS -- `build.ps1` says nothing about why, and no document found so far does either. **THE DECISION:** add it to the seven and accept that the build fails while the daemon runs; or leave it out and SAY SO in `build.ps1`'s banner, so the omission reads as a choice rather than an oversight; or add a named convenience target that stops, builds and restarts it. Owner's call; not made. |
| OI-023 | 2026-08-30 | 2026-09-20 | repo | **MOVING A TRACKED FILE BREAKS ITS CITATIONS SILENTLY, AND ONE OF THEM WAS A CURATED LIST THAT CAN LOSE AN ENTRY WITHOUT SAYING SO.** Moving `fields_mgr.cpp` out of `src/core/` and into `src/xbase/` on 2026-08-30 left EIGHT files naming the old location. **AND THIS ROW'S FIRST DRAFT WAS THE NINTH** -- it wrote that old location as one path token while explaining the hazard, and `cited-paths` reported it MISSING immediately. Respelled here for the same reason OI-010 was: a historical reference to a file that is correctly gone must not parse as a live pointer. Third instance in one day. `cited-paths` audits only documents in the CHANGE SET, so it saw none of them; the breakage surfaced two commits later when an unrelated edit happened to touch the intake register. Six live pointers were corrected in that commit. **THE ONE THAT IS NOT PROSE:** `tools/datadict/extractors/physical_dictionary_extractor.py` carries `KEY_SOURCE_ANCHORS`, a curated list of the source files that matter most, and `find_anchor_runtime_proof()` walks it as `if p.exists(): proofs.append({... "status": "present" ...})` -- **NO `else`, and `status` can only ever hold one value.** An anchor that moves drops out and the proof list goes from 26 entries to 25 with nothing anywhere recording the loss. That is the AIF-118 shape inside a PROOF GENERATOR: the artefact whose job is to attest presence cannot express absence. `DD-005`'s source map mirrors the same list and would drift the same way. **NOT FIXED -- another lane's tool.** The repair is small and obvious (emit a `status: missing` row, or fail) but the datadict lane owns what its manifest is allowed to say. **THE WIDER GAP, and it is the reason this is an item rather than a note:** nothing in this tree checks that a moved file's citations followed it. The gates are change-set scoped by deliberate design -- `cited-paths` says so in its own header, because a gate reporting every pre-existing widow stops being read -- so the very thing that makes them usable is what makes a rename invisible to them. A `git mv` needs a companion sweep and there isn't one. **AND THE SWEEP FOUND A SECOND, OLDER DRIFT IN THE SAME SPEC:** DD-005 line 66 also names a `cmd_vuse.cpp` under `src/cli/` and there is no `vuse` file anywhere in the tree -- not renamed, not moved, absent. **THE PATH IS SPELLED IN TWO PIECES HERE ON PURPOSE, AND THE FIRST DRAFT DID NOT DO THAT.** Written as one token it parsed as a live pointer, and the `cited-paths` gate reported it MISSING on the very next commit that touched this file -- correctly by its own rule, and misleadingly as to meaning, because the file's ABSENCE IS THIS SENTENCE'S SUBJECT. That made this row the SOLE source of a permanent advisory on a file the tree touches constantly, which is the failure `check_cited_paths.suppressed_lines()` names in its own docstring: a standing advisory trains people to skip the whole check. Respelled rather than suppressed, per OI-010, because this line must keep auditing the other citations around it. Fourth instance of this shape, third in two days. **AND THE QUESTION THIS PARAGRAPH LEFT OPEN IS NOW ANSWERED BY THE STEWARD, 2026-08-31:** *"vuse is an old command I deleted, it was a simple clone of cmd_use.cpp"*. So it became nothing -- it was a duplicate, and it was removed on purpose. Corroborated: the path enters history at `7c56022a1` (2026-05-03, the initial recovered baseline), is still PRESENT at `4c404bed5` (2026-06-22), and is ABSENT at `c86c18783` (2026-07-13, "Merge main and reconcile publication source") and in every ref checked -- HEAD, `origin/development`, `origin/main` and `main`. **WHICH MAKES THE DD-005 ENTRY CHEAPER TO FIX THAN THIS ROW ASSUMED AND STILL NOT OURS TO FIX.** Line 66 sits inside a fenced primary-sources block DIRECTLY BELOW `src/cli/cmd_use.cpp`, so the map listed a clone as a co-equal primary source for the same facts: deleting the line loses nothing, because the original is the line above it. Recorded for the datadict lane, NOT edited here -- that lane owns what its manifest may say, and the reason for holding off has changed from ignorance to ownership. **A METHOD NOTE WORTH MORE THAN THE FIX:** the removing commit could not be found by `git log --diff-filter=D`, on any ref, even with `--full-history` -- because the file went away in a MERGE and git does not diff merges by default. An empty deletion log is NOT evidence a file was never deleted; bracketing it between a commit that has it and one that does not is what actually answered it. Recorded beside OI-010's stale-ref lesson and OI-013's argument-list lesson, and one more was reproduced getting here: a recursive grep over the device mount returned EMPTY rather than erroring, which is exactly OI-013's warning, and it briefly read as "DD-005 does not cite it after all". It surfaced for the same reason as everything else here -- that document entered a change set for the first time in a while. **Which is the item in miniature: these citations do not rot when the code moves, they rot silently and are discovered by coincidence.** |
| OI-024 | 2026-08-31 | 2026-09-14 | repo | **THE GATES ARE A PER-CLONE HOOK INSTALL, SO NO FRESH CLONE ENFORCES THEM. REDRAFTED THE SAME DAY IT LANDED: THE FIRST DRAFT'S EXTERNAL-AI HALF WAS WRITTEN WITHOUT READING THE AI-BBS, AND THAT ERROR IS THE MORE USEFUL HALF OF THIS ROW.** Raised 2026-08-31 when the owner asked whether a paid GitHub AI subscription could be put to work here. **WHAT STANDS, MEASURED 2026-08-31 AND UNCHANGED BY THE REDRAFT.** The four published workflows run `tools/ci/source_policy.py` (LICENSE and README wording), an MSVC build, and a GCC build with ctest; a grep of the workflow directory for `prepush`, `gate`, `pre-commit`, `hooks`, `session_coordinator`, `repository_role_guard`, `check-aif` and `r_number` returns nothing. Zero house gates run in CI. One layer down, `tools/staging/prepush_gate.py` states it about itself: it runs at COMMIT time from the `pre-commit` hook, that hook lives in the repository's local hooks directory, the directory is not tracked, and its header says the wiring "cannot be documented from inside the repository except here". Confirmed: `git ls-files` matches no hooks path, `core.hooksPath` is unset, and this clone's six hooks arrived through `--install-hook`. **SO ENFORCEMENT IS A PROPERTY OF A CLONE, NOT OF THE TREE -- AND THERE IS A LIVE SECOND CLONE PROVING IT.** `D:\dev\x64base-site` is not a sibling project: its `origin` is this same repository, it sits on `codex/lean-sites-publish`, and it has **zero hooks installed** (samples only, `core.hooksPath` unset). Two working copies of one repository, one gated and one not, and nothing in the repository can tell you which is which. **WHAT DID NOT STAND, AND WHY IT IS RECORDED RATHER THAN QUIETLY REPLACED.** The first draft argued that a Copilot-authored pull request "meets the build, meets ctest, and meets none of the gates", and proposed running the gates in CI so agent work would be governed. It assumed the channel for an external AI is a GitHub PR arriving at CI. **IT IS NOT, AND HAS NOT BEEN SINCE 2026-07-25.** The owner's correction was blunt and correct: *"You are just ignorant of pseudo-chat/bbs."* **WHAT IS ACTUALLY THERE, read from the running site's AI views on 2026-08-31.** Governance sits at the IDENTITY layer, upstream of the gates entirely. An external AI is a MEMBER with a kind (Human / AI / External) and a default role; it authenticates over a loopback socket with Argon2id, passes an RBAC/POSTPERM gate per board, and `current_member()` attributes the real author before the post lands under a table FLOCK. Six boards with per-board post permission -- governance is owner-only, guestbook takes `bbs.guest`. Work is ASSIGNED on the board: a 2026-08-08 post reads `[assign:grok] Lane 1 write adapter M2`, names `docs/maintenance/LANE_L1_WRITE_ADAPTER_ASSIGNMENT_GROK_V1.md`, and instructs "claim a fresh AIF; attributed writes only". Grok filed a session handoff to the worklog board carrying `RUN=AIPR-20260725-001`. Two channels -- live BBS post and document intake -- converge on a HARVEST that must produce a run in `labtalk/registries/ai_runs.yaml`, an audit envelope, and an index entry in `labtalk/registries/ai_report_index.yaml`, and only then curate into an AIF claim plus an intake row. **THE AIF NUMBER IS THE JOIN** between an external agent's work and the register this house claims numbers from. **AND COPILOT IS ALREADY INSIDE IT** -- `AIPR-20260809-COPILOT-001`, provider Microsoft, a `review_needed_change_package` with a MANIFEST in `docs/maintenance/external_ai_intake/`. The first draft's closing worry, that vendor agents were outside the hub, was wrong within an hour of being written. **THE REAL GAP IS THE HOUSE'S OWN AND IT IS WIDER THAN THE HOUSE LAST MEASURED.** `docs/maintenance/MONITOR_HARVEST_CURATE_EXTERNAL_AI_V1.md` -- authored by `member.ai.claude.cowork`, an EARLIER SESSION IN THIS SAME MEMBER IDENTITY -- records four submissions with one indexed, and the published data-flow diagram draws the completeness gate ("has all records?", absent -> flag) as DASHED, meaning proposed. **RE-MEASURED 2026-08-31, twenty-five days later: the three it named uncurated are still uncurated.** `AIPR-20260725-001` appears 14 times in the runs ledger and ZERO times in the index; `evaldiff_eof_probe` and `AIPR-20260804` appear in neither. **AND THE WIDER COUNT IS WORSE THAN THE SAMPLE:** `docs/maintenance/external_ai_intake/` holds 12 tracked intake directories and the index holds 5 report_ids across three providers (Anthropic 3, Microsoft 1, xAI 1); **exactly 2 of the 12 directories are named anywhere in the index** -- and collapsing the seven `aif112_*` phase and amendment directories into one submission still leaves 2 of 7 distinct submissions indexed. So a submission can produce a run and an envelope, be worked, and never become findable, with nothing anywhere saying so. That is OI-023's proof generator with no `else`, one layer up: the artefact whose job is to attest coverage cannot express a miss. **A SECOND, SMALLER BLINDNESS FOUND WHILE MEASURING, and it is the same shape:** `ROOTS` in `tools/staging/check_cited_paths.py` lists ten prefixes and the workflow directory is not among them, so **no gate in this tree audits the workflow files at any time**. That is why OI-016 had to be found by hand and why `build-windows.yml` could be untracked-yet-published. One tuple entry, same as the `gui/uidef` fix that tuple's own comment records. **THE DECISIONS, REFRAMED BY THE ABOVE, and none is made.** (1) **Build the completeness gate.** It is drawn, specified and unimplemented, and it is the one that costs work today. (2) **Decide what the vendor branches are.** `origin` carries five `copilot/*` branches, two named `fix-repository-policy-job` and `fix-repository-policy-job-again` -- an agent pushing at the policy gate itself -- and four local `codex/*` branches. Copilot has an indexed intake report AND unindexed branches, so the two paths are both live and unreconciled: either branch work is a submission channel and harvest must see it, or it is out of scope and the row should say so. (3) **Only then, whether a green CI means "met the gates."** If yes, the gates must run there and a passing check becomes a claim the house can stand behind; if no, say so in the workflows so a green check is not read as an approval it never offered. The portability triage that follows is NOT done -- `tools/staging/` holds 18 non-test scripts and `tools/staging/prepush_gate.py` also invokes gates under `tools/coordination/`, `labtalk/ai_portal/` and `tools/fullstack_docs/`, so the population was never enumerated; `tools/staging/repository_role_guard.py` and `tools/staging/check_sandbox_git_guard.py` ask questions that have no meaning on a runner and would go green by confusion. **NOT A DEFECT IN ANY GATE.** Every one is correct and runs where it was wired to run. **AND THE METHOD FAILURE IS THE DURABLE PART:** a session reasoned for an hour about how to govern external AI, in a tree whose external-AI governance was designed, built, running, published on its own website, and documented in a note written by a prior session under the same member identity. Nothing in the change set pointed at it; the gates cannot point at it; the AI views are served by `tools/reports/serve_dynamic_reports.py` and are not in this repository's reading path at all. The tree could not tell a session what the tree already knew. |
