# Open items -- the rung below a lane

Small, real, deferred work. Too small for an AIF lane, an SDLC gate, or a PDLC
project; too real to lose. One line each.

**Why this file exists (2026-08-17).** The coordination ladder had a documented
lightest rung -- quips (`AI_SESSION_COORDINATION_PROTOCOL_V1.md`) -- but quips
live in a gitignored inbox and do not survive a fresh clone, so parking anything
there loses it. Above that, the next rung was a claimed AIF lane, which is heavy
for "check a DNS record some day". Between the two there was nothing, so small
items went into chat and evaporated.

**Why it nags rather than merely records.** AIF-006's own warning text carries
the measurement: an obligation with no gate held at **33 percent** compliance
while gated obligations held **83-94**. A register nobody surfaces is a register
that decays. `tools/coordination/check_open_items.py` runs in the pre-push gate
and prints any row whose NEXT LOOK date has passed. It never blocks -- these are
deferred by choice, and a gate that blocks on them would teach people to delete
rows instead of doing them.

## How to use it

- **Add** a row. Take the next id. Set NEXT LOOK to when you actually want to be
  reminded, not tomorrow. A date far out is a legitimate answer.
- **Snooze** by moving NEXT LOOK. That is not cheating; it is the point. But the
  RAISED date never changes, so an item you have snoozed six times shows it.
- **Close** by deleting the row and, if it produced anything durable, saying so
  in the commit message. No closed-items graveyard here -- git already has one.
- **Promote** to an AIF lane if it grows. Claim a number, cite this id, delete
  the row.

## WHERE it gets done

The column that stops this becoming a code-only list. An item whose work happens
at a registrar or a hosting panel can NEVER be closed by a commit, and a register
that assumes otherwise silently mis-files every infrastructure item it holds.

`repo` -- a change in this tree - `site` -- the x64base-site tree -
`registrar` / `host` / `dns` -- an external control panel -
`decision` -- needs a ruling before any work starts

## Items

| id | raised | next look | where | item |
| --- | --- | --- | --- | --- |
| OI-001 | 2026-08-17 | 2026-12-15 | dns | `derald.com` serves an unrelated third party over an invalid certificate. Registry checked 2026-08-17: NOT lapsed -- registered 2004, expires 2027-06-24, GoDaddy, `NS23/NS24.DOMAINCONTROL.COM`, all four client locks set, last changed 2026-07-08 (two days before `c244300da` retired it as a support host). Check the `@` and `www` A records, whether a hosting or forwarding product is still attached, and the certificate binding. Also sweep for other places that still point at the name. **PARKED BY OWNER RULING 2026-09-16: *"I am Derald of derald.com, let this site park for a while."* NEXT LOOK moved 2026-09-15 -> 2026-12-15. The RAISED date is unchanged, so the snooze is visible. This is a decision to leave it, not a failure to look. **MEASURED THE SAME DAY, so the next look starts from facts rather than the 2026-08-17 assertion.** `derald.com` and `www.derald.com` BOTH resolve to **107.180.57.101**, whose PTR is `101.57.180.107.host.secureserver.net` -- GoDaddy shared hosting, consistent with the registry reading above (GoDaddy, `NS23/NS24.DOMAINCONTROL.COM`). **That answers one of the three questions this row asks: a hosting or forwarding product IS still attached**, and an A record pointing at a shared-hosting vhost with no SNI provisioned for this name is exactly the shape that serves a stranger's site under a certificate that does not match. **THE CERTIFICATE ITSELF IS UNMEASURED and is recorded as unmeasured rather than inferred**: egress to that host is refused on both network paths available to the steward, so the binding was never read. Do not let the DNS reading above be mistaken for a certificate reading. **WHERE IT GETS DONE IS UNCHANGED AND IS WHY THIS CAN NEVER CLOSE BY A COMMIT:** the fix is detaching or repointing the hosting product in the GoDaddy panel. AT NEXT LOOK: re-read the A record, confirm from a browser what the certificate says, then decide park / repoint / retire. |
| OI-002 | 2026-08-17 | 2026-10-01 | repo | `src/bindings/` is dead code. Nothing `add_subdirectory`s it, and `src/CMakeLists.txt` excludes it twice with "hard safety: never compile src/bindings into dottalkpp". Its comment reads `# After add_library(pydottalk MODULE ...)` -- describing a target it does not create. Pre-dates the move to `bindings/pydottalk/`; last touched 2026-04-28. It is a SECOND `pydottalk` definition and it briefly misled this session into thinking it was the live one. Delete it, or leave a one-line note saying what it was. |
| OI-007 | 2026-08-17 | 2026-11-03 | repo | **Re-measure `DOTTALK_INDEX_MODE` once pydottalk exposes an index API.** Today the three modes ship a byte-identical module (665336 B, sha256 `54cb15eb...`, all three) because `module.cpp` references the index zero times, so the static `libxindex.a` resolves nothing and the linker discards it, `lmdb_backend.o` included. Confirmed against the AI portal on 2026-08-17: indexing has NEVER been tested with pydottalk -- the portal names it once, as a closeout filename; the four `launcher_pydottalk` proof runs are 356-389 B with zero index hits; the only registered pydottalk proof is APPEND BLANK, which is CRUD. The binding is CRUD-only by construction. THE TRIGGER for this row is the first `setOrder`/`seek`/tag binding added to `module.cpp`: at that moment `lean-none` should start FAILING to link, the modes stop being interchangeable, and on Windows the `.pyd` may acquire a runtime dependency on `lmdb.dll` (vcpkg builds lmdb as a DLL on x64-windows) that must then ship beside it. Settle the DLL half any time with `dumpbin /dependents pydottalk.cp312-win_amd64.pyd`. Evidence: `proof.build.index_mode_changes_nothing_shipped`. |
| OI-010 | 2026-08-17 | 2026-09-29 | decision | **`main` carries TWO build documents and `development` carries one; decide which is canonical before a third diverges.** Measured 2026-08-17: `origin/main` has both `BUILDING.md` and `BUILDING.md` under `docs/getting-started/`; `development` has only the root one, and the public README links the `docs/getting-started/` copy. This is the shape `CLAUDE.md` names citing AIF-082 6.8 -- "two shims that restate will diverge, and have" -- and they already have: the root copy asserted that `DOTTALK_PRODUCT` and `windows-lean-*` presets were "not on the public repository yet" while main's own `CMakePresets.json` carries 28 occurrences of those names. That claim is corrected in `development` as of this row, but the `docs/getting-started/` copy on main was NOT inspected and may repeat it. Decide: one canonical page with the other reduced to a pointer, or a deliberate split (quickstart vs reference) with a stated boundary. Note the reference tier now exists as `docs/manuals/developer/dev/dev-21-build-system.md` (DEV-21, written as DEV-19 here until 2026-08-21: the chapter was renumbered on the day it was authored, for the reason OI-011 records, and this citation kept the pre-rename number. The cited-paths gate reported it as MISSING at every commit that touched this file), so a three-way split is the live risk. Cannot be closed in `development` alone; the second file exists only on `main`, which makes this a promotion-scope call. **ITEM 1 IS DONE, MEASURED 2026-08-25 -- and a stale git ref hid it for nine days.** `origin/main:BUILDING.md` is BYTE-IDENTICAL to `HEAD:BUILDING.md` (sha f1ba8ade207b1a4f, 138 lines, carrying "Corrected 2026-08-17"): the 2026-08-21 promotion `9470a50d9` already took it, so the corrected root page has been public for four days. **ITEM 2 REMAINS** -- `BUILDING.md` under `docs/getting-started/` is still on `main` and needs removal in a rebuild-review-commit window, NOT a promotion run. THE THREE PREMISES THIS ROW CARRIED WERE ALSO CHECKED AND ARE ALL FALSE: the getting-started copy does NOT repeat the presets claim (47 lines, zero mentions of DOTTALK_PRODUCT or windows-lean); the public README does NOT link it (it links the SITE and the SIBLING `POSIX_WSL_QUICKSTART.md`, and `git grep` across main finds no reference at all); and it is NOT a quickstart tier -- `docs/getting-started/` is 4 files on main and THE SAME THREE on development, so it is one orphan, which is `PROMOTION_PROCESS.md` section 6's classification verbatim. **THE METHOD FAILURE IS THE DURABLE PART:** the first pass read `origin/main` without checking that the ref was last fetched 2026-08-16. The three premises survived re-measurement by LUCK (that promotion did not touch them); what the stale ref hid was item 1, in the document recommending it as work. A remote ref is a CACHED ANSWER and has a freshness stamp -- `git log -1 --format=%ad origin/main`. Record: `docs/maintenance/OI010_EXAMINED_V1.md` sections 11-14. **CITATION RESPELLED 2026-08-30, NOT SUPPRESSED.** `cited-paths` reported this row's `docs/getting-started/` copy as MISSING -- correctly by its own rule, and misleadingly as to meaning: **the file's absence on `development` IS THIS ROW'S SUBJECT.** It exists on `main`. The line-suppression marker was the wrong instrument because it suppresses a LINE and this line also cites `docs/maintenance/OI010_EXAMINED_V1.md` and `docs/manuals/developer/dev/dev-21-build-system.md`, both tracked and both of which must keep being audited -- the same trap the AIF-135 register row hit the same morning, where the fix was spelling rather than suppression. So the path is written as a filename and a directory rather than one token: neither half matches the path scanner, both halves still read, and the branch is now named, which the original citation never did. **AND THE MARKER IS DESCRIBED RATHER THAN NAMED IN THIS ROW, WHICH IS NOT FUSSINESS.** The first draft of this note wrote the marker literally while explaining it, and that DISARMED THIS ROW -- both tracked paths above silently stopped being audited, measured immediately after. It is the exact hazard `check_cited_paths.suppressed_lines()` documents in its own docstring: *the marker suppresses any line containing it, INCLUDING A LINE THAT ONLY NAMES IT*, with the R126 ruling's self-disarming sentence as the recorded precedent. Reproduced here within a minute of reading that docstring. **The gate was right and a reader would have been wrong**, which is the third instance today of a measured absence being mistaken for a defect -- and the disarming is a fourth, where a measured presence silenced a check nobody would have noticed going quiet. |
| OI-011 | 2026-08-17 | 2026-10-09 | repo | **THE DEVELOPER MANUAL IS ALMOST ENTIRELY UNTRACKED: 21 chapters on disk, ONE in git.** Measured 2026-08-17 with `git ls-files docs/manuals/developer/dev/`: the only tracked chapter is `dev-20-semantic-field-hooks-and-pronouns.md`. Every other chapter -- `dev-00-evidence-rules` through `dev-19-help-meta-crosswalk-and-manual-generation`, twenty files including `dev-02-build-and-runtime-layout` -- exists on disk only. They are not ignored (they show under `-uall`); they were simply never added, and `status.showUntrackedFiles=no` (OI-008) meant no `git status` ever mentioned them. **This is authored prose, not generated output**: `dev-00` sets evidence rules and `dev-17` sets contributor rules, which is not what a generator emits. Consequences: a fresh clone gets one chapter of the developer manual; nothing in CI or the gates can validate them; and the work is one `git clean -fd` from gone with no history to recover from. NEAR DATE ON PURPOSE -- this is the largest measured cost of OI-008 and the cheapest to fix if it is simply an oversight. Decide first whether the twenty are (a) intended to be tracked and never were, (b) drafts deliberately held back, or (c) superseded by `manualgen` output; only (a) is a simple `git add`. Found while adding a build chapter, which had to be renumbered from `dev-19` to `dev-21` because an untracked `dev-19-help-meta-crosswalk-and-manual-generation.md` already held that number and no tracked listing could show it. **WIDENED 2026-08-17, same day: the chapter count understated this badly. The WHOLE canonical manuals home is adrift -- `docs/manuals/` holds 9,641 files on disk and 14 in git.** Per subtree, on-disk / tracked: `anchors` 2/0, `assets` 21/8, `command_reference` 3/0, `developer` 9,598/6, `dist` 14/0, `student` 1/0, `user` 1/0. Inside `developer`: `manualgen` 9,536/3, `proofs` 32/0, `dev` 23/2, `generated` 3/0, `04_selfdoc` 2/0, plus loose `DEVELOPER_MANUAL_DRAFT_COMBINED.md` and `README.md`. **The decisive measurement is that NONE of it is gitignored** (`git check-ignore` returns nothing for `manualgen`, `dist` or `user`), so this is untracked by OMISSION, not by policy -- nobody decided to exclude it; the exclusion happened. Split the ruling in two, because the two halves are different questions: (1) the AUTHORED tiers are small and nearly all absent -- `command_reference`, `dist`, `student`, `user`, `anchors`, `proofs` and the `dev` chapters total under 100 files and are the real loss risk; (2) `manualgen` is ~9.5k MDO-* status artifacts that are plausibly REGENERABLE output, in which case they want a `.gitignore` entry rather than a `git add`, and saying so turns an accident into a decision. Do (1) first; it is the cheap half and the one where a `git clean` costs work that cannot be rebuilt. **PROGRESS 2026-08-18 on half (2), and one claim above is CORRECTED.** The `manualgen` bulk was measured: 11 GB, 96% in two directories (`backups` 5.5 G, `accepted_catalogs` 5.1 G), both dominated by five 1 GiB SPARSE LMDB preallocations -- `data.mdb` files of exactly 1,073,741,824 bytes holding a few KB, and git does not preserve sparseness. **No `.gitignore` change is needed and the earlier "NONE of it is gitignored" is wrong at the level that matters**: `**/*.mdb` (:224), `**/*.cdx.d/` (:218), `docs/**/backups/` (:323) and `manualgen/**/*.csv` (:341) already cover it, leaving residual exposure of 38 files / 0.07 MB in `accepted_catalogs`. `check-ignore` returning nothing for the `manualgen` DIRECTORY was read as covering its contents; it does not. That misreading is not cosmetic -- `docs/**/backups/` is exactly why twelve governance attestations (gate acceptance, canonical-apply authorization, human review decisions) sat unreachable: no `git add`, however broad, could have taken them, so the tree could not report its own gap. They are rescued to `docs/maintenance/manualgen_acceptance_evidence/`, flattened per maintainer instruction with run ids in the filenames and full original paths in a MANIFEST; verified 0 of 12 still ignored. Nine were expected and twelve landed -- two basenames had distinct siblings, including four 786-byte overlay plans in THREE different contents. Sources are NOT deleted; removing the 11 GB stays a separate maintainer step so the rescue can be reverted alone. Half (1), the ~72 authored files, is still open. **HALF (1) LARGELY CLOSED 2026-08-24, and this row's own headline was STALE.** It reads '21 chapters on disk, ONE in git'. Measured today: `docs/manuals/developer/dev/` holds 25 files with FOUR tracked -- `dev-19-external-references`, `dev-20`, `dev-21` and `dev-22` landed after this row was written. 20 numbered chapters plus one stray `.txt` were untracked, 53 KB in total. **The three-way question this row poses is answered, and the answer is (a) intended to be tracked and never were.** The decisive measurement is not the file count: `docs/manuals/developer/README.md` is TRACKED and links four chapters -- `dev-04`, `dev-05`, `dev-09`, `dev-13` -- and ALL FOUR were untracked, so a fresh clone got a manual index whose every chapter link was broken. Nobody holds back a draft while linking it from the committed index. Corroborating: `dev-00` opens with hand-written YAML front matter (`page_id`, `status: DRAFT_PATCHED`, `last_verified`, `evidence_classes`), which is editorial metadata and not what a generator emits, so (c) superseded-by-manualgen is out too. The 20 numbered chapters are now tracked. **WHAT IS DELIBERATELY NOT IN THAT COMMIT, so it is a decision and not an oversight:** (i) `DotTalk++x64base Field Types & Validation System.txt` -- 7,258 bytes, 454 lines carrying non-ASCII, a stray `.txt` rather than a numbered chapter, and a filename containing spaces and an ampersand. It would fail the house-style gate on sight and it may be imported third-party material; it wants its own look. (ii) THE `dev-19` COLLISION IS STILL LIVE and is now collision IN GIT rather than on disk alone: `dev-19-external-references.md` and `dev-19-help-meta-crosswalk-and-manual-generation.md` are both tracked and both claim chapter 19. That duplicate number is exactly what forced the `dev-21` renumber this row describes, so it is the next thing to rule on -- renumbering is a rename plus every citation, which is a decision, not housekeeping. **CLEARED 2026-08-24 on the steward's instruction.** SENIORITY DECIDED IT: `dev-19-help-meta-crosswalk-and-manual-generation.md` dates from 2026-07-08 and is the file that forced the `dev-21` renumber; `dev-19-external-references.md` dates from 2026-08-20 and took a number already in use -- the same mistake, in the other direction. So the NEWCOMER moved, to **DEV-23**, which is max+1 over a fully occupied 00-22 and not a gap (gaps are not free real estate; the same rule `next_aif.py` states for AIF numbers). THE COLLISION WAS IN THE IDENTITY METADATA TOO, not just the filename: BOTH files declared `page_id: DEV-19`, so renaming the file alone would have left the identical defect one layer down, which is the shape this house keeps finding. Heading and `page_id` moved with the file. Citations: exactly one outside this register -- `labtalk/ai_portal/AIF120_APPLICATION_UI_DSL_SEED_V1.md:30` -- updated. `docs/maintenance/REPO_HYGIENE_TRIAGE_CHARTER_2026-08-04.md:156` cites the INCUMBENT and is unaffected. `docs/manuals/developer/README.md` links neither. (iii) The other authored tiers named above -- `command_reference` 3/0, `dist` 14/0, `student` 1/0, `user` 1/0, `anchors` 2/0, `proofs` 32/0 -- are untouched. ONE CONTENT EDIT WAS MADE AND IS NAMED HERE RATHER THAN LEFT TO BE DISCOVERED: `dev-15-selfdoc-pipeline.md` line 148 carried a single U+2019 in "project's", which the house-style gate blocks; it was normalized to an ASCII apostrophe. One character, no meaning changed. NEXT LOOK moved to 2026-09-07 for the remainder (i)-(iii) rather than left stale. **HALF (2) RESIDUAL RE-MEASURED 2026-08-25 and it is now SMALLER and NAMED.** The 2026-08-18 figure of "38 files / 0.07 MB in `accepted_catalogs`" has moved: of 39 files under `docs/manuals/developer/manualgen/accepted_catalogs/`, `manualgen/**/*.csv` now takes 15, leaving **24 files / 58,873 bytes untracked and NOT ignored** -- 13 x64 DBFs across TWO accepted catalogs (`man_catalog_v1` 8 tables, `manstar_native_reference_v1` 5 tables, the second one absent from every prior inventory here), 5 CDX indexes, 4 MDO acceptance summaries, `man_catalog_v1_manifest.json`, and the tree `README.md`. Surfaced by a WIDOW advisory naming MANHASH.dbf alone, which understates it: MANHASH is not special, NONE of the tree is tracked, and only MANHASH raised an advisory because only MANHASH got cited by a document. **The case for (a) track-them is now evidenced rather than assumed:** a Phase 6 read of the catalog on 2026-08-25 found the published developer manual DRIFTED from its accepted hash 4h 26m after promotion and unnoticed for 90 days -- the accepted catalog is the only thing in the tree that can detect that, and a `git clean -fd` currently costs all of it with no history. 58 KB, explicit paths, gated and manifest-carrying. See `docs/maintenance/lanes/full_stack_documentation/runs/DOCFLUSH-20260812-001/manualgen_phase/PHASE6_CATALOG_VERIFICATION_V1.md`. **RE-MEASURED 2026-09-09 AND THE HEADLINE IS CLOSED.** `docs/manuals/developer/dev/` is 25 on disk and 25 TRACKED -- dev-00 through dev-23, every chapter this row said was missing. The 21/1 state was fixed and this row went on nagging every commit for 22 days about a solved problem, which is the failure the gate's own advisory warns about. THE WIDENED HALF HAS ALSO LARGELY CLOSED, per-subtree disk/tracked: anchors 2/2, assets 21/21, command_reference 3/3, student 1/1, user 3/3, 04_selfdoc 2/2 -- all were 0-tracked when this row was written. STILL OPEN: dist 14/0. AND `NONE OF IT IS GITIGNORED` IS NOW FALSE -- proofs/ is ignored at .gitignore:101, so a decision was taken there and this row never recorded it. MANUALGEN: 11,934 on disk, 213 tracked, and the 213 are exactly the ACCEPTED OUTPUT -- published/ 169, accepted_catalogs/ 24, accepted_manifests/ 15, accepted_artifacts/ 3, reports/ 2. That is what the (a) track-them argument above asked for, and it was done. WHAT WAS EXAMINED ON 2026-09-09 AND DELIBERATELY NOT TRACKED: the 396 loose *_STATUS.md at the manualgen top level (367 MDO plus 29 PIP, 503 KB, zero tracked). Classified by content rather than by name: 306 MACHINE RUN RECEIPTS (Runner: Python, boolean flags, SHA-256 hashes), 53 transient progress notes (Created: / Not yet done:), 26 decision-plus-counters, 11 human decision records. THE FIRST READING OF THIS SET WAS WRONG AND IS RECORDED AS SUCH: on the strength of ONE file (MDO-045, which is hand-written) they were called `authored prose, not generated output` -- this row's own test for dev/, where it was correct, and it does not transfer. 77 percent are telemetry. THE SECOND READING WAS ALSO WRONG. Eight MDO files named HUMAN_REVIEW_DECISION_RECORD carry an EMPTY `Decision:` field and read as reviews opened and never answered. THEY ARE NOT OPEN LOOPS: every one is dated 2026-05-25/26, the nine PIP-005 records that DID decide (Decision: ACCEPT_FOR_PROMOTION) are dated ONE DAY LATER, and the lane then ran two more months to MDO-350E on 2026-07-28. They are early scaffolding superseded within a day. A blank field was read as a question rather than as an abandoned shape, and only checking the dates around it settled that. RULING: process record of a COMPLETED lane whose accepted output is already tracked. Not tracked, and the 15.7 KB staging that had been prepared was withdrawn rather than committed. THE TRACKING POLICY FOR THE REMAINING ~11,700 IS NOT THIS ROW'S TO SET: manualgen is ACTIVE territory for a concurrent session as of 2026-09-08/09 (modified reports/*.csv, untracked TODO.md and accepted_docs/). A gitignore ruling over a tree another author is working belongs to whoever runs that lane. HANDED TO THEM RATHER THAN CHASED: MDO-350E (2026-07-28) reports GREEN_PUBLICATION_REPLACED while recording `Active hash matches candidate: 0` -- the replacement did not produce the hash it expected. That is the same drift class as the Phase 6 finding above, where the published manual drifted from its accepted hash 4h 26m after promotion and went unnoticed for 90 days. NEXT LOOK 2026-10-09 for the two remainders only: dist 14/0, and the manualgen tracking policy. |
| OI-003 | 2026-08-17 | 2026-10-01 | repo | `build.ps1` has no way to build the module alone: line 164/170 hardcode `--target dottalkpp pydottalk`, so `-WithPyDotTalk` always builds the CLI too. The lean route now exists (`build_pydottalk.ps1` -> `bindings/pydottalk` standalone), so this is no longer blocking -- but anyone reaching for `build.ps1 -WithPyDotTalk` still gets the heavy build with no switch to say otherwise. Either add `-PyOnly`, or make the wrapper the documented entry point and say so in `build.ps1`. |
| OI-015 | 2026-08-21 | 2026-09-29 | repo | **The staging build cannot cover the GUI lane, so promotion step 3 structurally misses it.** `C:\x64base\build\CMakeCache.txt` carries `DOTTALK_WITH_WX:BOOL=OFF`, so the 2026-08-21 staging build compiled the engine and all ten test executables and produced no `dottalk_wb.exe` at all. That build is still worth running -- it proves `main`'s engine source compiles from a clean tree -- but `PROMOTION_PROCESS.md` step 3 reads as though building staging validates what was promoted, and for anything behind `DOTTALK_WITH_WX` it cannot. The `wb` rename was proven on `D:` instead, which is where the two real defects were caught. Either enable the switch for promotion builds, or say plainly in step 3 which lanes staging does not cover. **WIDENED 2026-08-24: THE DEVELOPMENT SIDE HAD THE SAME HOLE AND IT WAS NOT WRITTEN DOWN ANYWHERE.** This row records that STAGING cannot cover the GUI lane. It could not cover it on `D:` either: `src/CMakeLists.txt:476` gates `gui/core` behind `DOTTALK_WITH_GUI`, which defaulted OFF, so a plain `build.ps1` did not COMPILE `src/gui/core` at all. MEASURED on `508325324`: a build reported "Built OK" while `session.obj` was a full DAY older than `dottalkpp.exe`, immediately after a commit that rewrote 132 call sites in `session.cpp`. **THE GATE IS AT CONFIGURE TIME**, which is why the obvious workaround does not work either -- `cmake --build build --config Release` over the whole tree built 25 targets and walked straight past the one that had changed, because the previous configure had excluded it from the solution. THE COST WAS SILENCE, NOT SLOWNESS: ctest went **18 -> 20** the moment the GUI was switched on, and the two that appeared -- `dottalk_gui_core_async_smoke` and `dottalkpp_gui_match_count_test` -- had not been failing, they had not been RUNNING, and nothing in a green summary said so. Same shape as `REGRESSION ALL` passing ten specs over a commit that rewrote the free-slot allocator without once exercising it. **FIXED 2026-08-24 on the steward's ruling ("-WithGui should be the default"):** `build.ps1` now follows its own idiom for a feature that is ON -- an opt-OUT `-NoGui`, like `-NoIndex` and `-NoTV` -- with one named `$GuiEnabled` that both the configure line and the target list read. `-WithGui` is still accepted and still forces it on, redundant rather than wrong, because scripts and habits spell it. `-NoWx` was deliberately NOT added: wxWidgets is a heavier dependency and `DOTTALK_WITH_WX` stays opt-in, which means **THE HALF THIS ROW ORIGINALLY RECORDS IS STILL OPEN** -- `dottalk_wb` is still behind an opt-in switch at both ends, and promotion step 3 still cannot validate it. |
| OI-017 | 2026-08-21 | 2026-10-06 | repo | **71 machine-absolute paths across 14 files are already published in `main`.** Measured by the overlay guard on 2026-08-21. NOT introduced by that promotion -- every one of the 14 was already in `main` and the overlay re-copied identical content; zero of them appear in that run's 30-file change set. Worst offenders are functional rather than prose: `cdxdemo.dts` under `dottalkpp/data/`, `dottalkpp/data/workspaces/manual.dtschema`, `dottalkpp/data/schemas/mcc_x64_output.txt`, `dottalkpp/help/find_repo_string.ps1` and `restore_dd058_backup.ps1` all carry `D:\code\ccode` and will not work for anyone who clones. `docs/gui/GUI_BUILD_AND_RUN_V1.md` was the fifteenth and was cleaned in `cac02a8b5`, which is the worked example. The guard already names them on every overlay, so the list is free; the work is sterilising them in `development` so the next promotion carries the fix. **STILL OPEN FOR THE THREE WORST FILES, re-measured 2026-08-25 against CURRENT main.** `9470a50d9`'s own message says "the data scripts carry relative paths instead of `D:\code\ccode`", and it did sterilise some -- but not these: `cdxdemo.dts` under `dottalkpp/data/` main 4 / dev 4, `dottalkpp/data/schemas/mcc_x64_output.txt` main 4 / dev 4, `dottalkpp/help/find_repo_string.ps1` main 1 / dev 1. **WHAT CHANGED IS THAT THEY ARE NOW FIXABLE:** all three were tracked in development by `f6a8d71a6` (cited-path widow triage, 2026-08-25). You cannot sterilise in `development` what `development` does not track, which is why this row and OI-018 were stuck together. **PARTIALLY RESOLVED 2026-08-25, and TWO OF THE THREE ARE NOT DEFECTS.** Characterised before editing, because a blanket replace would have been wrong in both directions. (1) `dottalkpp/help/find_repo_string.ps1` -- FIXED. The one occurrence was a `.EXAMPLE` in comment-based help; it now reads `-Root .`, which is runnable and matches the second example that already did. Zero machine paths remain in the file. (2) `cdxdemo.dts` under `dottalkpp/data/` -- NOT A DEFECT AND MUST NOT BE STERILISED. It is a DEFECT DEMONSTRATION: line 3 says `WILL FAIL BECAUSE IT WILL CONCATENATE THE ABSOLUTE PATH TO THE DEFAULT PATH DATA`, lines 5-7 record the resulting `warning: path does not exist` in comments, and line 9 then uses the direct `WORKSPACE OPEN <abs>` form that works. The absolute path is the SUBJECT MATTER; removing it destroys what the script demonstrates. (3) `dottalkpp/data/schemas/mcc_x64_output.txt` -- NOT A SCRIPT. It is a CAPTURED CLI TRANSCRIPT: `. SCHEMAS LOAD WORKSPACES\MCC_x64` is the prompt echo and the four paths are what the tool PRINTED. Editing it would falsify a record of what happened. **SO THIS ROW'S FRAMING IS WRONG FOR TWO OF ITS OWN HEADLINE FILES** -- they are described as "functional rather than prose" that "will not work for anyone who clones", but one is a demo whose point is the failure and the other does not run at all. **THE REAL QUESTION FOR BOTH IS PUBLICATION, NOT STERILISATION:** both match `PROMOTE.manifest` and therefore publish to the public repository. A scratch defect-demo and a captured error transcript on the public repo is a manifest-scope decision, not a path-hygiene one. The remaining 11 files of the original 14 are unexamined and may split the same way. **PUBLICATION HALF ANSWERED 2026-09-12 by `651ce911a`.** The overlay now publishes only tracked files, so `restore_dd058_backup.ps1` -- untracked, and named above as a worst offender -- no longer reaches `main` at all. `cdxdemo.dts` and `mcc_x64_output.txt` ARE tracked and still publish, correctly: for those two the question is now a reviewable manifest-scope decision rather than an accident of the filesystem, which is what this row said it wanted. **CORRECTION 2026-09-19 -- THE TRACKING CLAIM ABOVE WAS TRUE AND IS NOT, AND THE CITATION IS RESPELLED WITH IT.** This row says all three files "were tracked in development by `f6a8d71a6`". `b570837aa` ("data: stop publishing a bug repro, correct a claim I did not measure") REMOVED `cdxdemo.dts` from tracking afterwards, and `.gitignore:408` now pins it with an explicit single-file rule, so `git add` on that path is a permanent silent no-op -- R42.1, and the gate reported exactly that on 2026-09-19. The row's own later note says the file MUST NOT BE STERILISED because it is a defect demonstration, so untracking it is the CONSISTENT outcome, not a regression: what was stale was the sentence, not the decision. Two further facts a reader needs. The path is no longer on disk at all; an untracked copy with no history sits under `dottalkpp/data/scripts/`, so the two are not the same file and neither is in git. And the remaining sterilisation work named here covers the OTHER two files only. HOW IT SURFACED is OI-037's lesson arriving for the second time: this row was never in a change set, so no gate had read it since 2026-08-25, and an unrelated edit to this file on 2026-09-19 is what put it in scope. A register nobody stages is a register no gate audits. |
| OI-021 | 2026-08-30 | 2026-09-29 | lane | **AIF-108, the Memo Object Challenge, is ASLEEP FOR 30 DAYS by owner ruling** (*"we can reset the memo challenge for later, make it sleep for 30 days"*). It is a test-design lane -- *chartered: NO engine change proposed; this lane produces tests and a protocol for receiving tests* -- holding 100 test ideas opened as a governed challenge to external AI agents through the house BBS. **WHY IT IS BEING SLEPT RATHER THAN CLOSED:** ten of its recursion ideas were blocked on memo-sidecar carriage, that carriage landed 2026-08-12, and the row still read BLOCKED eighteen days later -- so the lane has been accruing an obligation nobody was told about. Sleeping sets a date; closing would lose the ten ideas. **WHAT IS NOT ASLEEP:** the engine defects found while answering the owner's two-level memo question are GRANDFATHERED INTO AIF-120 (R133) and stay live there. The two files that cite AIF-108 for engine behaviour -- `include/dottalk/minidb_hydrate.hpp` and `src/cli/cmd_workspace.cpp` -- are re-pointed at AIF-120 in the same commit, because a sleeping test lane must not be the cited authority for shipped code. **ON WAKING:** re-read AIF-120's amendment first; several of the ten ideas may already be answered by the probe. **OI-020 IS DELIBERATELY SKIPPED** -- it is drafted in `claude/OI020_DRAFT_BUILD_WINDOWS_UNTRACKED_AND_TESTS_CANNOT_FAIL.md` and not yet rowed. Writing over a number someone has already drafted against is how AIF-146 was burned. |
| OI-023 | 2026-08-30 | 2026-10-20 | repo | **MOVING A TRACKED FILE BREAKS ITS CITATIONS SILENTLY, AND ONE OF THEM WAS A CURATED LIST THAT CAN LOSE AN ENTRY WITHOUT SAYING SO.** Moving `fields_mgr.cpp` out of `src/core/` and into `src/xbase/` on 2026-08-30 left EIGHT files naming the old location. **AND THIS ROW'S FIRST DRAFT WAS THE NINTH** -- it wrote that old location as one path token while explaining the hazard, and `cited-paths` reported it MISSING immediately. Respelled here for the same reason OI-010 was: a historical reference to a file that is correctly gone must not parse as a live pointer. Third instance in one day. `cited-paths` audits only documents in the CHANGE SET, so it saw none of them; the breakage surfaced two commits later when an unrelated edit happened to touch the intake register. Six live pointers were corrected in that commit. **THE ONE THAT IS NOT PROSE:** `tools/datadict/extractors/physical_dictionary_extractor.py` carries `KEY_SOURCE_ANCHORS`, a curated list of the source files that matter most, and `find_anchor_runtime_proof()` walks it as `if p.exists(): proofs.append({... "status": "present" ...})` -- **NO `else`, and `status` can only ever hold one value.** An anchor that moves drops out and the proof list goes from 26 entries to 25 with nothing anywhere recording the loss. That is the AIF-118 shape inside a PROOF GENERATOR: the artefact whose job is to attest presence cannot express absence. `DD-005`'s source map mirrors the same list and would drift the same way. **NOT FIXED -- another lane's tool.** The repair is small and obvious (emit a `status: missing` row, or fail) but the datadict lane owns what its manifest is allowed to say. **THE WIDER GAP, and it is the reason this is an item rather than a note:** nothing in this tree checks that a moved file's citations followed it. The gates are change-set scoped by deliberate design -- `cited-paths` says so in its own header, because a gate reporting every pre-existing widow stops being read -- so the very thing that makes them usable is what makes a rename invisible to them. A `git mv` needs a companion sweep and there isn't one. **AND THE SWEEP FOUND A SECOND, OLDER DRIFT IN THE SAME SPEC:** DD-005 line 66 also names a `cmd_vuse.cpp` under `src/cli/` and there is no `vuse` file anywhere in the tree -- not renamed, not moved, absent. **THE PATH IS SPELLED IN TWO PIECES HERE ON PURPOSE, AND THE FIRST DRAFT DID NOT DO THAT.** Written as one token it parsed as a live pointer, and the `cited-paths` gate reported it MISSING on the very next commit that touched this file -- correctly by its own rule, and misleadingly as to meaning, because the file's ABSENCE IS THIS SENTENCE'S SUBJECT. That made this row the SOLE source of a permanent advisory on a file the tree touches constantly, which is the failure `check_cited_paths.suppressed_lines()` names in its own docstring: a standing advisory trains people to skip the whole check. Respelled rather than suppressed, per OI-010, because this line must keep auditing the other citations around it. Fourth instance of this shape, third in two days. **AND THE QUESTION THIS PARAGRAPH LEFT OPEN IS NOW ANSWERED BY THE STEWARD, 2026-08-31:** *"vuse is an old command I deleted, it was a simple clone of cmd_use.cpp"*. So it became nothing -- it was a duplicate, and it was removed on purpose. Corroborated: the path enters history at `7c56022a1` (2026-05-03, the initial recovered baseline), is still PRESENT at `4c404bed5` (2026-06-22), and is ABSENT at `c86c18783` (2026-07-13, "Merge main and reconcile publication source") and in every ref checked -- HEAD, `origin/development`, `origin/main` and `main`. **WHICH MAKES THE DD-005 ENTRY CHEAPER TO FIX THAN THIS ROW ASSUMED AND STILL NOT OURS TO FIX.** Line 66 sits inside a fenced primary-sources block DIRECTLY BELOW `src/cli/cmd_use.cpp`, so the map listed a clone as a co-equal primary source for the same facts: deleting the line loses nothing, because the original is the line above it. Recorded for the datadict lane, NOT edited here -- that lane owns what its manifest may say, and the reason for holding off has changed from ignorance to ownership. **A METHOD NOTE WORTH MORE THAN THE FIX:** the removing commit could not be found by `git log --diff-filter=D`, on any ref, even with `--full-history` -- because the file went away in a MERGE and git does not diff merges by default. An empty deletion log is NOT evidence a file was never deleted; bracketing it between a commit that has it and one that does not is what actually answered it. Recorded beside OI-010's stale-ref lesson and OI-013's argument-list lesson, and one more was reproduced getting here: a recursive grep over the device mount returned EMPTY rather than erroring, which is exactly OI-013's warning, and it briefly read as "DD-005 does not cite it after all". It surfaced for the same reason as everything else here -- that document entered a change set for the first time in a while. **Which is the item in miniature: these citations do not rot when the code moves, they rot silently and are discovered by coincidence.** **MEASURED BEFORE BUILDING, 2026-09-20, AND THE MEASUREMENT RESIZED THE ITEM.** This row assumed rename breakage was the widow population. It is 3.6% of it. Census over the whole document surface: **1,820 tracked `.md` documents cite 2,222 distinct repo paths, 1,888 tracked and 334 not.** In this check's own vocabulary the 334 are **75 IGNORED, 158 WIDOW (on disk, untracked), 97 MISSING (not on disk), 4 MALFORMED**. Of the 97 MISSING, **TWELVE ever existed in git history and 85 never did** -- history is complete back to 2025-08-14, so the 85 were wrong the day they were written: planned files, generated outputs nobody tracks, typos. **NO SWEEP OVER RENAMES WILL EVER FIND THEM.** Of the twelve, only FOUR actually moved and have a same-named successor. **THE DEAD SIDE OF EACH PAIR IS SPELLED IN TWO PIECES HERE ON PURPOSE** -- written as one token it parses as a live pointer, and this row would become the FIFTH instance of the very shape it is about, which its own first draft already managed once. `dbf_create.cpp` left `src/core/` for `src/xbase/dbf_create.cpp`; `table_state.hpp` and `table_write.hpp` left `src/cli/` for `include/cli/table_state.hpp` and `include/cli/table_write.hpp`; `build_help.ps1` left `src/cli/` for the repository root. The other eight were DELETED, not moved. **AND MOST OF THE FIFTEEN CITING DOCUMENTS ARE HISTORICAL RECORDS**, which is the trap this row's own first draft fell into: a session closeout dated 2026-07-19 naming `table_state.hpp` at its old `src/cli/` home is not a broken pointer, it is an accurate statement about July, and repointing it would make it a lie. OWNER 2026-09-20: report only, fix none of them yet -- each of the fifteen needs reading to tell a live pointer from a record. **BUILT, INTO `tools/staging/check_cited_paths.py` RATHER THAN BESIDE IT.** A separate gate would have copied `ROOTS`, and AIF-120 R81.4 is the record of what a drifted copy of that tuple costs: 175 citations invisible, never verified, not once. Same reasoning that consolidated three hand-written lock guards on 2026-09-19. **THE RENAME SWEEP** inverts the scope rule instead of breaking it -- it fires only when the change set REMOVES a citable path, then searches the WHOLE tree, because a widow made by a rename is by definition not in the commit that made it. `--diff-filter=DR` takes deletes AND renames, because git pairs a rename only when the content survives the move and a file moved-and-heavily-edited arrives as a delete plus an add. **IT RUNS BEFORE THE `no documents in scope` EARLY RETURN, AND THAT IS THE WHOLE POINT:** the commit that moved `fields_mgr.cpp` touched no `.md` at all, so every other lane in that file returned early and saw nothing. A selftest case asserts exactly that shape -- `staged_docs` empty, `main()` exits 3. **`--audit`** drops the scoping entirely and always exits 0, so the backlog is countable without anyone being blocked by it; precedent for the mode and the never-judge rule is `check_house_style.py --audit`. Selftest **31 cases, 0 failures**, nine of them new. **ONE INCIDENTAL FINDING ABOUT THE INSTRUMENT:** `PATH_RE` swallows prose. `workbench_session.hpp` and its `.cpp` sibling under `gui/uidef/`, written as a SINGLE token, are two file names in one, and every audit that ever looked reported that token MISSING -- four such tokens exist. They are now a named MALFORMED class in `--audit` and **the matcher was deliberately NOT narrowed**, for the same R81.4 reason: a quiet change to what PATH_RE accepts changes what every other lane in the file sees. **STILL OPEN AND NOT OURS:** the datadict half -- `find_anchor_runtime_proof()` emitting only `status: present`, so an anchor that moves drops out of a 26-entry proof list with nothing recording the loss. The repair is small and obvious and the datadict lane owns what its manifest may say. **ALSO STILL OPEN AND NOW SIZED:** the 158 WIDOW and 75 IGNORED citations, which are a different problem entirely and want a decision rather than a gate. **THE BACKLOG WAS RULED ON IN THREE PARTS, 2026-09-20, BECAUSE IT WAS THREE QUESTIONS WEARING ONE NUMBER.** The 334 cited-but-untracked paths split 75 IGNORED / 158 WIDOW / 97 MISSING / 4 MALFORMED, and each wants a different answer. **(1) IGNORED -- TEACH THE GATE THE RULING.** 48 of the 75 are `docs/manuals` output already excluded by decisions taken under OI-011; grouped by rule the 75 collapse to EIGHT rules, every one deliberate. `check_cited_paths.py` no longer lets an ignored citation move the exit code and now prints the rule beside it, grouped by rule rather than by path. A citation to a deliberately ignored path is CORRECT, and re-reporting it as a problem is the failure that file's own SCOPE note names. **(2) WIDOW -- TRACK THEM, SUBTREE BY SUBTREE, MEASURING EACH FIRST.** Concentrated in `docs/maintenance` (55), `docs/cases` (21) and `docs/datadict` (16). `docs/cases` went first as the starkest and **THE DECISIVE TEST IS OI-011'S, PASSED HARDER THAN OI-011 ITSELF:** the TRACKED registry `docs/cases/REGISTRY_CASES_v0.csv` holds 16 rows and **14 OF THEM NAME AN UNTRACKED FILE.** Only 2 registered cases were in git. Nobody registers a case while deliberately holding the file back. **THE COUNTER-HYPOTHESIS WAS CHECKED AND IT FAILS ON ITS OWN DATA:** every row reads `runtime_visibility: hidden_until_reviewed`, including BOTH of the two that ARE tracked -- so that field cannot be what decides tracking. 17 files, 24,000 bytes, clearing 16 WIDOW entries. **HELD OUT AND NAMED RATHER THAN SWEPT IN:** `docs/cases/reports/` is GENERATED review output -- `review_summary_v1.json` carries counters and a gate verdict, `CASE_REVIEW_V1.md` says REPORT_ONLY / NO MUTATION, the inventory CSV is the registry plus derived columns -- and it is STALE, counting 15 case files against 17 on disk. Same class as the 396 `*_STATUS.md` files OI-011 examined and left untracked. **ONE CHARACTER WAS NORMALIZED AND IT IS NAMED HERE BECAUSE IT IS NOT PURE COSMETICS:** `CASE_HIST_040_XBASE_MAJOR_PLATFORM.md` line 14 carried U+2192 inside a QUOTED SOURCE FILENAME, which `check_house_style` blocks on an added line. It became `->`. Unlike OI-011's U+2019 in "project's", this one sits in the name of an external .docx, so the fidelity loss is real if small -- recorded rather than left to be discovered. **(3) MISSING -- READ THEM AND PRODUCE A LIST.** 20 of the 97 sit under `src/cli` and 5 under a `src/backends` that IS NOT A DIRECTORY IN THIS TREE AT ALL, which is a finding about the documents rather than about the engine. They split into live pointers that should follow a moved file, historical records that must be respelled rather than repointed, and paths that were never real; only reading separates them. NOT YET DONE. **STILL OPEN after all three:** `docs/maintenance` and `docs/datadict` and the smaller WIDOW subtrees, the 97 MISSING, and the datadict half that is another lane's. **A CORRECTION TO THIS ROW'S OWN DATES, AND IT IS RECORDED RATHER THAN QUIETLY EDITED.** The three rulings above were first written as 2026-09-21. THEY HAPPENED ON 2026-09-20. The agent advanced the date by a day with nothing to support it: the machine clock, `check_open_items` (*none due before 2026-09-20*) and both commits' own author dates all say the 20th. Corrected in the row and in the four comments in the gate; **the two commit messages still carry the wrong date and are NOT being rewritten**, because a message is immutable history and rewriting two commits to fix a date costs more than the error does. **THIS IS THE THIRD TIME THIS CORRECTION HAS BEEN NEEDED** -- the owner has given it twice before. The rule it breaks is simple and the fix is cheaper than the habit: a date that is not read off the machine is a guess, and a register whose rows are cited BY DATE cannot carry guesses. **THE 97 MISSING ARE READ AND SORTED, 2026-09-20 -- AND THE FIRST RESULT IS THAT YESTERDAY'S SPLIT WAS WRONG.** This row said 12 of the 97 ever existed and 85 never did. **THE TRUE SPLIT IS 25 AND 72.** The instrument was at fault, not the tree: the census asked `git log --diff-filter=D --name-only` over the whole tree, and **HISTORY SIMPLIFICATION PRUNES MERGE COMMITS**, so every file deleted AS PART OF A MERGE was invisible to it. Thirteen paths were misfiled that way, including four real commands -- `cmd_vuse.cpp`, `cmd_codaysl.cpp`, `cmd_cobol.cpp`, `cmd_smart_browser.cpp` -- which had been called typos or aspirations on the strength of that sweep. **THE RELIABLE QUESTION IS THE OPPOSITE ONE:** ask what was ever ADDED (`git log --all --diff-filter=A`), because an addition happens in an ordinary commit and a merge cannot hide it. 13,072 paths have been added in this repository's history. **HOW IT WAS CAUGHT, because the method transfers:** one path this very register already documents -- `cmd_vuse.cpp`, recorded under this row as added at 7c56022a1, modified at 4c404bed5 and absent by c86c18783, a MERGE -- was checked against the sweep's answer. The sweep disagreed with the register, and the register was right. **A NEGATIVE RESULT NEEDS A KNOWN POSITIVE TO TEST IT, and that is now four instrument failures in three days** -- the timestamp-preserving copy, the gates reading an empty index, `Write-Host` never entering the pipe, and this. The first three were harnesses; this one was analysis. **THE CORRECTED SORT, by what each wants doing:** (1) MOVED, successor exists -- 10 paths, 6 carrying a live-spec citation, including the `edu_*.cpp` family that left `src/cli/` for `src/edu/` and `fields_mgr.cpp`, THIS ROW'S OWN FOUNDING EXAMPLE, still stale in one document. (2) DELETED OUTRIGHT -- 15 paths, 9 with a live-spec citation; a SPEC citing a deleted file is a spec that is wrong about the tree, not a stale pointer. (3) NEVER AT THAT PATH BUT THE SAME NAME EXISTS ELSEWHERE -- 22 paths, 13 live-spec, which read as wrong-prefix citations with a definite right answer. (4) NEVER ANYWHERE -- 50 paths, and **35 OF THEM ARE CITED BY LIVE SPECS**. That last number is the real finding in this backlog: it is not stale pointers, it is specifications describing files that have never existed in this repository. **ONE TRAP IS NAMED SO NOBODY 'FIXES' IT:** the getting-started copy of the build page resolves to the root `BUILDING.md` by basename and MUST NOT be repointed -- OI-010 records that it exists only on `main` and that the two-piece spelling is deliberate. **THE CLEANUP IS FOUR REPOINTS, NOT TWENTY-TWO, AND READING IS WHAT MADE THE DIFFERENCE.** Every citing LINE behind the moved-and-misprefixed buckets was read before anything was edited. **FIXED, four documents, each a LIVE POINTER in a live document:** the manual blueprint's TABLE BUFFER row now names `include/cli/table_state.hpp`; the memo-WAL lane doc now cites `docs/maintenance/AI_RUN_TRACEABILITY_LANE_V1.md`; the MDO intake README now points at `docs/manuals/developer/DEVELOPER_MANUAL_DRAFT_COMBINED.md`; the workspace-manager design now names `src/gui/core/session.cpp`. **EVERYTHING ELSE WAS LEFT, WITH A REASON, AND THESE ARE THE REASONS WORTH KEEPING.** (a) GENERATED OUTPUT -- the four `edu_*.cpp` moves and two more appear ONLY in the two command-reference harvests, whose own headers say *Generated from `@dottalk.usage v1` blocks*. Editing generated text is undone by the next regeneration; the producer is the fix, which is OI-026's ruling. THE HARVEST IS STALE ABOUT WHERE THAT FAMILY LIVES and that is a finding for the manualgen lane, not a citation to patch. (b) A DOCUMENT THAT DOCUMENTS ITS OWN ABSENCE -- the build-stamp ruling cites a shim path in the words *for a different, nonexistent path*. Repointing it would delete the sentence's subject. (c) A CORRECT RELATIVE PATH -- the cascade ERP README cites `scripts/` and that directory sits beside the README, so it resolves. The gate reads every citation as repo-root-relative and cannot know where the citing document lives. (d) ELISIONS -- five cited 'paths' contain `...` because an author shortened a long path in prose, and PATH_RE matches them since `.` is in its character class. (e) HISTORICAL RECORDS -- the bulk. Session closeouts of 2026-07-19 and 2026-07-26 name the OLD paths while SAYING THEY WERE MOVED (*MOVED -> include/cli/table_state.hpp*); they are accurate and repointing them would make them lies. (f) THE `main`-ONLY BUILD PAGE, five citations, all of them documents ABOUT the duplication -- *Pick one location* -- which is OI-010's subject and must not be resolved by repointing. **THE INSTRUMENT COUNT, over the 243 non-ignored citations: 5 ELISION, 4 MALFORMED, 1 RELATIVE = TEN THAT ARE ARTEFACTS OF HOW PATH_RE READS TEXT rather than backlog.** A first cut of the relative-path measurement said TEN relative hits; it tested the path against the citing document's directory without excluding documents AT THE ROOT, where that test collapses into the plain root check. Ten became one. Same failure family as the merge-pruned delete sweep above -- a check that cannot tell its subject from its baseline -- and the second one caught in a single morning. **WHETHER PATH_RE SHOULD LEARN ABOUT ELISIONS AND RELATIVE PATHS IS OPEN AND IS A DECISION, NOT A CLEANUP:** AIF-120 R81.4 records what a quiet change to that matcher costs. **THE MATCHER NOW REFUSES TWO TOKEN SHAPES, AND THE CHANGE WAS COUNTED RATHER THAN TRUSTED, 2026-09-20.** Owner ruling: teach it elisions and two-names-in-one, leave relative paths alone. **NOT A NARROWER REGEX.** PATH_RE still matches exactly what it always matched; a predicate `not_a_path` sits in `cited()` and refuses a token containing `...` (an author shortening a long path in a sentence) or one whose non-final component carries a known extension (`shell_api.cpp/.hpp`, one file name written as two). **AND IT SAYS SO OUT LOUD.** `cited()` takes a `rejected` collector and both the gate and `--audit` print what was refused and why. That is the whole counter-measure to AIF-120 R81.4, where a wrong ROOTS tuple hid 175 citations and no line anywhere said it had: **a narrowing nobody can see is the failure; a narrowing that reports itself is a measurement.** **THE PROOF IS A BEFORE/AFTER CENSUS OF THE WHOLE CITED SET:** 2,219 distinct cited paths before, 2,210 after, **exactly 9 removed and ZERO added** -- five elisions and four two-in-one tokens, named individually. R81.4 had no such count, which is why it took a lane to find. **RELATIVE PATHS WERE DELIBERATELY NOT TAUGHT:** resolving a citation against its own document's folder makes one string mean different things in different files and would silently resolve citations nobody meant, for a benefit of exactly one path in this tree. Selftest 43 cases, 0 failures, nine new -- including one that asserts the rule's own premise by checking that NO TRACKED PATH HAS A `...` COMPONENT, because the elision rule rests on that and a tree that grew one would start dropping real citations in silence. **AND A SECOND INSTRUMENT IN THIS SAME FILE WAS CARRYING THE MERGE BUG:** `--audit` split its MISSING population with `git log --diff-filter=D`, the very sweep that misfiled thirteen paths this morning. It now asks what was ever ADDED, `--all --diff-filter=A`, and reports 25 existed / 64 never where it used to say 12 / 77. **THE TELL WORTH KEEPING:** the delete-sweep and the add-sweep both returned 985 on this tree, which reads like agreement and is not -- they are different sets of the same size, and comparing TOTALS instead of MEMBERS is how the first error survived a check. **`docs/datadict` IS MEASURED AND THE ANSWER IS DO NOT TRACK IT HERE, 2026-09-20 -- the subtree has already ruled on itself.** 16 widows across seven directories, every one of which has ZERO tracked files while the `specs/` that cite them are tracked -- the same registry-and-members shape as the cases tree. **BUT THE LANE HAS A MACHINE-READABLE DISPOSITION POLICY FOR EXACTLY THIS QUESTION.** DD-029 exists to classify *generated or temporary maintenance package artifacts*; its rule `datadict_docs` covers `docs/datadict/**` with disposition DATADICT_LANE_CHANGE and baseline_action **ELIGIBLE_AFTER_DATADICT_SELF_REVIEW**, and `savepoint_journal` covers the runlog as evidence. That is a gate the datadict lane passes, not this one, and OI-023's own note already says the datadict half is another lane's. **THE FINDING TO HAND THEM IS BETTER THAN THE TRACKING WOULD HAVE BEEN:** `tools/datadict/disposition/artifact_disposition.py` is TRACKED and the policy file DD-029 names as *the policy file* is NOT. Nothing is broken -- `load_policy(None)` falls back to a `DEFAULT_POLICY` built into the tool, and the two were compared as canonical JSON and are **IDENTICAL**, same policy_id, same nine rules. **THAT IS THE PROBLEM, NOT THE RELIEF.** It is two declarations of one policy, agreeing today, with the spec pointing at the copy that a fresh clone does not have -- the shape this tree consolidated three times in one week. A lane policy edited in the JSON would not change the tool, and a reader following DD-029 to its stated authority finds nothing on a clean checkout. `DD095_DATADICT_LAYOUT_POLICY.md`, a layout policy, is untracked on the same footing. **`docs/maintenance` IS SIZED BUT NOT RULED:** 55 widows, **600,215 bytes** -- twenty-five times the cases tree -- and 46 of the 55 sit under `lanes/` as run evidence, which is the population OI-011 examined and deliberately LEFT untracked as *process record of a completed lane*. Five are top-level SESSION_CLOSEOUT documents, which are authored prose and a different question. That subtree needs its own measurement pass rather than the cases-tree answer applied by analogy. **HOUSEKEEPING PASS, 2026-09-20, AND IT FOUND A GAP IN WORK THIS ROW ALREADY CALLED DONE.** The house rule is one sentence, recovered from history because the file itself was deleted and turned up in this row's own DELETED-OUTRIGHT bucket: *A task isn't done until the housekeeping is finished.* **THE CASES TRACKING MISSED A SUBDIRECTORY.** The measurement listed the top level only, so `runtime_proofs/` was read as tracked on the strength of ONE file inside it. Recursively: 6 files, **1 tracked**, and the five untracked ones are cited by THREE tracked documents. Same shape as the case files, one directory deeper, and the tracked `ENG-060_RUNTIME_PROOF.md` is the sibling that shows the intent. A top-level `os.listdir` reported a tree as clean when a recursive walk says otherwise -- the same class as the merge-pruned sweep and the root-dirname baseline earlier today, and the THIRD scoping error in one day. **A SHARED RULE IS EXTENSION-SCOPED AND MISSES ITS OWN DIRECTORY'S EVIDENCE:** `.gitignore:524` is `**/reports/**/*.csv`, so five CSVs under the cases reports directory are excluded and the `.md` and `.json` evidence sitting beside them are not. Whoever decided report CSVs are generated meant the directory, not the extension. **NOT EDITED HERE** -- `.gitignore` is versioned and shared, so widening it is a ruling rather than a cleanup. **AND TWO OF THIS LANE'S OWN PROBES DID NOT FINISH.** `recovery_under_foreign_lock.ps1` removed the lock and the journal -- the interesting debris -- and left `JRN5` in the sandbox; the six-door probe left `LOCKP` the same way. The INSERT probe did erase its fixture, which is what made the other two visible by contrast. Both now carry an `ERASE ... CONFIRM` in their own teardown, because a fixture left in SANDBOX is a table the next probe may open by accident and a sandbox that fills up is how a clean run stops meaning clean. **GOOD-NEIGHBOUR CHECK, RUN RATHER THAN ASSERTED:** no commit today touched `src/help`, `gui/uidef`, `docs/datadict`, the manualgen tree or the pseudo-chat board. The board and twenty-six other files ARE modified in the working tree by other hands, which is precisely why every staging script in this lane lists explicit paths. |
| OI-024 | 2026-08-31 | 2026-10-17 | repo | **THE GATES ARE A PER-CLONE HOOK INSTALL, SO NO FRESH CLONE ENFORCES THEM. REDRAFTED THE SAME DAY IT LANDED: THE FIRST DRAFT'S EXTERNAL-AI HALF WAS WRITTEN WITHOUT READING THE AI-BBS, AND THAT ERROR IS THE MORE USEFUL HALF OF THIS ROW.** Raised 2026-08-31 when the owner asked whether a paid GitHub AI subscription could be put to work here. **WHAT STANDS, MEASURED 2026-08-31 AND UNCHANGED BY THE REDRAFT.** The four published workflows run `tools/ci/source_policy.py` (LICENSE and README wording), an MSVC build, and a GCC build with ctest; a grep of the workflow directory for `prepush`, `gate`, `pre-commit`, `hooks`, `session_coordinator`, `repository_role_guard`, `check-aif` and `r_number` returns nothing. Zero house gates run in CI. One layer down, `tools/staging/prepush_gate.py` states it about itself: it runs at COMMIT time from the `pre-commit` hook, that hook lives in the repository's local hooks directory, the directory is not tracked, and its header says the wiring "cannot be documented from inside the repository except here". Confirmed: `git ls-files` matches no hooks path, `core.hooksPath` is unset, and this clone's six hooks arrived through `--install-hook`. **SO ENFORCEMENT IS A PROPERTY OF A CLONE, NOT OF THE TREE -- AND THERE IS A LIVE SECOND CLONE PROVING IT.** `D:\dev\x64base-site` is not a sibling project: its `origin` is this same repository, it sits on `codex/lean-sites-publish`, and it has **zero hooks installed** (samples only, `core.hooksPath` unset). Two working copies of one repository, one gated and one not, and nothing in the repository can tell you which is which. **WHAT DID NOT STAND, AND WHY IT IS RECORDED RATHER THAN QUIETLY REPLACED.** The first draft argued that a Copilot-authored pull request "meets the build, meets ctest, and meets none of the gates", and proposed running the gates in CI so agent work would be governed. It assumed the channel for an external AI is a GitHub PR arriving at CI. **IT IS NOT, AND HAS NOT BEEN SINCE 2026-07-25.** The owner's correction was blunt and correct: *"You are just ignorant of pseudo-chat/bbs."* **WHAT IS ACTUALLY THERE, read from the running site's AI views on 2026-08-31.** Governance sits at the IDENTITY layer, upstream of the gates entirely. An external AI is a MEMBER with a kind (Human / AI / External) and a default role; it authenticates over a loopback socket with Argon2id, passes an RBAC/POSTPERM gate per board, and `current_member()` attributes the real author before the post lands under a table FLOCK. Six boards with per-board post permission -- governance is owner-only, guestbook takes `bbs.guest`. Work is ASSIGNED on the board: a 2026-08-08 post reads `[assign:grok] Lane 1 write adapter M2`, names `docs/maintenance/LANE_L1_WRITE_ADAPTER_ASSIGNMENT_GROK_V1.md`, and instructs "claim a fresh AIF; attributed writes only". Grok filed a session handoff to the worklog board carrying `RUN=AIPR-20260725-001`. Two channels -- live BBS post and document intake -- converge on a HARVEST that must produce a run in `labtalk/registries/ai_runs.yaml`, an audit envelope, and an index entry in `labtalk/registries/ai_report_index.yaml`, and only then curate into an AIF claim plus an intake row. **THE AIF NUMBER IS THE JOIN** between an external agent's work and the register this house claims numbers from. **AND COPILOT IS ALREADY INSIDE IT** -- `AIPR-20260809-COPILOT-001`, provider Microsoft, a `review_needed_change_package` with a MANIFEST in `docs/maintenance/external_ai_intake/`. The first draft's closing worry, that vendor agents were outside the hub, was wrong within an hour of being written. **THE REAL GAP IS THE HOUSE'S OWN AND IT IS WIDER THAN THE HOUSE LAST MEASURED.** `docs/maintenance/MONITOR_HARVEST_CURATE_EXTERNAL_AI_V1.md` -- authored by `member.ai.claude.cowork`, an EARLIER SESSION IN THIS SAME MEMBER IDENTITY -- records four submissions with one indexed, and the published data-flow diagram draws the completeness gate ("has all records?", absent -> flag) as DASHED, meaning proposed. **RE-MEASURED 2026-08-31, twenty-five days later: the three it named uncurated are still uncurated.** `AIPR-20260725-001` appears 14 times in the runs ledger and ZERO times in the index; `evaldiff_eof_probe` and `AIPR-20260804` appear in neither. **AND THE WIDER COUNT IS WORSE THAN THE SAMPLE:** `docs/maintenance/external_ai_intake/` holds 12 tracked intake directories and the index holds 5 report_ids across three providers (Anthropic 3, Microsoft 1, xAI 1); **exactly 2 of the 12 directories are named anywhere in the index** -- and collapsing the seven `aif112_*` phase and amendment directories into one submission still leaves 2 of 7 distinct submissions indexed. So a submission can produce a run and an envelope, be worked, and never become findable, with nothing anywhere saying so. That is OI-023's proof generator with no `else`, one layer up: the artefact whose job is to attest coverage cannot express a miss. **A SECOND, SMALLER BLINDNESS FOUND WHILE MEASURING, and it is the same shape:** `ROOTS` in `tools/staging/check_cited_paths.py` lists ten prefixes and the workflow directory is not among them, so **no gate in this tree audits the workflow files at any time**. That is why OI-016 had to be found by hand and why `build-windows.yml` could be untracked-yet-published. One tuple entry, same as the `gui/uidef` fix that tuple's own comment records. **THE DECISIONS, REFRAMED BY THE ABOVE, and none is made.** (1) **Build the completeness gate.** It is drawn, specified and unimplemented, and it is the one that costs work today. (2) **Decide what the vendor branches are.** `origin` carries five `copilot/*` branches, two named `fix-repository-policy-job` and `fix-repository-policy-job-again` -- an agent pushing at the policy gate itself -- and four local `codex/*` branches. Copilot has an indexed intake report AND unindexed branches, so the two paths are both live and unreconciled: either branch work is a submission channel and harvest must see it, or it is out of scope and the row should say so. (3) **Only then, whether a green CI means "met the gates."** If yes, the gates must run there and a passing check becomes a claim the house can stand behind; if no, say so in the workflows so a green check is not read as an approval it never offered. The portability triage that follows is NOT done -- `tools/staging/` holds 18 non-test scripts and `tools/staging/prepush_gate.py` also invokes gates under `tools/coordination/`, `labtalk/ai_portal/` and `tools/fullstack_docs/`, so the population was never enumerated; `tools/staging/repository_role_guard.py` and `tools/staging/check_sandbox_git_guard.py` ask questions that have no meaning on a runner and would go green by confusion. **NOT A DEFECT IN ANY GATE.** Every one is correct and runs where it was wired to run. **AND THE METHOD FAILURE IS THE DURABLE PART:** a session reasoned for an hour about how to govern external AI, in a tree whose external-AI governance was designed, built, running, published on its own website, and documented in a note written by a prior session under the same member identity. Nothing in the change set pointed at it; the gates cannot point at it; the AI views are served by `tools/reports/serve_dynamic_reports.py` and are not in this repository's reading path at all. The tree could not tell a session what the tree already knew. **STATUS 2026-09-19: DECISIONS 1 AND 3 ARE BUILT AND COMMITTED; 2 IS HALF-MEASURED. NEXT LOOK MOVED 2026-09-14 -> 2026-10-17, which this row's own advisory calls legitimate where leaving it stale is not.** DECISION 1, THE COMPLETENESS GATE, is `labtalk/ai_portal/check_report_index_completeness.py` (975acb8e2, extended 951654520). It reconciles four populations and always prints them, so zero findings over zero inputs cannot be read as zero findings over fourteen. A: 12 of 14 intake directories named by no index entry -- RULED, the index header says received packages MUST be added. C: the merged run ledger was stale by 14 fragments including the previous day's, so the gate reads runs.d/ and CHECKS ai_runs.yaml rather than trusting it; REGENERATED 2026-09-19 and ruled findings fell 26 -> 12. B: 22 of 22 runs unindexed, ADVISORY and never failing, because whether an internal run owes an index entry is a decision this row still owes. D: 5 of 14 intake directories carry no ai_report_audit envelope at all -- which is what `intake=9` in the audit line has always meant, and the five produce no finding because a missing envelope is an absence. DECISION 3 IS DONE AS A STATEMENT, NOT AS A CI CHANGE (975acb8e2): all four workflows now say what a green check means, that they run none of the house gates, that the hook is per-clone and untracked so a fresh clone enforces nothing, and that repository_role_guard and check_sandbox_git_guard would go green by confusion on a runner. Whether the gates SHOULD run in CI remains open; until they do, the check no longer implies an approval it never offered. DECISION 2 IS HALF-ANSWERED BY ROOT COMMIT AND ANCESTRY, and the method is this row's own lesson from OI-029 -- compare root commits before comparing anything else. All five copilot/* branches root at 7c56022a1, the ENGINE root, not the site root 6ee42f04c; they are branches of the public engine repository and cannot be what a local site server runs. Merged into origin/main and therefore deletable on evidence: code-review-parameters, research-architecture-code-review, research-deraldg-repositories-audit (all ahead=0). NOT merged, two commits each: fix-repository-policy-job and fix-repository-policy-job-again -- AND BOTH DO THE SAME THING, replacing the LICENSE with 'To be determined.' to make the repository-policy check pass. An agent buying a green check by deleting the thing the check protects is the sharpest argument this row could have for decision 3, and it arrived after decision 3 was written. Do not merge either. THE SITE-BRANCH HALF IS NOT MEASURED: a per-file reconciliation was drafted and its verdicts disagreed with a manual check on one bracketed path, so nothing there is deletable yet. TWO CORRECTIONS TO THIS ROW'S OWN NUMBERS, found by building the gate. The index has NOT grown: it held 5 report_ids on 2026-08-31 and holds 5 today, against 12 then and 14 now intake directories -- a `grep -c report_id` that answers 7 is counting two mentions in prose. And AIPR-20260725-001, named here as the flagship miss '14 times in the runs ledger and ZERO times in the index', is a LINE count rather than fourteen records, and runs.d/AIPR-20260725-001.yaml reads `member: member.ai.claude.cowork` with three closeouts. It is an INTERNAL run, so it belongs to the unruled population B, not the ruled population A. The miss is real; its classification was not. |
| OI-029 | 2026-09-02 | 2026-12-01 | repo | **FIVE WEBSITE BRANCHES, ONE PUBLISHES, AND THE NAME MISLEADS. PARKED FOR AGED RELEASE, NOT MERGE.** Measured 2026-09-02 in `D:\dev\x64base-site`. **x64base.com serves `gh-pages`, built from `codex/lean-sites-publish`** -- read from the DEPLOYED artifact, `git cat-file -p origin/gh-pages:artifacts/site-release.json`: release 132, source_commit cb3575556, published 2026-08-17. **WHY THE NAME MISLEADS (owner, 2026-09-02): Codex originally designed the site and named the branch.** The `codex/` prefix is an AUTHORSHIP artifact from the scaffolding tool, not a workflow classification, and it has outlived the scaffolding by two months of hand work. Read no branch policy into it. Despite that prefix the branch IS THE SITE'S WORKING TRUNK; `main` is the lagging one, last touched 2026-07-03 and **204 commits behind, 2 ahead**. The steward initially read the prefix as meaning a side branch and flagged the day's site commits as possibly misplaced. They were not. THE 2 COMMITS ON `main` ARE NOT LOST WORK: `93623d981` (LabTalk campus content) and `d427bb02f` (trim ~35 MB of media -- 8 slides, a montage, a 22 MB pptx). The trimmed media is ABSENT from the publishing branch too, so no orphaned payload; and every content file checked (`contributors.mdx`, `developer-handbook.mdx`, `trademarks.mdx`, `app/licensing/page.tsx`) exists on BOTH and is FURTHER EVOLVED on codex -- larger in three cases, and deliberately SMALLER for `trademarks.mdx`, which matches the recorded site-wide trademark removal. Superseded by content evolution, exactly as OI-025 describes for the `ai-portal-*` pair. THREE LOCAL-ONLY BRANCHES, never pushed, one commit each, all frozen 2026-07-12: `codex/site-hardening` (Clarify public website and project status), `codex/site-notices-under-about` (Move project notices under About), `codex/site-project-first` (Put the project story first). SPOT-CHECKED AND THE IDEAS LANDED ANYWAY: site-project-first deleted `app/licensing/[...slug]/page.tsx` and that file is ABSENT from the publishing branch today, reached by a different route; the Footer no longer carries the notices block that site-notices-under-about moved. **A GAP WORTH KNOWING BEFORE THE NEXT PUBLISH:** `publish-github-pages.mjs` verifies the DEPLOY worktree is on `gh-pages` and that origin is `deraldg/x64base`, but it does NOT constrain WHICH BRANCH YOU PUBLISH FROM -- it records `source_branch` and proceeds. Publishing from an abandoned branch would succeed, and the only evidence would be a field in a JSON file. **RETRACTED, SAME DAY, BEFORE ACTION: "the default branch is the stale one" WAS WRONG AND THE FIX IT IMPLIED WOULD HAVE DAMAGED THE PUBLIC REPO.** `origin/HEAD` does resolve to `main`, but `main` IS NOT A SITE BRANCH AT ALL. **`deraldg/x64base` IS FOUR UNRELATED PROJECTS SHARING ONE REPOSITORY AS ORPHAN BRANCHES**, proven by root commit -- `git rev-list --max-parents=0`: ENGINE `7c56022a1` (`main` and 12 others, the public C++ repo: CMakeLists.txt, BUILDING.md, CONTRIBUTING.md), DEV `ee49498b1` (`development`, the ccode tree), SITE `6ee42f04c` (`codex/lean-sites-publish`, and NOTHING ELSE on the remote), and `gh-pages` on its own root `572f33cd5`. `git merge-base origin/main origin/codex/lean-sites-publish` returns NOTHING. **SO: DO NOT REPOINT `origin/HEAD`.** `main` is the PUBLIC ENGINE REPOSITORY'S FRONT PAGE; changing the default would land visitors to x64base on the website instead of the engine. **THE TRAP THAT PRODUCED THE ERROR -- TWO DIFFERENT THINGS NAMED `main` IN ONE WORKING DIRECTORY.** In `D:\dev\x64base-site`, LOCAL `main` is `d427bb02f` (2026-07-03, SITE root, **no upstream, never pushed**) while `origin/main` is `9470a50d9` (2026-08-21, ENGINE root). The steward compared against the local one and then described it using the remote one's name. The underlying widows-and-orphans finding SURVIVES -- local `main` really is the site's own abandoned branch, really is 204 behind and 2 ahead of the trunk, and those 2 commits really are superseded -- but every sentence calling it "on GitHub" was false. THE SITE HAS EXACTLY ONE BRANCH ON THE REMOTE. **A SECOND LIVE TRAP:** all three abandoned local branches have `upstream` CONFIGURED to `origin/codex/site-*` refs THAT DO NOT EXIST, so a bare `git push` while standing on one would CREATE the remote branch rather than refuse. **A CHECK THAT WOULD HAVE CAUGHT THIS IN ONE COMMAND, and the generalizable lesson: in a multi-project repository a branch NAME identifies nothing -- compare ROOT COMMITS before comparing anything else.** **RENAMING THE PUBLISHING BRANCH IS CHEAP -- MEASURED, NOT ASSUMED.** Nothing gates on the name: `site-checks.yml` triggers on `branches: ["**"]`, so any branch runs checks; the publisher does not constrain the source branch (see the gap above); and the only two tracked files containing the literal string are GENERATED report HTML (`public/AI/AI_PORTAL_REPORT.html`, `public/eco/index.html`), both of which regenerate -- though `eco/index.html` records the name as HISTORY of commit 49fd9f607 and would read oddly, not wrongly, after a rename. AT NEXT LOOK: confirm the three local branches still hold nothing unique, then delete them; decide whether `main` should be fast-forwarded, retired, or left as a deliberate lagging snapshot (it is not the publication route either way); and consider gating `source_branch` in the publisher. Do not merge `main` into the publishing branch on the strength of those 2 commits. |
| OI-028 | 2026-09-02 | 2026-10-15 | repo | **THE MANUAL PUBLISHES `supported` FOR 33 COMMANDS WHOSE OWN CONTRACT SAYS OTHERWISE, AND EVERY DISAGREEMENT RUNS THE SAME DIRECTION.** Measured 2026-09-02 by `tools/manualgen/check_contract_status_drift.py` (report-only, exit 0; `--strict` exits 2 for later): 219 commands declare a status in `@dottalk.usage`, 164 are published, 109 do both, and 33 DISAGREE -- all 33 with the manual saying `supported`. Uniform direction is the informative part: this is not scattered staleness, it is a MISSING COMPARISON. Grouped by the question the contract was actually answering: LIFECYCLE 1 (`LMDB_UTIL` declares `deprecated`, published `supported` -- the single most misleading row in the manual, because a reader cannot distinguish a command being retired from one being recommended); COMPLETENESS 12 (`SQL`, `SQLHELP`, `SQLERASE`, `SQLVER`, `UPDATE`, `TUPLEDELTA`, `AUTODBF` = experimental; `TVISION` = stub; `IMPORTSQL`/`EXPORTSQL` = supported-stub-mixed; `FOXPRO` = supported-conditional; `SIX` = implementation-present); AUDIENCE 13 (`AREA51`, `LMDB`, `LMDBDUMP` and the four `*_BUFFER` commands = developer; `STUDENTECHO`/`STUDENTHELLO` = sample-extension; `CANARY` = dev-canary; `IDX` = backend-helper; `DOTSCRIPT` = supplemental; `PSHELL` = implementation-helper); MECHANISM 7 (`ELSE`/`ERP`/`STRUCT` = implementation-shim; `ENDIF`/`ENDSCAN`/`ENDUNTIL`/`ENDWHILE` = active, which is not a maturity claim at all -- they are block terminators, a SIXTH question the one field is being asked). ROOT CAUSE, and it is vocabulary before it is data: 242 contracts use 24 DISTINCT status values, one field answering five or six questions, with `supported` at 178 acting as the default that swallows whatever cannot be expressed -- which is why `supported-stub-mixed` and `supported-conditional` exist. TWO READINGS REMAIN OPEN and this tool deliberately does not choose: either the catalog is stale, or catalog `supported` means DISPATCHABLE while contract `status:` means MATURE -- one word, two questions. Glossary rows TERM.CONTRACT.AUDIENCE / COMPLETENESS / LIFECYCLE / SUPPORTED were proposed the same day (GREEN_TENTATIVE) to settle it, with two values already ruled by the owner: SQL/SQLSEL are `in-progress` ('actively developing... not a claim of completeness') and TVISION is `template`, not `stub` ('a working template'). AREA51 IS THE CANARY -- its contract prose already reads 'It read supported until 2026-08-30 while THIS PARAGRAPH already called it a developer probe'; the author caught it on one command and the systemic count is 33. AT NEXT LOOK: rule on the axes, then decide whether the manual publishes them separately; only then is `--strict` affordable. |
| OI-026 | 2026-09-02 | 2026-10-01 | repo | **NON-ASCII REACHED THE ACCEPTED MANUAL, AND THE RULE WAS BEING ENFORCED ON THE FAR BANK.** `check_house_style.py` blocked a commit over 9 characters in GENERATED files: 3 x U+26A0 in the command reference README and 6 x U+2192 in `commands/pshell.md`. Committed with `--no-verify` on owner instruction. TWO THINGS WERE WRONG, and only one was the characters. (1) THE DEMAND WAS UNSATISFIABLE IN ITS OWN TERMS: both files are rendered output, so the only way to comply is to edit the generator or its source -- editing the page is undone by the next regeneration. A rule whose fix is guaranteed to be overwritten is enforcement on the far bank, and its practical effect is a bypass. (2) THE ENFORCEMENT WAS BROADER THAN THE DECLARED RULE: `CLAUDE.md` bans em-dashes and names `--` and `->`; the checker adds a catch-all for any codepoint over 127, which is what caught U+26A0 -- by nothing the maintainer had written down. That is AIF-079 inverted: enforcement without a declaration. FIXED 2026-09-02, same day, at the producer: the renderer now emits `(!)` (owner's choice) and `--` in the review book title, verified in the accepted manual after Gate 4 apply MANRUN-20260902T164010Z-78127AF4 -- U+26A0 now appears NOWHERE in the accepted command reference; `check_house_style.py` excludes generated manualgen output (`published/`, `generated/`, `harvested/`, `backups/`) from the GATE path, applying the reasoning its own `--audit` walk already carried -- 'mostly vendored and generated text that nobody authored and nobody should edit' -- which had been written for the reporting mode and never applied to the enforcing one; and `tools/manualgen/check_harvest_ascii.py` reports the backlog at CANDIDATE stage, attributed to producer, report-only for the reason `check_house_style` already learned the hard way (a gate that goes red on somebody else's backlog on day one is switched off by day three). REMAINING, and it is source work not documentation work: 22 right-arrows from SOURCE_MINER (PSHELL, SQL HELP) and 6 em-dashes from SHARED_MSG, 28 harvest rows across 3 topics. `helpdata_messages.cpp` alone carries 419 non-ASCII lines, so the true backlog is far larger than what surfaces on a page, and it is runtime-visible error text -- its own lane, changing what users see. AT NEXT LOOK: fix `cmd_pshell.cpp` (6 lines, cheapest and fully visible in the manual), locate the `SQL HELP` source which is NOT `cmd_sql.cpp` (0 non-ASCII, checked), then decide whether `--strict` on check_harvest_ascii is affordable. |
| OI-025 | 2026-09-02 | 2026-12-01 | repo | **TWO JULY `ai-portal-*` BRANCHES SHARE NO HISTORY WITH `development`. PARKED FOR AGED RELEASE, NOT MERGE.** `origin/ai-portal-audit-closeout-20260715` (53 commits) and `origin/ai-portal-canonical-start-20260715` (52), both frozen 2026-07-15. Measured 2026-09-02: `git merge-base development <branch>` FAILS on both. This repo carries TWO UNRELATED HISTORIES -- `development` roots at `ee49498b1` (2025-08-14), while `main` AND both these branches root at `7c56022a1` (2026-05-03, 'Initial recovered x64base'). `main` and `development` are joined by PROMOTION, which copies content; `main`'s tip literally reads 'promotion from development'. So `git merge` would refuse without `--allow-unrelated-histories`, and 'merge them into dev' is the wrong frame. NOTHING UNIQUE IS AT RISK, which is why parking is safe rather than lazy: the July portal holds 14 files against development's 65; the only file present there and absent from `development` is `labtalk/ai_portal/publication/ai-portal.mdx`, and that content lives on as `content/docs/labtalk/ai-portal.mdx` on `codex/lean-sites-publish`. `EXTERNAL_AI_CHANGE_PACKAGE_V1.md` and `LOCAL_ACCESS_AGENT_CHECKLIST_V1.md` were checked by name and are both PRESENT on `development`. AT NEXT LOOK: confirm the finding still holds, then delete both remote branches. They are a July checkpoint of the public-lineage portal, superseded on both lineages. Do not delete before confirming, and do not attempt a merge. |
| OI-030 | 2026-09-05 | 2026-10-06 | decision | **A DEFFN body CANNOT USE ITS ARGUMENTS, so the runtime custom-function facility is provable but not teachable.** AIF-155 ruled custom functions a STUDENT surface (owner, 2026-09-05). Measured the same day: arguments are accepted up to kCustomFnMaxArgs = 16 and DROPPED; the MVP body returns its stored text, so DEFFN GREET = hello answers hello whatever you pass it. The formula body DEFFN NAME(a,b) = <expression> is listed not-MVP in docs/maintenance/RUNTIME_DEF_FAMILY_LANE_V1.md section 4. Everything shipped 2026-09-05 (validator, promotion, lane identity) makes the facility PROVABLE and none of it makes it TEACHABLE. CORRECTLY BLOCKED, not merely deferred: AIF-109 holds five pending owner rulings on the parameter surface and this must not pre-empt them. Promote to a milestone under AIF-155 the day those rulings land. |
| OI-033 | 2026-09-05 | 2026-09-26 | decision | **Two rulings now point in opposite directions about who may use DEFFN, and this AIF created the collision rather than finding it.** AI_DEV_TOOLS_SECURITY_DOCTRINE_V1 section 0 requires scoped permission of every agent with the owner as sole exemption; AIF-155 rules custom functions a STUDENT surface. The gate is REAL and CALLED (cmd_deffn.cpp:96, cmd_defcmd.cpp:106, both devtools_permitted()) and currently DORMANT -- it permits unless DOTTALK_DEVTOOLS_REQUIRE_PERMISSION is set. STUDENTS ARE EXACTLY THE POPULATION IT DECLINES THE DAY IT IS ARMED. Harmless today, wrong the moment enforcement is switched on. Needs an owner ruling before either document is edited. |
| OI-034 | 2026-09-07 | 2026-10-19 | repo | **FOURTEEN `.dts` BASENAMES NAME MORE THAN ONE TRACKED FILE WITH DIFFERENT CONTENT, AND TWO OF THEM NAME THREE.** Measured 2026-09-07 while closing OI-019 half (2), over 287 tracked `.dts` in 270 distinct basenames: 15 basenames have more than one tracked copy, ONE is byte-identical and **FOURTEEN DIFFER**. `regression.dts` and `version.dts` each have THREE. **THE FIRST MEASUREMENT WAS SCOPED WRONG AND UNDERSTATED IT BY MORE THAN HALF.** OI-019's walk compared UNTRACKED against TRACKED and found six; it never asked whether the tracked corpus collides with itself, because the question it was built to answer was 'what is missing', not 'what is ambiguous'. A walk answers the question it was shaped for and is silent about every other one -- the count was not wrong, the SCOPE was, and a scope error reads exactly like a clean result. **WHAT THE RESOLVER ACTUALLY DOES, read rather than assumed** (`src/cli/cmd_dotscript.cpp:300-338`, `build_candidate_specs` + `resolve_existing_script_path`): candidates are built in a fixed order -- `scripts/<spec>`, `scripts/<spec>.dts`, `tests/<spec>`, `tests/<spec>.dts` -- and the FIRST that exists on disk wins. **It is deterministic first-match and it does NOT recurse into subdirectories.** So the hazard is NOT a coin flip, and an earlier statement of mine that 'which copy runs depends on a runtime setting' was overstated: it depends on slot state only in the sense that any path slot is settable. **TWO CONSEQUENCES, AND THE SECOND IS THE REAL ONE.** (1) Subdirectory copies are unreachable by a bare `DO <name>` and are inert for resolution -- including a `manual/` pair that commit `6cfd181f6` itself created, which is mine and is recorded here rather than quietly left. (2) A name present in BOTH the SCRIPTS and TESTS trees ALWAYS yields the SCRIPTS copy, silently and correctly and possibly not the one the author was reading. That is the reader-facing defect: two files, one name, and no diagnostic saying which you got. The forks: `MCC_EXHAUSTIVE_FULL_X64_REGRESSION_CANARIES_SETMODERN_VB_10_2_FIXED.dts`, `SYSTEM_METADATA_BOOLEAN_FIX.dts`, `SYSTEM_METADATA_SEED_v1.dts`, `SYSTEM_METADATA_SEED_v2.dts`, `SYSTEM_METADATA_VALIDATE_v0.dts`, `cascade_erp.dts`, `ersatz_rel_enum_browser.dts`, `fullstack_post_refresh_runtime_v1.dts`, `fullstack_pre_refresh_runtime_v1.dts`, `index_txn_lmdb_maintenance.dts`, `mcc_exhaustive_full_regression_canaries_setmodern_v5.dts`, `pydottalk_shakedown.dts`, `regression.dts`, `version.dts`. **NOT ALL FOURTEEN ARE DEFECTS** and this row must not be closed by renaming fourteen files: the two `fullstack_*_runtime_v1.dts` pairs are per-run captures under dated DOCFLUSH directories, where the same name in two runs is the POINT, and `index_txn_lmdb_maintenance.dts` has an archived session copy under `src/AIPortal` that is a snapshot by design. The judgment is per pair: same thing twice (retire one), genuinely different things (rename one), or an intentional dated snapshot (leave, and say so here so the next walk stops re-finding it). **WHAT IS OWED:** that per-pair ruling, and a decision on whether `DO` should WARN when a bare name matched more than one candidate -- the resolver already builds the full candidate list and records its attempts in `attempts_out`, so it knows, and today it says nothing. Raised out of OI-019, which is closed. |
| OI-035 | 2026-09-08 | 2026-10-08 | decision | **`SID` IS A COMPILED-IN MAGIC FIELD NAME THAT MINTS A KEY IN EVERY TABLE OF EVERY FLAVOUR, AND THE OWNER RULED IT DEPRECATED AND NOT REPLACED (2026-09-08, AIF-158).** `plan_sid_if_needed()` (`src/cli/append_support.cpp`) looks a field up by the literal spelling SID, asks NO registry and NO header, and mints max+1 into it when it is blank -- on every APPEND, in every table, of every flavour, whether or not anyone declared anything. Nothing documents it as a contract and nothing lets a table opt out, so a customer column that merely happens to be called SID is silently autoincremented. **IT IS ALSO LOAD-BEARING IN THE TREE'S OWN FIXTURES RIGHT NOW**, which is why the ruling is deprecate-and-warn rather than delete: PKPOLICY's PKP_G1/G2/G3 require the next three APPENDs to mint 1, 2 and 3 through this path, and PKDURABLE's PKD_W5/PKD_G5 ride on it DELIBERATELY, as the live control that tells generation-by-NAME apart from generation-by-STAMP. Deleting the generator without rewriting those arms turns a working control into a permanently green line, which is the failure OI-031 and OI-032 were both raised about. **AND IT IS WHY A REAL DEFECT WAS INVISIBLE FOR THE LIFE OF THE FEATURE:** every fixture in this tree that generates a key names it SID, so exactly one generator ever fired, and the buffer trample fixed at `86f628026` had nothing to destroy until a fixture finally declared a key under a different name (AIF-158, 2026-09-08). **WHAT IS OWED, in order:** (1) a WARNING when the name-keyed generator fires -- shape and channel need an owner ruling, because a warning on every APPEND of a legacy student table is its own defect; (2) a way for a table to DECLINE it, which the registry and the header stamp already give the other two triggers and this one has never had; (3) **THE REMOVAL IS A MIGRATION QUESTION AND NOT A CODE QUESTION** -- DBFs in the field carry SID columns whose values were minted by this path, and nothing anywhere records which of them were ever meant as keys. Do not close this row by deleting the function. Raised out of AIF-158, where the ruling and the measurement both sit. |
| OI-036 | 2026-09-09 | 2026-10-10 | repo | **`docs/messaging` is 419 authored files with ZERO history, and a governing document said otherwise.** Measured 2026-09-09: `git ls-files docs/messaging` returns 0; `git status --porcelain -uall -- docs/messaging` returns 419, none of them gitignored. `dottalkpp/data/messaging` is 4 files with 1 tracked (`gui_messages.csv`); the two `.dbf` tables and the `.dtx` memo sidecar are untracked, and the sidecar was rewritten the day this was measured. THE DOCUMENT CLAIM IS ALREADY FIXED -- R141 corrected `PROMOTION_PROCESS.md` rule 4, its section 7 and the `PROMOTE.manifest` header comment, all three of which asserted these lanes were "versioned in development" as a statement of fact. THIS ROW IS THE WORK THAT CLAIM DESCRIBED. `UNTRACKED_TREE_DISPOSITION_PROPOSAL_V1.md` puts `docs/messaging` in the durability keep-set -- authored, never ignore -- and its own recommended sequence makes this step 4: "commit as scoped per-lane slices when each is ready. This is the durability work; it is not a hygiene sweep and should not be rushed into one." So the answer is NOT one 419-file commit. WHAT HAS TO BE DECIDED FIRST, and it is why this is `repo` and not `decision`: which of the 419 are authored and which are generated outputs of the messaging pipeline. The proposal's own rule is that disposition follows the file's ROLE, not its extension. Nobody has classified this lane. ALSO UNSETTLED: the three data tables. The 2026-08-05 owner ruling lets `.dbf`/`.dtx` THROUGH so any source table can be tracked with a plain `git add`, but permits rather than requires it -- and `SYSTEM_MESSAGE_TEXT.dtx` churns, so tracking it manufactures conflicts with whoever is writing it. Decide whether these tables are fixtures worth history or working data that a backup should hold instead; "nothing holds them" is the current answer and is not one anybody chose. |
| OI-037 | 2026-09-09 | 2026-10-17 | repo | **`PROMOTION_PROCESS.md` section 9 is a private backlog that nothing surfaces.** It holds four "open items to reconcile"; one is struck and ruled (AIF-092 R4), THREE ARE LIVE and have never appeared in this register, so none of them nag. That is the exact condition this file's own header quotes a measurement against: an ungated obligation held 33 percent compliance while gated ones held 83-94. THE THREE: (2) `PROMOTE.manifest:57` promotes `BUILDING.md` FROM THE REPO ROOT, where it does exist (5596 B, verified 2026-09-09), while `main` carries a second copy of it under `docs/getting-started/` -- the manifest even carries a NOTE about it at line 75. So promotion refreshes the root copy and the `docs/getting-started/` one is an orphan in `main`, which is precisely the off-allow-list DIFF that section 6's own drift audit says should trend to zero. "Pick one" has been the instruction for some time and nobody has. (3) expand the allow-list via `PROMOTE.additions.manifest` so engine source and active docs are owned by promotion rather than frozen in the baseline. (4) purge the `__pycache__` (58) and `messaging`/`metadata` (41) files now in `main` -- note this one is downstream of R141, which corrected what the same document says about those lanes. HOW IT SURFACED, because it is the point: editing that document for R141 pulled it into `cited-paths` scope FOR THE FIRST TIME, and the gate immediately reported that document's mention of the `docs/getting-started/` copy as cited-but-absent. The gate is right and the sentence is also right -- that path deliberately exists only in `main` -- so the advisory is a false positive, and the true finding is the one behind it: A DOCUMENT NOBODY STAGES IS A DOCUMENT NO GATE READS. Either move these three into this register and leave section 9 pointing here, or decide that section 9 is the right home and give it something that nags. |
| OI-038 | 2026-09-12 | 2026-09-26 | decision | **`main` carries 915 tracked files the allow-list does not authorize, and the gate that measures them is not in the promotion anyone actually runs.** Measured 2026-09-12 by `tools/staging/audit-drift.ps1 -ReportOnly` against `e1425cb10`: projection matching dev **869**, projection DRIFT **3**, **OFF-projection 915**, git-managed engine/build **1206** (blind BY DESIGN, certified by a cold-clone build), GONE **11**, `__pycache__` **0**, non-publish MDO lanes **80**. **Gate verdict: FAIL.** **THIS SUPERSEDES THE FIGURE 296** that this row carried on the day it was raised. 296 counted files that were off-manifest AND absent from development; the gate does not care whether development tracks a file, because an allow-list means allow-list -- anything tracked on `main` that no glob claims is off-projection regardless. The gate's test is the correct one and mine was too narrow. Reproduced independently from `git ls-tree -r origin/main` plus `PROMOTE.manifest` using the gate's own glob and classifier regexes: all four counts match exactly, so the triage is against the same set the gate sees. **DRIFT = 3 IS THE NUMBER THAT GROWS.** `dottalkpp/data/workspaces/WORKSPACES.dbf`, its `.dtx`, and `my_custom_workspace.dtschema` are manifest-matched, differ between the trees, and are UNTRACKED in development -- so after `651ce911a` the overlay will never refresh them again. That is an unanticipated consequence of the OI-018 fix: 545 files on `main` were refreshed by every promotion and now are not, so *publishing unreviewed content* became *publishing STALE unreviewed content*, frozen at 2026-08-21. Today three have diverged; the count only rises. (`WORKSPACES.dbf` is the live workspace catalog, mutated by any session that mints a workspace -- arguably session state that does not belong on `main` at all.) **TRIAGE, in `claude/TRIAGE_THE_915_OFF_PROJECTION_FILES_ON_MAIN.md`: (A) DECIDE READINESS on ~575**, which is NOT "add because section 3 names the lane" -- `PROMOTION_MODEL_SEED_V1.md` rules *"do not add a lane to the manifest to tidy up; add it when it is genuinely ready to publish, with its own environment"*, so section 3 says which LANE a file belongs to while the seed says READINESS gates entry, and being on `main` already is an artifact of the baseline rather than evidence of readiness: 215 rendered manual pages under `docs/manuals/developer/manualgen/published/**` (once judged ready, under a since-RETIRED entry -- is that judgement current?), 42 more `docs/manuals/` assets, ~100 `labtalk/**` portal seeds, ~100 `tools/**` doc engines, 34 contracts and AI-facing docs, and 17 root launchers -- the launchers being the ONE group where the case is consistency rather than readiness, since `run-cli.ps1`, `run-wb.ps1` and `launch-common.ps1` ARE listed while `datarun.ps1`, `biblerun.ps1` and `run-erp.ps1` are not and nothing distinguishes them. **(B) PURGE ~150**, unambiguous and needing no decision: 65 `tests/*.txt` scratch (`New Text Document - Copy (2).txt`, `sgfsd.txt`, `combined_sources_alpha_4_2.txt`), the 80 non-publish MDO lanes, three `X64BASE_*.md` working docs, `windows_book.txt`, and `WORKFLOW_X64BASE.md` -- which section 9.1 **RULED RETIRE on 2026-08-07** and which is still published. **(C) DECIDE 253**, and this is the only part that is actually a decision: 54 `SESSION_CLOSEOUT_*.md` and 118 other `docs/maintenance/**` (a model repo arguably should show how the work is done -- or `main` is not a diary), and 81 structured `tests/` suites and probes (section 3's PRODUCT lane includes "databuild and smoke scripts" -- do these qualify?). **DO B FIRST**: it needs no ruling and clears ~150 of the failure. Then A as `PROMOTE.additions.manifest`, which is section 9.3's *active docs* half and is uncontested. Section 9.3's *engine source* half is NOT settled by any of this. `PROMOTION_MODEL_SEED_V1.md` gives the exclusion two reasons, and **exactly one of them died today**: (1) an overlay of development's *current file state* "would push half-finished engine source to `main`" -- DISSOLVED by `651ce911a`, since the overlay now publishes only git-tracked files and uncommitted experiments cannot travel; (2) "source must be coherent and build-tested... presets, `CMakeLists.txt` and the `src/` guards they depend on must travel together or the clone will not build" -- STANDS, because committed is not cold-clone-certified and an overlay produces no build proof. So 9.3 is a proposal against a still-valid safety boundary, and the real gap is that the SOURCE LANE ITSELF -- branch off `main`, apply the coherent changeset, cold-clone build, merge -- has not run since 2026-08-09. The same seed also PREDICTED the doc/source skew on 2026-07-18 ("the doc can reach `main` before the source it documents... always ground a published doc against what is actually on the publication target, proven by a cold clone, not against development"), and the 2026-09-12 promotion refreshed the manuals from development, which is the one thing that paragraph forbids. Checklist section 5 is partly done -- `__pycache__` is 0 -- but its "41 files" for the messaging/metadata lanes has GROWN to 80. GONE includes `BUILDING.md` under `docs/getting-started/` ON `main`, which is section 9.2's unreconciled path mismatch, now measured rather than asserted. **CITATION RESPELLED 2026-09-19, NOT SUPPRESSED, ON OI-010'S PRECEDENT.** `cited-paths` reported this path as MISSING the first time an unrelated edit pulled this file into scope. The gate is right by its own rule and misleading as to meaning: the path is in the GONE bucket, so **its absence from `development` is what this sentence measures.** Written as a filename and a directory rather than one token -- neither half matches the path scanner, both halves still read, and the branch is now named, which the original never did. Do not re-join it, and do not reach for the line-suppression marker: this line also cites tracked paths that must keep being audited, which is the trap OI-010 records. |
| OI-039 | 2026-09-16 | 2026-10-16 | decision | **Per-clone git state decides what the tooling can see, and none of it is in version control. Three instances are now known and two of them are already rows in this register.** (1) **OI-008** -- `status.showUntrackedFiles=no`, so a bare `git status` reports nothing for a file just created. (2) **OI-024** -- the gates are a per-clone hook install, so no fresh clone enforces them. (3) **NEW, 2026-09-16** -- `.git/info/exclude:12:content/portal/` in the sibling `D:\dev\x64base-site`. Measured: `git -C D:\dev\x64base-site ls-files -- content/portal/` returns EMPTY -- nothing in that directory is tracked, not `schemas.mdx`, not `overview.mdx` -- and `git check-ignore -v content/portal/schemas.mdx` names the rule. **THE THREE FAIL IN DIFFERENT DIRECTIONS, WHICH IS WHY NOBODY GROUPED THEM.** OI-008 makes real files invisible; OI-024 makes enforcement absent; `.git/info/exclude` makes a directory unstageable HERE and loose EVERYWHERE ELSE -- `git add` on it is a silent no-op (R42.1) on this machine, while a fresh clone has no such exclusion and one careless `git add -A` sweeps the same files in. `.gitignore` is versioned and shared; `.git/info/exclude` is neither. **HOW IT WAS FOUND, WHICH IS THE PART WORTH KEEPING.** Not by a sweep. `DOTTALKPP_MANUAL_ANCHOR_MAP_V1.md` cites 19 files in the site repo; a hand check confirmed all 19 EXIST and reported the citations sound. Existence is not tracking. The owner asked whether the cross-repo work was necessary at all, ruled `go gold`, and the resolver that produced (`tools/staging/check_cited_paths.py`, `9d8f54e73` and `45ac6cdd0`) contradicted the hand check on its first real run. `git status --short -- content/portal/` had returned empty, and EMPTY READS LIKE CLEAN, NOT LIKE EXCLUDED -- only `ls-files` and `check-ignore` separate the two. **WHAT THIS NEEDS: a ruling, not code.** Per-clone state cannot be versioned away; the question is whether the repo should MEASURE it. Candidate: one gate step that PRINTS the per-clone settings which change what the tooling sees -- `status.showUntrackedFiles`, `core.excludesFile`, the `.git/info/exclude` line count, the installed hook list -- so the state is visible in every gate run even though it can never be committed. **This proposes no change to anyone's configuration.** Closing OI-008 or OI-024 individually does NOT close this row; the pattern is that a fourth instance will be found the way the third was, by accident, while looking for something else. Full account: project doc `FINDING_PER_CLONE_GIT_STATE_IS_THE_SAME_BUG_THREE_TIMES_AND_A_HAND_SWEEP_CANNOT_SEE_IT.md`. |
| OI-040 | 2026-09-16 | 2026-10-07 | repo | **THE WEBSITE CONTENT MANIFEST DECLARES TWO PAGES THE MAINTAINER MARKED DO-NOT-PUBLISH, AND A HARD GATE CALLS THAT PASS.** Raised on closing OI-027, because it is the same defect one surface over and was written down as such on 2026-09-02 -- `docs/maintenance/lanes/full_stack_documentation/GATE_CORRECTIONS_REQUIRED_V1.md` section G3, *severity: high*, *"It is OI-027 in a second surface"* -- where it has sat 14 days with nothing nagging it, because a lane document is not a register. That is OI-037's complaint one rung down, and closing OI-027 without rowing this would have deleted the only pointer to it. **THE MECHANISM.** `tools/fullstack_docs/validate_website_content_manifest.py:70` classifies against `content_root.rglob("*.mdx")`. The file is 142 lines and contains the strings `git`, `tracked` and `ls-files` ZERO times, so a page that exists on the author's disk and NOWHERE ELSE classifies cleanly. Unchanged since `156fbea48` (2026-08-26), which PREDATES the G3 recommendation it was the subject of. **MEASURED 2026-09-16, AND THE TWO FILES ARE NOT THE ONES G3 NAMED.** G3's examples -- `content/lab/ai-portal-human-guide.mdx` and `content/lab/website-matrix-inspector.mdx` -- are both TRACKED today, healed by hand and not by the gate, which is exactly why the count did not stay at zero and why the instance is worth less than the instrument. Of the manifest's 152 declared pages, 152 exist on disk and **150 are tracked**. The two that are not are `portal/overview` and `portal/schemas`, both declared class `maintained`, and `publication_check` runs `content_inventory` as a **hard** gate proving *"all website MDX routes classified exactly once"* (`website_matrix_check.py:22` shells to this validator). It passes. **AND UNTRACKED HERE DOES NOT MEAN OVERLOOKED -- IT MEANS DELIBERATELY NOT PUBLISHED, RECORDED WHERE NOTHING CAN READ IT.** `git check-ignore -v` names `.git/info/exclude:12:content/portal/` in `D:\dev\x64base-site`, under a line-8 comment reading *"Maintainer-only very early alpha report preview. Do not publish."* So the tree holds TWO STATEMENTS ABOUT THE SAME TWO FILES: a TRACKED manifest declaring them publishable `maintained` routes and passing a hard gate, and an UNVERSIONED per-clone exclude saying do not publish. The second does not travel -- a fresh clone carries no such exclusion, and one careless mass add there sweeps both files in. **THAT IS OI-039 INSTANCE 3, THESE EXACT TWO FILES, FOUND FROM THE OTHER DIRECTION ON THE SAME DAY** and by a different route (a cited-paths resolver, not a manifest gate); neither row knew about the other, which is itself the evidence for OI-039's proposal. **WHAT IS OWED, and the halves are separable.** (1) THE ASSERTION, uncontested and prior-arted: every declared page must EXIST and be TRACKED, with untracked-but-declared reported as its own class because it is wrong whichever way it resolves. Copy `tools/staging/check_manual_link_integrity.py`, written for precisely this defect on the accepted manual, live and HARD since `f3dcfa6fb`. Report-only first, for the reason `check_house_style.py` learned the hard way. NOT FREE, and this is why it was not done in passing: the assertion must run `git ls-files` in the SITE tree while the validator lives in THIS one, so it needs a repo argument the tool does not take. (2) THE RULING, which the assertion forces and which is not the steward's: are `portal/overview` and `portal/schemas` publishable pages that should be tracked, or maintainer-only previews that should be OUT of the manifest? Today they are BOTH, and the contradiction is invisible because the only instrument that could see it reads the filesystem. |
| OI-041 | 2026-09-16 | 2026-10-07 | decision | **AN INTERACTIVE COMMAND IN THE WORKBENCH EATS THE SENTINEL THAT ENDS IT, AND THEN EATS THE CONVERSATION.** Raised from a maintainer transcript: *"some commands don't run and some have a significant delay if launched from the command line in appgui"*. **THOSE ARE ONE DEFECT SEEN FROM EITHER END.** `src/gui/core/gui_shell_runtime.cpp` keeps ONE `dottalkpp.exe` alive and marks the end of each command by appending a sentinel to the pipe (`:168`, `ECHO __DOTTALK_GUI_DONE_<n>__`), then waiting for it on stdout (`:179`, `wait_for(..., seconds(60))`). **SMARTBROWSER paints its first page and calls `std::getline(std::cin)` at `-- More --`, so THE PAGER READS THE SENTINEL AS A PAGER COMMAND**, does not recognise it, prints its hint and loops. The marker is never echoed, the GUI waits the full minute and reports `exit=-1`. **The delay is not variable -- it is SIXTY SECONDS by construction, and it is the timeout rather than the work.** **AND THE PAGER IS STILL RUNNING**, so the next command goes into the same pipe and is eaten too. From the transcript: `> SQLVER` answered by `(hint: OPEN CHILD/BACK/...)`. SQLVER never ran. That is the whole of *"some commands don't run"*, every later command costs another sixty seconds, and **the session does not recover without restarting the Workbench.** **SIX FILES IN `src/cli` READ `std::cin`. EXACTLY ONE DECLARES IT.** SMARTBROWSER carries `noargs: interactive` and `risk: interactive_prompt: yes` in its `@dottalk.usage` block; SIMPLEBROWSER, BROWSER, REBUILD and REINDEX carry nothing, plus the shared `dirty_prompt.cpp` helper. **The contract is not missing a field -- it HAS the field, one command sets it, and no consumer reads it**, least of all the caller that most needs to. **TWO SEVERITIES, AND THE SECOND IS MILDER THAN IT LOOKS, WHICH IS WORTH STATING SO IT IS NOT OVERSOLD.** (a) Interactive LOOPS (SMARTBROWSER, BROWSER, SIMPLEBROWSER) poison the session, above. (b) PROMPTING commands (REBUILD, REINDEX, dirty-buffer confirm) eat the sentinel AS THE ANSWER and then finish -- and both parsers default to NO (`dirty_prompt.cpp parse_yes_default_no()` returns false for empty and for anything but `y`/`yes`; `cmd_rebuild.cpp:140` tests `c == 'Y'`). **The sentinel begins with `E`, so it always answers NO**: the command refuses, completes, and the shell recovers. Cost is sixty seconds and a silent refusal read as a hang, NOT a spurious mutation. The narrow real hazard is a command beginning with `Y` typed while a prompt is pending. **THIS IS R146'S GRADING PROBLEM IN THE OTHER DIRECTION, FROM THE SAME ABSENCE.** Under a `.dts` stdin is at EOF, `getline` FAILS and the pager quits after one page (recorded in `dottalkpp/data/scripts/buffer_visibility_probe_v3_smartbrowser.dts`); under the GUI the pipe never closes, `getline` SUCCEEDS, and the pager consumes the driver's own traffic. Script mode loses information; GUI mode loses the session. Neither driver can ask *does this command need a console* and neither command can answer -- and the prompt half's suppression already exists unused (R131: `g_suppress_prompts`, `dirty_prompt.cpp:27`, set only on QUIT, never touched by DOTSCRIPT). **THE DECISION, AND IT IS WHY THIS IS `decision` AND NOT `repo`: WHICH OF THREE SHAPES.** (1) the bridge reads `interactive_prompt` and REFUSES what it cannot drive -- one place, no engine change, **but it covers only the commands that declare, today one of six**; (2) backfill the declaration on the other five -- contract-only, no behaviour change, and what makes (1) worth having; (3) the engine knows it has no console and an interactive command refuses up front -- the real fix, the largest, and the one that would ALSO have made a `.dts` say *SMARTBROWSER needs a console* instead of painting one page and letting the suite print PASS. **(1) alone would ship a guard narrower than it reads**, which is the AIF-079 shape. **NOT MEASURED:** whether sixty seconds is right for anything (a slow honest command and a wedged pager are indistinguishable to the bridge); whether a poisoned shell can be recovered without a restart by sending `Q` or restarting the child on timeout; and `src/tv`, not examined at all and possibly driving the engine the same way. Full account: `docs/maintenance/AIF120_INTERACTIVE_COMMANDS_AND_THE_GUI_SHELL_BRIDGE_V1.md`. **HALF RULED THE SAME DAY -- R147, `docs/maintenance/AIF120_DIALOG_OVER_REFUSAL_V1.md`.** Owner: *"yes they can ask questions - we will support modal"*, then *"dialog is to be desired"*. **A command that needs input from a person is not one a GUI must REFUSE; it is one the GUI ANSWERS, by asking.** So the BY-CIRCUMSTANCE half (REBUILD, REINDEX, the dirty-buffer confirm) is settled: the engine asks, the Workbench raises a modal, the answer goes back -- R144's `DISPATCH = host` capability route pointed one layer down at the engine's own prompts. **THE THREE FIX SHAPES ABOVE ARE SUPERSEDED FOR THAT HALF AND SHAPE (1) WAS WRONG ON ITS OWN TERMS:** this row argued a static `interactive_prompt: yes` is a MAY and not a WILL, so refusing on it would refuse REBUILD on every clean table -- true, and aimed at REFUSAL, which is no longer the answer. A driver that ANSWERS needs only to know what to do when a prompt arrives, so **a MAY is adequate**, and the declaration backfill (shape 2) becomes an OUTPUT of the ruling rather than a prerequisite. **STILL OPEN AND THIS ROW IS WHY IT NAGS: THE BY-CONSTRUCTION HALF.** SMARTBROWSER, BROWSER and SIMPLEBROWSER are a loop with state, not one question, and a modal per page turn is a prompt wearing a window. The direction is a real browser surface; the interim is refuse-and-name LABELLED INTERIM (R22.4, R143(b)). Undecided: whether that window is AIF-120's work or its own lane. **ALSO OWED, per R147 section 5:** the prompt event's spelling (text, message id, or both -- `helpdata_messages.cpp` carries these strings in it/es/fr/de, which argues for an id); the bridge side, including holding the 60s timeout while a modal is open; and `g_suppress_prompts`, NOT superseded, because a `.dts` running unattended still needs the documented default. Two drivers, two answers, one prompt event. **THIS ROW'S OWN COUNT WAS TOO SMALL AND IS CORRECTED, SAME DAY** (detail in R147 section 5a). It measured the files that READ `std::cin` -- six -- and named REBUILD and REINDEX as the prompting commands. **That is the set of DIRECT readers, not the prompt surface.** The dirty-buffer confirm is a SHARED HELPER and `dottalk::dirty::maybe_prompt_*` has **nine call sites in five files**: `USE` and `CLOSE` (`shell_commands.cpp:108,117`), `CLOSE` again three times (`cmd_close.cpp:243,249,259`), `WORKSPACE OPEN`, `WORKSPACE CLOSE` and `WORKSPACE LOAD` (`cmd_workspace.cpp:6055,6191,6407`), and `QUIT` (`shell.cpp:716`) -- plus REBUILD and REINDEX prompting directly. **EVERY SITE CANCELS ON A NO**, read at each rather than inferred. **So in the Workbench, with any dirty buffer, the NAVIGATION SURFACE OF THE LANGUAGE waits sixty seconds and then refuses:** `USE <table>` prints `USE canceled.` after a minute, and `USE` is the most-typed command there is. `QUIT` is on the list too, so the persistent shell cannot be shut down cleanly while a buffer is dirty. The row's *"sixty seconds and a silent refusal, NOT a spurious mutation"* stands and is still the right reassurance -- what it understated is WHICH COMMANDS. **AND THE REGRESSION SUITE ALREADY KNEW:** `cmd_regression.cpp:5755` engineers around it inside the TRIGGER VETO teardown -- *"this also clears the buffer, which is what stops CLOSE ALL raising the interactive COMMIT changes? (y/N) prompt that would hang the arm"* -- so one lane designed around the hazard in a test while the driver meeting it live called it a timeout. OI-024's method failure, second instance. **`src/tv` IS A THIRD DRIVER AND FAILS FOR A THIRD REASON:** it runs commands IN-PROCESS (`foxtalk_shell_bridge.cpp` calls `shell_execute_line`, no pipe, no sentinel, no timeout) and `foxtalk_redirect.cpp:90-97` redirects `std::cout` and `std::cerr` and **not `std::cin`** -- so in the TUI a prompt is VISIBLE in the output window and UNANSWERABLE, because the `getline` reads a console stdin TVision's event loop owns. **Three drivers, one absence**, and it argues FOR R147's structured event: under stdout scraping TV would need a separate implementation, since its stdout is a custom `streambuf` and not a pipe. |
| OI-042 | 2026-09-16 | 2026-10-24 | repo | **A RUNNING WORKBENCH MAKES `build.ps1 -WithWx` FAIL WITH TWO HUNDRED LINES THAT DO NOT NAME THE CAUSE.** Measured 2026-09-16. The compile and link SUCCEED -- `dottalk_wb.vcxproj -> build/src/gui/wx/Release/dottalk_wb.exe`, 1,499,648 bytes -- and the build then dies in the POST_BUILD staging step that copies the binary into `dottalkpp/bin` (`src/gui/wx/CMakeLists.txt:221-229`, the GUI path slot APPGUI launches from). **Windows holds an exclusive lock on a running image**, so `cmake -E copy_if_different` cannot overwrite it, and the same applies to the wx runtime DLLs beside it which the running process has loaded. **WHAT THE OPERATOR ACTUALLY SEES** is one readable line -- `Error copying file (if different) from ... to D:/code/ccode/dottalkpp/bin` -- buried above roughly two hundred lines of MSBuild echoing `setlocal`, `:cmEnd`, `exit /b %1` and a single `copy_if_different` invocation listing seventy-odd DLL paths, ending in `MSB3073 ... exited with code 1` and `CMake build failed with exit code 1`. **The exit code is indistinguishable from a real compile failure**, and the natural reading is that the GUI is broken. It is not; the new binary is sitting in the build tree and only the copy was refused. **THE COST IS NOT THE MINUTE, IT IS THE DIAGNOSIS** -- the session that hit this went looking for a code fault first. **WHAT IS OWED:** a preflight in `build.ps1` that, when the target list includes `dottalk_wb`, checks whether the staging target is locked and says so in one sentence before MSBuild is invoked. Cheap, no CMake change, and it turns a two-hundred-line failure into *close the Workbench and re-run*. **RELATED BUT SEPARATE:** `dottalk_wb` is built ONLY under `-WithWx` (`build.ps1:229`, opt-in by design for the heavier dependency), which is why a plain `.\build.ps1` leaves the Workbench untouched -- correct behaviour, and worth knowing beside this row because the two together are how a binary reached **23 days and eight GUI commits stale** while the tree it came from was being built daily. |
| OI-043 | 2026-09-19 | 2026-10-03 | decision | **A BUFFERED INSERT'S RESERVED RECNO IS PINNED BY AN INVARIANT NOTHING ENFORCES, AND TWO SITES SPEND IT AS AN ADDRESS.** `src/cli/table_state.cpp:556` states it -- "insert reserves its final recno while a table lock is held" -- and the lock does not pin it: `try_lock_table` is re-entrant for its own process (owner is `host:pid:ms`, `src/xbase/xbase_locks.cpp:41`), and the legacy bare INSERT verb (`src/cli/cmd_sql_insert.cpp:217`) takes no lock at all. `src/cli/sqlsel_statement.cpp:3859` makes the reservation; `src/cli/cmd_commit.cpp:365` and `src/cli/table_state.cpp:1015` each spend it guarded by `rn > recCount()` -- a BOUNDS check where a STALENESS check belongs. MEASURED 2026-09-19 on build c3cfa4c7, both sites, every arm: `dottalkpp/data/scripts/sql_insert_intervening_append_probe.dts` (ARMs B/D/E) and `dottalkpp/data/scripts/jrn_replay_probe.ps1` (JRN2/JRN3/JRN4). A live record is overwritten, a committed row is dropped, and a partial field list produces a BLENDED row that is neither the old one nor the new one -- all silently, all reported as success. NEEDS A RULING on which repair: always-append (recno becomes a buffer key, record identity minted at COMMIT), refuse-on-stale, or both -- and whether to close the unlocked door by routing `cmd_sql_insert.cpp` through the locking `src/cli/append_support.cpp` already uses. Findings are in the Claude project as FINDING_A_BUFFERED_SQL_INSERT_EATS_ANY_RECORD_APPENDED_BEFORE_ITS_COMMIT and FINDING_THE_RECNO_RESERVATION_INVARIANT_IS_WRITTEN_DOWN_UNENFORCED_AND_HAS_THREE_DEPENDENTS. **REFRAMED THE SAME DAY BY OWNER INPUT, AND THE ITEM ABOVE ASKS THE QUESTION ONE LEVEL TOO LOW.** Owner 2026-09-19: *"only a delete/pack / append/insert changes the record count, there should only be one common function called for increasing the record count and one for decreasing the record count"*, with the scenario that settles it -- reccount 200, student A buffers ten inserts reserving 201-210, student B commits five, and *"if student A commits the record number sequence is complete, if not there is a gap"*. **THE GAP IS THE PROOF.** A DBF record's identity IS its physical position -- `src/xbase/dbf_file.cpp:320` `gotoRec64` is arithmetic plus a bounds test, with no directory, no free list and no tombstone index -- so record 211 cannot exist unless 201-210 physically exist. A gap is representable in an allocator and NOT representable in the file, which means **the reservation can never be authoritative** and no guard at COMMIT can make it so. Only three shapes are self-consistent: (A) reserve nothing, identity minted at COMMIT, and nobody learns a recno before it commits; (B) reserve by MATERIALIZING the blank rows now, which keeps the read-your-own-writes promise and pays in rollback debris plus pre-commit visibility; (C) reserve logically and enforce it, which in a positional file collapses into (B) or into blocking. The engine implements (C) WITH NO ALLOCATOR. **MEASURED 2026-09-19, and it is better news than the ask implies: the single common function already exists on the grow side.** `++_rec_count64` occurs EXACTLY ONCE in the tree, `src/xbase/dbf_file.cpp:421` inside `DbArea::appendBlank()`, and nothing else grows a table -- the public setters in `include/xbase.hpp` have no production caller at all. What is missing is a GATEKEEPER: **37 call sites in 17 files** reach the primitive directly, it takes no lock, and it records no allocation; only `src/cli/append_support.cpp` locks first, while `src/cli/cmd_sql_insert.cpp` and `src/cli/table_state.cpp` -- the two sites this item is about -- do not. **AND THE DECREASE SIDE ALREADY HAS THE DISCIPLINE BEING ASKED FOR:** `src/cli/cmd_pack.cpp`, `src/cli/cmd_zap.cpp` and `src/cli/cmd_turbo_pack.cpp` never decrement a live count -- they rebuild the file, swap it in and force a reopen. The asymmetry is the finding. SO THIS ITEM IS NOW: rule (A) or (B); put one gatekeeper over `appendBlank` that takes the table lock and returns the recno it actually assigned; and prove nobody goes around it, which is OI-045. Option (5) of the original framing -- the unlocked legacy INSERT door -- closes as a side effect of the funnel rather than as its own change. THE PRECEDENT IS IN THIS TREE: AIF-156 met the same shape for field writes and its recorded answer was to CONSOLIDATE THE ROUTE rather than mint arms -- one funnel (`xbase::cli::replaceFieldStored()`) plus a static gate (`tools/staging/check_field_write_callers.py`) proving nobody goes around it. Detail in the Claude project as OI043_OWNER_INPUT_THE_RESERVATION_CANNOT_BE_AUTHORITATIVE_AND_THE_COUNT_NEEDS_ONE_GATEKEEPER. **RULED (A) AND BUILT 2026-09-19, AND THIS ROW NOW NARROWS TO THE PART THAT DID NOT SHIP.** Owner: *"go a on oi-43"*. An INSERT now ALWAYS APPENDS and the record number is minted at COMMIT, in both spenders -- `src/cli/cmd_commit.cpp` `apply_one_recno` and `src/cli/table_state.cpp` `recover_table_buffer_journal`. The staged number is a buffer key and nothing may read it as an address; both contracts say so. ONE IMPLEMENTATION DETAIL WORTH KEEPING: `apply_one_recno` had a single `rn` doing two jobs, harmless only because the staged and real numbers could never differ on the append path. They can now, so it is split -- `key` from the buffer, `rn` from `A.recno64()` after the append -- and the record lock, the index snapshot pair and the AFTER trigger's `ev.recno` all take `rn`. One name would have put index keys and trigger events on the wrong record, silently. VERIFIED on a clean build (exit 0), every arm flipped to its SAFE branch: ARM D count 5 with the blank intact, ARM E count 5 with both blanks intact, JRN2 keeps `L3/LIVE3` and appends at 5, JRN3 places its row at 2 instead of dropping it, JRN4 lands a NEW row carrying only the field the journal set -- so the blended row that looked like valid data is gone, replaced by an obviously incomplete one. `MCC_CORE_SURFACE_AND_INDEX_REGRESSION_V9` shows no FAIL marker and `Recs: 200` throughout. **WHAT REMAINS, AND IT IS THE OWNER'S ORIGINAL ASK RATHER THAN THE DEFECT:** there is still NO GATEKEEPER over `appendBlank` -- 37 sites reach the raw primitive, it takes no lock and records no allocation, and `src/cli/cmd_sql_insert.cpp` still grows a fenced table from another process with nothing in the way. That is no longer a COMMIT-correctness hazard, because (A) leaves no reservation to invalidate; it is a locking gap. Related and also still open: the legacy bare INSERT verb ignores `TABLE ON` entirely -- measured from the DBF header 2026-09-19, `B0/BAREINS` survives a ROLLBACK and no journal is ever written. The caller gate `tools/staging/check_append_callers.py` is wired into the pre-push gate and will report any 38th door while this is pending. **THE LOCKING GAP IS MEASURED AND CLOSED, 2026-09-19. WHAT IS LEFT IS THE 34-DOOR CONVERSION, AND IT IS NO LONGER URGENT.** The remaining half had been read from source and never measured, so it was measured first: a FOREIGN, LIVE table lock was planted beside a table and five doors tried against it. `dottalkpp/data/scripts/foreign_lock_probe.ps1` -- legitimate in one process because the fence is a lock FILE whose owner is `host:pid:ms` and `create_or_validate_owned` removes one only when its pid is PROVABLY dead (AIF-116 fails closed), so a hand-written lock over a live foreign pid IS a second engine. RESULT: `APPEND BLANK`, `SQLSEL INSERT` and `REPLACE` refused by name; **the legacy bare `INSERT` inserted a row and `IMPORT` appended one** -- count 2 -> 4 on a table another engine held. A FAMILY, not a verb, which is what decided funnel over patch. **BUILT:** `include/cli/append_fence.hpp` -- `cli::fence::TableFence` plus `append_fenced()`, the one gatekeeper: take the fence, append, return the recno THE FILE assigned. It is `cmd_commit.cpp`'s `InsertTableLockGuard` MOVED, not copied, and that file now aliases the shared name. BORROWED-AWARE is the whole subtlety -- `try_lock_table` is re-entrant for its own process by design, so a guard that unconditionally unlocks would TEAR DOWN AN ENCLOSING HOLDER'S FENCE, which is worse than never taking one; that same re-entrancy is what lets `IMPORT` hold ONE fence for the whole file while each row borrows it. RE-MEASURED after the build: both doors now print `table locked (lock exists)` and the count stays 2, the unfenced path is unchanged (ARM E still 5), regression clean. **THE GATE FOUND A GAP IN ITSELF AGAIN:** it scanned `src/` only, so the moment the funnel was written as a header the one call that matters most would have been invisible to the instrument measuring it. It now scans `include/` too and exempts the funnel by name, which is the model's posture toward `src/cli/xbase_cli_write.cpp`. 37 -> 34 sites in 15 files, three FIXED, zero NEW, baseline refrozen. **WHAT REMAINS:** 34 doors still reach the raw primitive. None is known to be reachable on a user's open table -- `COPY`/`SORT` write fresh outputs, the rest are internal stores -- and `cmd_IMPORTSQL` and `cmd_AUTODBF` are NAMED AS UNMEASURED rather than assumed safe. The gate ratchets against a 35th while this waits. **THE COMMAND SURFACE IS NOW CLOSED AND THE BACKLOG IS 26 IN 9 FILES, ALL INTERNAL STORES. 2026-09-19.** A THIRD door was measured before anything else was routed: `IMPORTSQL FILE <f> TO <t>` reported `IMPORT: OK / Rows imported: 1` against a planted foreign lock and left its row on disk, while L1/L3/L4 were refused by name and the two doors fixed earlier stayed refused. `run_file_import` takes the area threaded down from `cmd_IMPORTSQL`, and `current_area_matches_target()` exists precisely so it can append into the table the session already has open. Routed, one fence for the whole file, same shape as IMPORT. **THAT IS ALL THREE USER-REACHABLE GROWERS: bare `INSERT`, `IMPORT`, `IMPORTSQL FILE`.** **EIGHT SITES EXEMPTED BY FILE WITH THE REASON, never by dropping lines into the baseline -- a baseline of things that are FINE is how a measurement stops meaning anything.** `src/cli/append_support.cpp` is THE GENERATOR (it holds the fence across primary-key generation, so routing would nest a fence in its own); `src/cli/cmd_copy.cpp`, `src/cli/cmd_sort.cpp`, `src/cli/cmd_ddl.cpp` and `src/xbase/fields_mgr.cpp` all append into a destination THEY JUST CREATED AND OPENED in the same function -- verified by reading where each receiver comes from, not inferred from the command name -- so there is no concurrent holder to fence against. 37 -> 34 -> **26 in 9 files**, zero NEW at every step. **WHAT REMAINS IS A DIFFERENT QUESTION AND SHOULD NOT BE CONFUSED WITH THIS ONE:** the 26 are internal stores -- `identity_dbf_store.cpp` (10), `bbs_store.cpp` (4), `hierarchy_service.cpp` (3), `group_log.cpp` (2), `cmd_workspace.cpp` (2), `message_catalog.cpp` (2), `table_state.cpp`, `cmd_commit.cpp`, `cmd_autodbf.cpp`. No USER command reaches them on an open table; the exposure is two dottalkpp processes both writing the same engine store, and the right fence for each is at its OWN save boundary rather than per row. `src/help/message_catalog.cpp` IS DELIBERATELY UNTOUCHED -- the help DBFs belong to a concurrent session. `cmd_commit.cpp` already holds the fence via `insert_lock`. The gate ratchets against a 27th meanwhile. **26 -> 2, 2026-09-19. EVERYTHING BUT THE HANDS-OFF FILE IS ROUTED.** Each store got its fence at ITS OWN SAVE BOUNDARY rather than per row, which is why 24 sites cost eight edits. `identity_dbf_store.cpp`: ONE fence in the `append` lambda that IS the save boundary, and all ten appends inside the emit() callbacks borrow it. `group_log.cpp`: a FRESH fence on the group catalog, which is shared across processes by design -- the member tables' fences are on different files. `hierarchy_service.cpp` (3), `cmd_autodbf.cpp`, `table_state.cpp` (recovery), `cmd_commit.cpp` (borrows the fence `commit_area` already holds; routed anyway so the gate sees ONE route rather than a route plus an exception nobody re-checks). **AND TWO MORE HAND-WRITTEN COPIES OF THE LOCK PROTOCOL TURNED UP AND WERE REBUILT ON THE SHARED FENCE:** `TableLock` in `src/bbs/bbs_store.cpp` and `WsLock` in `src/cli/cmd_workspace.cpp`, each keeping its own constructor signature so no caller changed. Both were ALREADY CORRECT in the sense that their appends sat inside them -- bbs's four have been lock-guarded since it shipped -- **but both carried the same latent bug**: `held = try_lock_table(...)` is TRUE when this process already held the lock, because the lock is re-entrant for its own owner by design, so the destructor would RELEASE A FENCE ITS SCOPE NEVER TOOK. Nothing nests them today, so it never fired. Three copies of one protocol is the shape that ends with two of them disagreeing, and that is now one definition. **WHAT IS LEFT IS TWO SITES IN `src/help/message_catalog.cpp` AND THEY ARE DELIBERATELY UNTOUCHED** -- `apply_seed_message_rows` and `apply_seed_text_rows`, both writing the help store, which belongs to a CONCURRENT SESSION by owner instruction. They stay in the baseline as the named remainder rather than being exempted, so the gate keeps counting them. Routing them is two lines and needs an explicit go. **THE WHOLE-LOOP RECOVERY FENCE WAS PROPOSED, MEASURED AND DECLINED, 2026-09-19.** Owner: *"we could lock the table for the transaction, so multiple sessions don't clobber reccounts"* -- which COMMIT ALREADY DOES, and has all along: `src/cli/cmd_commit.cpp` gates on `has_insert` and emplaces `ctx.insert_lock` for the whole transaction, commented "released LAST" so it outlives the apply loop. The lock's SCOPE was never the bug. The NUMBER was chosen outside it, and rule (A) is what moved the minting inside. The one real gap the idea points at is RECOVERY: `recover_table_buffer_journal` fences each append individually, so a multi-row replay is not held under one lock. Rather than build that fence on an argument it was MEASURED -- `dottalkpp/data/scripts/recovery_under_foreign_lock.ps1` plants a TWO-RECORD journal beside `JRN5` together with a foreign LIVE lock, the same one-process technique as the six-door probe. RESULT: with the table HELD, `USE` opened it, applied NOTHING, left the count at the two seeds and KEPT THE LOG (82 bytes) -- OI-044's keep-the-log path firing against a genuine refusal rather than a corrupt file. The lock was released and the log was still present. The next `USE` printed `USE: recovered a committed table-buffer journal (.tbj).`, reached `Record count 4` with `R1/RECOVER1` and `R2/RECOVER2` in order, and removed the log. **A CONTENDED RECOVERY IS A DEFERRAL, NOT A PARTIAL WRITE.** Per-append fencing is therefore sufficient for CORRECTNESS; a whole-loop fence would buy CONTIGUITY and nothing else, and no reader requires a journal's rows to be adjacent -- replay stopped treating journal recnos as addresses when (A) shipped. NOT BUILT. The measurement is recorded at the fence site in `src/cli/table_state.cpp` so the next reader does not re-litigate it from the argument. **THE LEGACY BARE INSERT NOW HONOURS TABLE ON, 2026-09-20 -- the last verb that did not.** This row recorded it as related-and-still-open: the bare verb ignored `TABLE ON` entirely, so an INSERT inside a buffered session was durable before COMMIT ran and ROLLBACK did not undo it. **THE PRECEDENT WAS EXACT AND ALREADY IN THE TREE:** `src/cli/cmd_replace_multi.cpp` carries AIF-151's note in the same words -- *MULTIREP used to ignore TABLE ON entirely and lock-and-write regardless* -- so this was the same defect in a second verb and took the same repair. **AND SO WAS THE MEASUREMENT.** ARM 0 of `dottalkpp/data/scripts/sql_insert_intervening_append_probe.dts` already asked this exact question, in NATIVE mode on purpose because `SET MODE SQL` aliases INSERT to SQLSEL and a probe in SQL mode measures the wrong door. That arm now asserts the REPAIR rather than the defect, in three parts -- 0a stages and ROLLBACKs, 0b stages BOTH SYNTAX FORMS and COMMITs, 0c proves TABLE OFF is unchanged. **0b EXISTS BECAUSE 0a IS ONLY HALF THE CLAIM:** a verb that staged and then lost the row at COMMIT would pass 0a and be worse than the defect it replaced. **OWNER RULING 2026-09-20: NO FENCE ON THE BUFFERED PATH.** A staged row touches no file, COMMIT's own contract says *TABLE ON buffers changes; no OS locking should occur*, and MULTIREP's buffered branch is taken BEFORE its record lock for that reason. The visible consequence is stated in the contract rather than discovered: with TABLE ON a foreign lock no longer refuses this verb at the statement, the refusal moves to COMMIT, and with TABLE OFF -- which is what the foreign-lock probe measures -- nothing changes. **ONE CONSOLIDATION RODE ALONG AND IT IS THE POINT OF THE CHANGE, NOT A TIDY-UP.** The rule that mints a staged insert's BUFFER KEY -- start past the last physical record, then past the highest key already staged -- lived inline in `execute_insert_statement` and nowhere else. A second buffered inserter would have made two copies of the rule that decides record identity under ruling (A), which is the shape this tree consolidated three times the day before. It is now `dottalk::table::next_insert_key` in `include/cli/table_state.hpp`, called by both. **ALSO CONSOLIDATED:** `src/cli/cmd_sql_insert.cpp` had the append-and-write sequence written out TWICE, once per syntax form; adding a buffered branch to each would have made four paths where there should be one, so the fork lives in one function and ARM 0b exercises both forms to prove it. **NOT REUSED, AND THE REASON MATTERS:** SQLSEL's `stage_dml_rows` refuses any table not enlisted in an SQL transaction, so TABLE ON and SQL-transaction enlistment are TWO BUFFERING REGIMES rather than one funnel with two doors. Merging them is a larger question nobody has asked. **NAMED AS UNMEASURED RATHER THAN ASSUMED SAFE:** memo. MULTIREP had to write memo payloads immediately and buffer only the handle; this verb has never had memo-specific handling at all, so staging changes nothing about where a payload lands, but what `A.set()` does to a memo field here is measured by nothing. |
