---
title: "Full-stack Documentation Push"
description: "Complete maintained record of the current source-to-comments-to-HELP-to-reference-to-manual-to-website documentation vertical."
---

> **Current as of 2026-08-26 -- `maintained_current` -- v6 development closeout and local website reconciliation.**
> `DOCFLUSH-20260825-001` is closed in the development tree and review-needed.
> E0-E7 are proven; E8 remains the distinct owner gate for manual acceptance,
> source staging, and website publication. No public mutation is claimed. Earlier
> runs remain preserved below with their original event dates.

## Why this vertical exists

The documentation system derives from source behavior, source comments, usage
contracts, and file metadata. A major messaging and runtime change therefore requires
a governed reharvest: not because every small change deserves a full publication run,
but because this change crossed enough source, HELP, manual, and public-documentation
boundaries to justify the complete vertical.

The chain is:

```text
source and *ref.hpp contracts
-> COMMENTS SRC* evidence and mutation audit
-> CMDHELP build + CMDHELPCHK + locale canaries
-> SelfDoc / reference identity / lineage
-> manualgen candidate + accepted-layer preservation
-> assembled Markdown, HTML, and PDF
-> reviewed website pages and machine-readable feeds
-> human review
-> explicit commit, publication, and live-readback gates
```

## Current proof table

| Layer | Reviewed current state | Boundary |
| --- | --- | --- |
| Source harvest | 1,082 tracked source files with universal file-contract coverage; 231 usage-bearing files | Source defines; contracts describe. |
| COMMENTS | Guarded reload with rollback set and independent readback | Snapshot/audit evidence; not permission to mutate source. |
| HELP | 462 legacy command rows; 2,614 argument rows; 670 reachable topics; 29,480 lines | `CMDHELPCHK`, direct table join, and topic-set checks pass. |
| Canonical harvest | 14/14 tables current; 63,217 rows | Guarded apply, readback, retained backup, and rollback controls. |
| Website catalogs | 239/239 command rows with zero fallback; 73 core plus 2 extension function rows | Derived from current source; count-bound diagram regenerated. |
| Accepted reader | 4,118 lines; 237 headings; publication readiness 26/26 | Accepted reader remains pinned and unchanged. |
| Manual candidate | `MANRUN-20260826T012054Z-B9F8B8BD`; 25 sections, 19 media items, 13 appendices | Accepted as review-needed; no publication replacement and no canonical-manual ruling. |
| Website | Matrix delta reconciled for unblocked rows; production build generated 171 static pages and indexed 164 | Local build/review surface only; current-work feed deferred because its registry contains unrelated uncommitted work. |

> **2026-07-28 update — manualgen command-reference lane advanced.** Past this
> 07-23 snapshot, a manualgen gate-4 apply (`MANRUN-20260728T041930Z-5DE4733C`,
> harvest `HELPMETA-20260728T003402Z`) accepted the five FoxPro string functions
> `STUFF`, `PADL`, `PADR`, `PADC`, and `PROPER` into the dev-tree command
> reference: 164 reader-linked pages, 10,502 lineage rows, SYSCMD 212 / SYSFUNC
> 74, publication-readiness `PASS`. This is a distinct manualgen assembly from
> the 191-page candidate above (do not compare page counts), and it is not
> source-promoted or deployed. See the
> [Command Reference](/docs/dottalk/command-reference) manualgen lane.

## Coverage repairs made during review

The first local website candidate was too scant. A source-to-reference reconciliation
found and repaired eight post-baseline command pages: `BUILDVECTORS`, `DEFCMD`,
`DEFFN`, `STOP_ON_ERROR`, `UNDEFCMD`, `UNDEFFN`, `USER`, and `VDISK`.
The current-source blocking count is zero; 102 inherited historical gaps remain visible
as backlog rather than being relabeled as new regressions.

Two large implementation families also received dedicated documentation:

- [RAM DBF and VDISK](/docs/engine/ram-dbf-vdisk): process-local X64 DBF tables,
  CDX-V64 indexes, path routing, and destructive lifetime boundaries.
- [Identity, Authentication, and RBAC](/docs/engine/identity-security):
  `USERS` identity records, `TEAM_MEMBER` participation, authentication sessions,
  roles, permissions, authorization grants, DBF persistence, owner-gated management,
  runtime enforcement, and the diagnostic-only legacy `SECURITY` selector.

## Human-viewable products

- [Current Tasks & Projects](/docs/labtalk/current-work) — what is active, owner,
  evidence state, Pseudo-Chat inbox, and next gate.
- [Documentation Progress](/docs/dev/documentation-progress) — published checkpoint
  plus current vertical without conflating their dates.
- [Developer Manual](/docs/dev/developer-manual) — assembled current manual.
- [Command Reference](/docs/dottalk/command-reference) — accepted and current page
  layers with lineage.
- [Website Documentation Matrix](/docs/dev/website-documentation-matrix) — content
  maintenance classes, feed authorities, and proof gates.
- [Pinocchio Benchmarks](/docs/engine/pinocchio-benchmarks) — historical before/after
  evidence and machine identity limitations.

Machine-readable companions:

- [current-work-v1.json](/artifacts/current-work-v1.json)
- [documentation-progress-v1.json](/artifacts/documentation-progress-v1.json)

## Date and maintenance contract

This page, Current Tasks & Projects, Documentation Progress, project truth, roadmap,
manual landing, and AI Agent Sync are permanent but not static. They use the
`maintained_current` class:

1. Display a reconciled `as_of_date`.
2. Name the authority that feeds the present state.
3. Show proof state and next gate.
4. Replace the present-state region when work advances.
5. Preserve historical event dates and immutable closeout evidence.

Reported products such as benchmark rows and sealed publication checkpoints remain
append-only. Static brand/history pages change only by deliberate human review.

## Additional maintained steps (added in pass 4)

Pass 4 added standing steps to the vertical; **Document FullStack push v5** runs them
by default:

1. **Website matrix gate.** Read and classify against the
   [Website Documentation Matrix](/docs/dev/website-documentation-matrix) at the start
   of the website phase; update and re-audit it at closeout. A stale matrix is a
   fail-closed condition on the website signoff.
2. **Conceptual diagram collection.** The BBS/AI process diagrams and the
   start-to-finish documentation-push DFD are maintained from source `.mmd`;
   regenerate the SVGs from source and never hand-edit a rendered SVG.
3. **AutoSSL trigger.** When the certificate validates, flip the artifact/manual-room
   links to HTTPS and drop the "while SSL is settling" caveats.
4. **Count reconciliation (AIF-032).** Reconcile the command count in source and
   regenerate the data-carrying harvest diagram and command catalog; do not hand-type
   harvest-derived numbers.
5. **Interchange coverage.** New import/export surfaces (such as `AUTODBF`, CSV-to-DBF)
   land in the DotScript CSV/DBF import-export section with a proof label.

## Separate missions and open boundaries

- `METACOLLECT-238` is a separate 238-finding metadata mission. It was not silently
  folded into this documentation flush.
- The Pseudo-Chat palette-stub return is held for correction and is not implemented.
- A local green website does not equal a commit, GitHub push, saved deployment,
  production deployment, or public live-readback.
- The next gate is human inspection of the complete local website followed by an
  explicit publication decision.
