---
title: "Runtime Evidence Gallery"
description: "Screenshot evidence for DotTalk++ Workbench, Arctic TUI, LabTalk Portal, workspaces, table browsing, tuple output, and SelfDoc-facing manual/website proof."
---

This page records visual evidence from the local runtime. These images are evidence artifacts for the manual and website, not product screenshots claiming a finished release.

Evidence date: July 3, 2026.

Screenshot media is curated like the rest of the documentation system: stable filename, evidence ID, source capture, caption, interpretation boundary, and review-before-promotion. Duplicate captures are not repeated unless they show a distinct runtime state.

## DotTalk++ Workbench Workspace Areas

![DotTalk++ Workbench workspace areas showing 12 loaded DBF areas and active TEACHERS.DBF](/images/evidence/arctictalk-workspace-areas-12-relations-evidence-20260703.jpg)

Observed evidence:

- DotTalk++ Workbench GUI evidence is running against `TEACHERS.DBF`. The captured build/window may still show the older Arctic code-name text; the product-family name is DotTalk++ Workbench.
- Workspace pane lists 12 DBF areas: `BUILDING.DBF`, `CLASSES.DBF`, `COURSES.DBF`, `DEPT.DBF`, `ENROLL.DBF`, `MAJORS.DBF`, `ROOMS.DBF`, `STUD_MAJ.DBF`, `STUDENTS.DBF`, `TASSIGN.DBF`, `TEACHERS.DBF`, and `TERMS.DBF`.
- Runtime output reports `WORKSPACE LOAD: restored 12 area(s) and 15 relation(s)`.
- GUI mirror reports 12 GUI areas created from the `mcc_x64.dtschema` workspace.

## Table Browser and CDX/LMDB Evidence

![DotTalk++ Workbench TEACHERS.DBF browse grid with CDX/LMDB order status](/images/evidence/arctictalk-teachers-browse-cdx-evidence-20260703.jpg)

Observed evidence:

- Browse grid displays `TEACHERS.DBF` fields including `TID`, `LNAME`, `FNAME`, `DEPT_ID`, `EMAIL`, `HIRE_DATE`, and `TENURED`.
- Status bar reports 20 rows shown of 20.
- Status bar reports active ordered state: `Order: ASC tag TID`.
- Output pane shows `SET ORDER` CDX metadata mismatch diagnostics with a `.cdx` container and `.cdx.d` LMDB environment path. This is useful evidence for the CDX/LMDB diagnostics lane and should be documented as a diagnostic state, not a clean success state.

## Command Output Readback

![DotTalk++ Workbench workspace table view with command output readback](/images/evidence/arctictalk-workspace-command-output-evidence-20260703.jpg)

Observed evidence:

- DotTalk++ Workbench can show table/workspace state and command-output readback in the same GUI.
- The output pane preserves diagnostic text for later manual or SelfDoc review.
- This supports the policy that GUI evidence should cite visible runtime text, not only visual layout.

## LabTalk Portal

![LabTalk Portal docs index and README preview](/images/evidence/labtalk-portal-docs-index-evidence-20260703.jpg)

Observed evidence:

- LabTalk Portal has a section navigator with `Docs`, `Apps`, `Labs`, `Concepts`, `Proofs`, `Diagrams`, `Runtime`, and `Launchers`.
- The Docs section shows items including `LabTalk README`, `Campus Architecture`, `Education Map`, `Portal Concept`, and `Database Literacy Starter Lab`.
- The preview pane resolves a document record with ID, section, kind, and local path.

## Arctic TUI Tuple and Record Browser

![Arctic terminal TUI showing tuple grid and record popup](/images/evidence/arctictalk-tui-tuple-grid-record-evidence-20260703.jpg)

Observed evidence:

- Terminal/TUI shell shows the Arctic code-name surface with menus and command/output panes.
- Tuple grid displays joined fields such as `STUDENTS.LNAME`, `STUDENTS.FNAME`, `enroll.CLS_ID`, `GRADE`, `classes.CLS_ID`, and `tassign.CLS_ID`.
- A record popup displays `BLDG` and `NAME` for a related record.
- This supports the SmartBrowser/TupTalk/RelTalk evidence lane: tuple output, relation readback, and record inspection are visible in the runtime.

## DotTalk++ CLI Startup and BibleTalk Evidence

![DotTalk++ CLI startup showing build, SQLite availability, EDU_BIBLETALK, and command prompt hints](/images/evidence/dottalk-cli-startup-bibletalk-evidence-20260703.jpg)

Observed evidence:

- DotTalk++ CLI starts with the `dottalk++ beta-0` banner and DotTalk++ build timestamp.
- Runtime reports SQLite availability and version `3.50.4`.
- `EDU_BIBLETALK` opens the SQLite seed `biblebase\bible_kjv_x64.sqlite`.
- Startup/readback shows education-facing CLI output, including the ASCII display, BibleTalk quote, `HELP`, `USE`, `SELECT <n>`, `AREA`, `COLOR`, `ABOUT`, and `QUIT` prompt text.
- Developer/status hints include `CMDHELPCHK`, `GPS`, `WORKSPACE`, and `ERSATZ`.

## DotTalk++ CLI Workspace and Relation Load

![DotTalk++ CLI workspace load showing ERSATZ, SETPATH, REL lines, and 12 areas with 15 relations restored](/images/evidence/dottalk-cli-workspace-relation-load-evidence-20260703.jpg)

Observed evidence:

- DotTalk++ CLI runs `ersatz load`.
- `SETPATH` readback shows x64 `DBF`, `INDEXES`, and `LMDB` roots.
- Workspace close/reload output is visible before relation restoration.
- `REL:` lines enumerate table relationships such as `STUDENTS -> ENROLL ON SID`, `CLASSES -> TEACHERS ON TID`, and `ROOMS -> BUILDING ON BLDG`.
- Runtime reports `WORKSPACE LOAD: restored 12 area(s) and 15 relation(s)`.
- `ERSATZ` loads `default.erz` with `SETUP=x64`, `WORKSPACE=mcc_x64.dtschema`, and the path `enroll -> classes -> tassign -> teachers`.

## DotTalk++ CLI Relation Tree and Tuple Grid

![DotTalk++ CLI relation tree and tuple grid showing current root record, descendant summary, and joined tuple rows](/images/evidence/dottalk-cli-relation-tree-tuple-grid-evidence-20260703.jpg)

Observed evidence:

- DotTalk++ CLI shows the current root record with fields including `SID`, `LNAME`, `FNAME`, `DOB`, `GENDER`, `MAJOR`, `ENROLL_D`, `GPA`, and `EMAIL`.
- Relation tree output expands from `STUDENTS` through `ENROLL`, `CLASSES`, `COURSES`, `DEPT`, `TERMS`, `ROOMS`, `BUILDING`, `TEACHERS`, `TASSIGN`, `STUD_MAJ`, and `MAJORS`.
- Descendant summary and tuple grid are visible in the CLI, separate from the TUI surface.
- Tuple grid includes joined fields such as `STUDENTS.LNAME`, `STUDENTS.FNAME`, `enroll.CLS_ID`, `GRADE`, `classes.CLS_ID`, `tassign.CLS_ID`, `TID`, and `teachers.LNAME`.

## DotTalk++ Workbench Preview

![DotTalk++ Workbench Preview showing empty workspace graph and bootstrap service notes](/images/evidence/dottalk-workbench-preview-empty-workspace-evidence-20260703.jpg)

Observed evidence:

- DotTalk++ Workbench Preview opens with menu groups for `File`, `Workspace`, `Run`, `Table`, `Record`, `Index`, `Lists`, `Browse`, `Rel`, `Tuple`, `Cmd`, `Language`, and `Help`.
- Workspace Graph tab reports no open areas, no active area, no indexes, and no relations.
- Bootstrap notes identify browsers/lists workspace graph service pending, ERSATZ runtime output available, visual presets pending, and DTSchema load/save bootstrap menu active.
- This is preview/bootstrap evidence for the Workbench surface, not a loaded workspace proof.

## Website Documentation Evidence

![x64base website FAQ page showing live documentation navigation and DBF format links](/images/evidence/x64base-website-faq-live-evidence-20260703.jpg)

Observed evidence:

- The live site is open at `x64base.com/docs/getting-started/faq/`.
- Navigation shows `About`, `Products`, `Documentation`, `Licensing`, `Brand`, `News`, and `Contact`.
- Documentation sidebar includes `DBF Flavors and Index Defaults`, `DBF_64 Specification`, `FPT64 Memo Format`, and `Indexing Rules`.
- FAQ copy refers to x64base as a lineage evolution and points to `DBF_64` and `FPT64` documentation.
- This is website matriculation evidence. It proves publication/navigation state, not independent engine behavior.

## Manual Use

For manual generation, these screenshots should be cited as reviewed media evidence. They support sections on:

- DotTalk++ Workbench GUI surfaces.
- Arctic TUI code-name surface.
- Workspace/area restoration.
- Table browsing and SmartList/browser lanes.
- CDX/LMDB order diagnostics.
- LabTalk Portal and documentation index surfaces.
- Tuple/relationship browsing.
- DotTalk++ CLI startup, education hooks, workspace/relation loading, relation tree output, and tuple-grid readback.
- DotTalk++ Workbench Preview bootstrap state.
- Live x64base website documentation navigation and media-curation workflow.

They should not be used to claim final product polish, completed release packaging, or absence of known diagnostics.
