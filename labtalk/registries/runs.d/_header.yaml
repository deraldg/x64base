# Header block for ai_runs.yaml -- static vocabulary/schema, not a record.
runs_registry:
  schema: ai-runs-v1
  status: candidate
  contract: docs/maintenance/AI_RUN_TRACEABILITY_CONTRACT_V1.md
  required_fields:
  - run_id
  - member
  - project
  - lanes
  - handle_binding
  - status
  handle_bindings:
  - SELF_REPORTED
  - MAINTAINER_ATTESTED
  - NOT_RESOLVABLE
