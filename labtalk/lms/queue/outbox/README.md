# LabTalk LMS Local Outbox

This directory is the local-only outbox placeholder registered by the LabTalk
LMS communications lane.

It contains no endpoint, credentials, student data, or live delivery
configuration. Queue records created during local testing must remain
unpublished unless a separate reviewed LMS transport and privacy gate explicitly
authorizes them.
