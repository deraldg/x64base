# Phase 7 — Manual Web-Ascent — PICK-UP HERE (AIF-072)

> **This is an open, not-yet-started lane. It is the current recommended next
> target of the DOCFLUSH full-stack push.** Registered so it cannot be lost the
> way it nearly was: it lived only in an ephemeral task list until 2026-07-28.

**Lane:** AIF-072 (`docflush-manual-web-ascent`) · **Run label:**
`DOCFLUSH-WEBASCENT-20260728` · **Owner:** member.derald ·
**Steward:** member.ai.claude.cowork · **Status:** not started (pick-up-ready).

## The one-line objective

Promote the **accepted DOCFLUSH-20260728 manual payload** — the manualgen
command-reference lane (the five FoxPro string functions `STUFF`/`PADL`/`PADR`/
`PADC`/`PROPER`, 236 registered command keys, 74 scalar functions) — up to
x64base.com through the manual-publication gates, and live-verify it, mirroring
the 2026-07-18 nine-gate documentation checkpoint.

## Where things actually stand (so you don't redo done work)

- **Done and live:** the documentation *cluster* and the engine/dev pages are
  published (website source `5703c5ad`; the BBS/AI-Portal reports rode `bb862645`
  on `gh-pages`). The website's current-work feed already shows the manualgen
  command-reference lane as a distinct, dated state.
- **Accepted in dev, NOT web-projected:** the assembled **manual payload** itself.
  `docs/dev/project-truth` (the 2026-07-28 update) states it plainly: *"the
  manualgen command-reference lane is accepted in the development branch and not
  yet deployed to public `main`."* That deployment is Phase 7.
- **Not started:** the promotion → website-feed → publish → live-verify ascent for
  that payload.

## The gates (the ascent, from the 2026-07-18 pattern)

The prior checkpoint completed "nine of nine controlled gates" and produced the
accepted Markdown manual, the command-reference snapshot, and the website
projection. Phase 7 re-runs that ascent for the *current* payload:

1. Confirm the accepted payload baseline (gate-4 apply provenance; the five
   functions in `SYSFUNC`; 236/74 counts).
2. Assemble / refresh the developer-manual artifact from the accepted lane.
3. Regenerate the source-derived website projections (command catalog, function
   catalog, harvest diagram) — **codex regenerates the diagrams** (deferred item).
4. Promotion-manifest / drift-gate check (`tools/staging/audit-drift.ps1`,
   `PROMOTION_CHECKLIST.md`).
5. Refresh the website feed / documentation-progress for the manual payload.
6. Build the website (`npm run build`) — the content gate must pass.
7. Local visual review (maintainer) + deploy (`publish:github-pages`).
8. Live-verify the manual pages on x64base.com (do not trust the green build
   alone — the review/verify step is now mandatory).
9. Record the closeout + set `current_by_lane[AIF-072]` to the first real run.

## Authoritative sources

- `docs/dev/project-truth` 2026-07-28 update (the "accepted, not deployed" record)
- The manualgen published tree under `docs/manuals/developer/manualgen/published/`
- gate-4 acceptance provenance (the 168-row apply, `AIF-068` manualgen lane)
- `PROMOTION_PROCESS.md` / `PROMOTION_CHECKLIST.md` (the promotion runbook)
- Website: `D:/dev/x64base-site` (`content/docs/**`, the current-work feed builder)

## To resume (make the lane live)

1. `python tools/coordination/session_coordinator.py claim-aif` is already done
   (`AIF-072.claim`). Register the first real run in `labtalk/registries/ai_runs.yaml`
   and set `current_by_lane: { AIF-072: <run_id> }` so it appears in the AI Portal
   report's lane table.
2. Walk the gates above; keep each gate's evidence.
3. Live-verify, then drop a `SESSION_CLOSEOUT_*` and a `board.worklog` handoff.

**Known deferred sub-item:** the source-derived diagrams (command-catalog,
function-catalog, harvest SVG) are regenerated by codex, not hand-edited.
