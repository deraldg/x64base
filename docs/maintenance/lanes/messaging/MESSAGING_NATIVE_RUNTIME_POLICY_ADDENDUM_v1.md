# Messaging Lane Placement Addendum

Messaging remains a Blackbox and maintenance lane. It should eventually connect source text extraction, message definitions, language/locale rows, and runtime message output.

The official runtime messaging integration should be native C++ and catalog-backed. Python helper tools may inventory or generate candidate reports. PowerShell must not become the permanent messaging workflow.
