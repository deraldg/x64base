# DOCFLUSH-20260914-001 -- Gate 0 run envelope

    opened    2026-09-14
    owner     member.derald
    steward   member.ai.claude.cowork
    status    OPEN. Baseline PARTIAL -- see "what is not measured".

## Why this is not a continuation of v9

DOCFLUSH-20260902-001 (v9) is scoped, in its own envelope, as **WEBSITE PUSH
ONLY**: "No source change, no HELP rebuild, no manual re-acceptance," and:

> If any of those becomes necessary, this run STOPS and a dev-tree phase
> reopens. That is the cookbook's rule and it is also the practical one: a push
> that starts rebuilding is no longer a push.

Between 2026-09-02 and today both happened. `src/**` changed and the HELP store
was rebuilt twice. Cookbook V2 Step 0 classifies on exactly that question, and
its first row is unambiguous:

    Did src/** or include/** change since the last run?
        YES -> FULL RUN. Phases 1-7, then PUSH.

So v9's inheritance of E2-E5 and E7 is **VOID**, by the condition v9 itself
wrote down. This run carries v9's completed website-tree work forward as
material, not as proof.

**v9's local site work is not lost and must not be redone.** Its Gate 6 record
stands: the nine-field authority artifact was reconciled, all 13 freshness
contracts pass, the self-test still rejects staleness, the FAQ review found and
fixed a contradiction and a gap. What is stale is only the FIGURES, because the
store moved after they were written. That is a re-measure, not a re-run.

## What was already done, and where it lands in this run

Not planned as a flush -- it happened as engine work and is being claimed here
because it is the same work the front phases do.

    source usage contracts   NORMALIZED. One store family. The bridge gained six
                             reader fixes; the miner's contract emission is gated.
    HELP store               REBUILT. CMDHELP BUILD LEGACY + CMDHELP BUILD.
    contract drift           CLEAN -- 0 NEW, 0 GONE, 0 TEXT DIFFERS, by
                             tools/fullstack_docs/contract_inventory.py (new).
    dotref                   268 entries, refcheck_v1 PASS, 0 phantoms.

## Baseline measured today

    contracts in src        223 commands, 241 blocks
    USAGE_CONTRACT rows     4,437 mined / 4,445 by SOURCE, 223 commands
    CONTRACT_* (miner)      0
    content in BOTH         0
    legacy command rows     464
    legacy argument rows    2,378
    reachable topics        669
    HELP line rows          18,566

Against v9's Gate 0 baseline of 2026-09-02 -- the numbers v9 wrote into the
site's authority artifact and command-reference page:

    legacy command rows     462  ->  464
    legacy argument rows  2,368  -> 2,378
    reachable topics        666  ->  669

All three moved. Publishing v9 unchanged would publish figures that were honest
on 2026-09-02 and are false today, and **eleven of the thirteen freshness
contracts would still pass**, because they check that a visible marker matches
its authority, not that the authority is current. v9's own Gate 6 named that
failure mode about a different field.

## THE BLOCKER: THE STORE DOES NOT BIND TO A COMMIT

The engine that wrote this store reports:

    version metadata : beta 1.1 2026-09-13 9f810894 dirty=1

`dirty=1`. The exe was built from a dirty worktree, so **the store is not
reproducible from any commit**, and the preflight's `binding` sub-check exists
for precisely this failure (recorded 2026-08-24).

The dirty paths are this session's own uncommitted normalization lane:

    src/cli/helpdata_cmdhelp_bridge.cpp
    src/help/helpdata_source_miner.cpp
    tools/fullstack_docs/contract_inventory.py     (new file)
    include/dotref.hpp

**Nothing downstream may be treated as a baseline until that is committed and
the store rebuilt from the clean tree.** A manual candidate built on this store
would cite an engine that no commit produces.

**COMMITTED 2026-09-14 as `b6dabae63`** -- "selfdoc: one contract family in the
store, and a reader that reads all of it". Five files: the four above plus
`TIER0_STATE.md`, regenerated by the `tier0-refresh` hook and declared as riding
along by design. The full gate chain passed: refcheck 0 guarded phantoms,
normcheck 0 findings in every fail-severity lane, edrefcheck PASS, house-style
PASS, manual-link-integrity 164/164, AIF and R-number collision gates PASS.

**THE HALF OF THE BLOCKER THAT REMAINS IS AN ORDERING PROBLEM, and it is
structural rather than a mistake.** The store files are THEMSELVES TRACKED, so a
store rebuild dirties the worktree by definition. An exe built from a clean tree
and an uncommitted store cannot coexist. The sequence that resolves it:

    1. git checkout -- dottalkpp/data/help     (safe: regenerable, baseline above)
    2. .\build.ps1                             -> expect b6dabae63 dirty=0
    3. .\datarun -> CMDHELP BUILD LEGACY, then CMDHELP BUILD . d:\code\ccode\src
    4. commit the store                        -> binding closed

Step 1 touches files owned by a CONCURRENT SESSION. The modified content is this
session's own rebuild output and regenerates in seconds, so the revert is
believed safe -- but it is the owner's call, not the steward's.

Step 2 also settles whether any of this is needed: if the `dirty` flag ignores
data files, `dirty=0` will already be true and steps 1 and 4 collapse into
"rebuild the store and commit it".

## E1-E8 entry rows

Marked PROVEN, INHERITED (with its condition), OPEN, or UNRUN. Rule 5: unrun is
not pass, and an inherited proof is never restated as a current one.

    E1  previous run closed          PROVEN. v8 (DOCFLUSH-20260901-002)
                                     RUN_CLOSEOUT_V1.md, closed 2026-09-02.

    E2  HELP current + CMDHELPCHK    PARTIAL. HELP is current as of the rebuild;
                                     CMDHELPCHK HAS NOT BEEN RUN. And the store
                                     it would check is the unbound one above.

    E3  contracts 100%, fallback 0   UNRUN. source_census.py not run this cycle.

    E4  refcheck_v1 + normcheck_v1   PARTIAL. refcheck_v1 PASS (268, 0 phantoms).
                                     normcheck_v1 NOT RUN.

    E5  harvest re-exported after    OPEN, AND THIS IS THE SECOND BLOCKER.
        the build                    HELP_META_HARVEST_EXPORT_v1.ps1 has NOT run
                                     since the store changed. The canonical
                                     harvest is older than the store it claims to
                                     represent, so any manual candidate built now
                                     is built on stale input. Use the SANCTIONED
                                     engine-backed exporter, never the Python
                                     scaffold.

    E6  command-catalog.mdx          OPEN.
        regenerated, fallback 0

    E7  HELP store backup + named    SATISFIED -- by version control, not by a
        rollback                     backup root. VERIFIED 2026-09-14 after the
                                     commit: all nine files under
                                     dottalkpp/data/help are TRACKED and show
                                     ` M`, over baseline 8dc6e9983 "Refresh
                                     canonical HELP for E2". That baseline is the
                                     V8 STORE -- the one the accepted manual and
                                     the canonical harvest correspond to, which is
                                     exactly the rollback point this row wants.
                                     Named rollback: git checkout --
                                     dottalkpp/data/help

                                     THIS ROW FIRST READ "NOT SATISFIED". It was
                                     written that way because the check had not
                                     been run, which was correct at the time -- an
                                     unrun check is not a failure and must not be
                                     recorded as one either. The store had in fact
                                     been safe all along. What was actually true is
                                     narrower and still worth keeping: the rebuilds
                                     were run as engine verification, under a name
                                     no gate watches, and nobody confirmed the
                                     rollback point before relying on it.

    E8  owner authorization per      OPEN. v8 granted manual acceptance only.
        distinct mutation            Website publication remains unauthorized and
                                     v9's Gate 6 left publication_authorized
                                     false. Nothing here changes that.

## What is NOT measured, named rather than omitted

    ccode HEAD (clean)      b6dabae63 as of 2026-09-14. The store rebuild that
                            binds to it has NOT yet been run.
    site HEAD               UNMEASURED. v9 recorded 85cdb95c5; two further
                            commits (df980a68c, 6dce892a7) are known to exist on
                            codex/lean-sites-publish. Current tip unread.
    published site          v9 recorded the live gh-pages branch as
                            2026-08-26. NOT re-checked. `git ls-remote`, not the
                            local ref -- the local pointer was stale by a month
                            once already.
    CMDHELPCHK              not run
    normcheck_v1            not run
    source_census           not run
    harvest freshness       not run

## Order of work

    1. COMMIT the normalization lane. Explicit paths only.   DONE b6dabae63
    2. REBUILD, then rebuild the store from that commit.     DONE 02ad70354
    3. Re-run contract drift, CMDHELPCHK, normcheck_v1, source_census, refcheck.
       DONE except drift and source_census -- see the status block below.
    4. RE-EXPORT the harvest with the sanctioned script (E5).
    5. Manual candidate chain, then the Gate 4 PARTITION -- zero unexplained, or
       do not apply. Counting is not partitioning.
    6. Commit the flush.
    7. PUSH: re-measure v9's nine fields in place against the new store, then
       present E8 for signature.

Steps 1-3 are the ones that unblock everything else, and step 1 is one commit.

## Status 2026-09-14 -- BLOCKER 1 CLOSED

    b6dabae63   the normalization lane (4 files + TIER0_STATE.md)
    02ad70354   the store, rebuilt by b6dabae63 (9 files + TIER0_STATE.md)

The engine comes from a named commit and the store comes from that engine.
Both are in history. `dirty=1` at build time was the nine tracked DBFs under
dottalkpp/data/help and nothing else -- SOURCE was clean, which is the
condition the binding check actually asks about.

THE REBUILD IS DETERMINISTIC, measured rather than assumed: 464 command rows,
2,378 arg rows, 18,566 line rows, 669 topics -- identical across the dirty-tree
rebuild and the commit-bound one. A store that moved when nothing moved could
not be a baseline either, and nobody had checked that before.

    E2  CMDHELPCHK                    CLEARED. "OK no structural issues found",
                                      run against THIS store, not inherited.
    E4  refcheck_v1 + normcheck_v1    CLEARED, AND BY THE COMMIT HOOK. The
                                      prepush gate runs both plus edrefcheck on
                                      every commit: dotref 268 / 0 guarded
                                      phantoms, 0 findings in both fail-severity
                                      lanes, edrefcheck PASS. This envelope had
                                      them recorded as UNRUN; they were running
                                      all along, inside a gate nobody had read
                                      the output of closely enough to credit.
    E7  backup + named rollback       SATISFIED (see the row above).

    E3  source_census                 CLEARED. census 1,108 of 1,108 tracked
                                      source files, uncovered 0, coverage 100.0%.
    E5  harvest re-export             CLEARED. HELPMETA-20260914T012823Z, by
                                      HELP_META_HARVEST_EXPORT_v1.ps1 -- the
                                      SANCTIONED engine-backed exporter, and the
                                      output proves which one ran: "10 current +
                                      carried 4 stale". The Python scaffold's
                                      tell is relabelling stale tables EXPORTED;
                                      this one carried them as stale and said so.
                                      14 tables, matching AIF-068's 14-CSV input
                                      contract.

                                        HELP_COMMANDS        464
                                        HELP_CMD_ARGS      2,378
                                        HELP_ARTIFACTS    10,587
                                        HELP_LINE         18,566
                                        HELP_SECTION      10,587
                                        HELP_TOPIC           669
                                        META_SYSCMD          212
                                        META_SYSFUNC          79
                                        META_SYSARGS         249
                                        META_SYSSUBCMD        31

                                      Against the canonical harvest this
                                      replaces -- HELP_COMMANDS.csv, 403 rows /
                                      290 names, written 2026-05-25 -- that is a
                                      92-DAY-OLD INPUT finally refreshed. The
                                      34-name harvest debt recorded in
                                      PHASE6_REHARVEST_DEBT.md was always "one
                                      gated action, not 34 writing jobs". This
                                      was the gated action.
    E6  command-catalog.mdx           open.
    E8  owner authorization           open.

    contract drift after the store commit  CLEAN. 223 commands / 241 blocks in
    source; 4,437 rows / 223 commands in store; NEW 0, GONE 0, TEXT DIFFERS 0;
    CONTRACT_* 0, content in BOTH families 0.

## The run id is correct, and the reason is worth keeping

An earlier draft of this envelope flagged a possible date mismatch: the host
stamps builds `Sep 13 2026` while this run is named 20260914. SETTLED by the
harvest's own run id, `HELPMETA-20260914T012823Z` -- **the lane stamps run ids
in UTC**, and UTC had already rolled to the 14th. The directory name follows the
lane's convention. No rename.

## A FIELD MAPPING THE PUSH WILL NEED AND THIS RUN HAS NOT ESTABLISHED

v9's Gate 0 recorded the site's `help_lines` as 29,700. This harvest's HELP_LINE
table is 18,566 rows. **THOSE ARE NOT THE SAME MEASURE and no attempt is made
here to reconcile them** -- one of them counts something the other does not, and
guessing which would be the third count-discipline error in one day.

Before the push updates `help_lines`, establish WHICH number feeds that field.
The producer is build_documentation_progress.py; read it rather than inferring
from the name.

## AN UNRECONCILED COUNT, named before anyone quotes it

Two instruments measured the contract-bearing population on 2026-09-14 and did
not agree, and neither is known to be wrong:

    source_census.py        commands (@usage)           232 FILES
    contract_inventory.py   mined contracts from        208 FILES
                            distinct commands            223
                            contract BLOCKS              241

A 24-file gap between the two file counts. Candidate explanations exist -- the
16 files carrying `@dottalk.usage.voluntary` (which the store's miner does not
match and this tool lists separately), and the 9 plain-form blocks in
doc-tooling files that declare no `command:` -- but 16 + 9 is 25, not 24, so
the arithmetic does NOT close and no explanation is adopted here.

THIS IS THE COUNT_DISCIPLINE SHAPE EXACTLY: two correct sums over different
sets, wearing the same noun. The lesson from 2026-08-25 was that such numbers
are not guesses, they are correct answers to unasked questions -- and the fix
is to apply the discriminator, not to pick the one that looks right.

Settle it before either number reaches a published surface:

    git grep -l "@dottalk.usage" -- "*.cpp" "*.hpp" "*.h" | Measure-Object -Line
    git grep -l "@dottalk.usage.voluntary" -- "*.cpp" "*.hpp" "*.h" | Measure-Object -Line
    git grep -l "@dottalk.usage v1" -- "*.cpp" "*.hpp" "*.h" | Measure-Object -Line

If `source_census` matches the bare prefix and `contract_inventory` matches the
versioned marker, the difference is the voluntary set plus whatever else the
prefix sweeps up -- and one of the two tools should say which it counts.

## A gate finding, recorded where the next reader will hit it

Committing the store was REFUSED by prepush_gate.py with **exit 3** and
"data/runtime fixtures staged ... re-run with --allow-data". Correct behaviour:
the gate wants a data mutation named out loud. The acknowledgement is an
environment variable (`X64BASE_ALLOW_DATA=1`), narrow and loud, and hard-blocks
still fail under it.

**But GATE_CORRECTIONS_REQUIRED_V1.md G1 recommends giving "could not run" its
own exit 3, distinct from "found a problem" at 2. THAT NUMBER IS ALREADY TAKEN,
inside the same gate family, for "needs acknowledgement".** Implementing G1 as
written would reintroduce exactly the ambiguity G1 exists to remove. Whoever
takes G1 needs a fourth code.

## Status 2026-09-14T02:30Z -- BLOCKER 2 CLOSED, AND THREE CORRECTIONS

### E5 is now actually cleared, and the earlier row overstated it

The row above records E5 CLEARED on the strength of the EXPORT. Exporting
writes a candidate under `harvested/export_runs/<id>/`. Promoting it into
`manualgen/harvested/` is a separately authorized gate, and it had not been
run. Freshness measured against the two workspaces disagreed by design:

    canonical harvest    E5 FAIL   7 of 14 tables, 7 manifest findings
    candidate export     E5 PASS  14 of 14

The reassuring number was the one that did not matter. The promotion applied
at 02:01:27Z under authorization `B9122EA8...`:

    status APPLIED   findings NONE   canonical_files_mutated 8   rollback 0
    E5 PASS: 14/14 tables match current HELP/META; manifest_findings=0

Verified by counting the files rather than reading an exit code:
464 / 2378 / 669 / 10587 / 10587 / 18566 / 79, all seven as predicted, and
ARTIFACTS == SECTION 1:1 holds on both sides.

### E7 IS WRONG IN THE ROW ABOVE. There is no rollback point for the harvest.

E7 reads SATISFIED "by version control". That is true of the HELP STORE
(`dottalkpp/data/help`, committed as `02ad70354`, rollback point `8dc6e9983`).
It is FALSE of the harvest.

    git ls-files docs/manuals/developer/manualgen/harvested/     (empty)
    .gitignore:525   docs/manuals/developer/manualgen/**/*.csv

The canonical harvest is deliberately outside version control, along with the
export manifest that binds it to a run id. The boundary is legible once the
directory names are read: `accepted_artifacts/`, `accepted_catalogs/`,
`accepted_manifests/` and `published/` are tracked; `harvested/`, `generated/`
and `backups/` are not.

**The only copy of the pre-apply harvest is**

    runs/DOCFLUSH-20260914-001/harvest_promotion_apply_v2/before/

and it sits in ignored space, where `git clean -xdf` removes it without a
prompt. Hashes in the execution manifest prove what a file WAS; they do not
restore it. Retain that directory until a later promotion supersedes it.

This also explains the promotion gate. Plan, mutation ledger, written
authorization, before/staged_after snapshots, a `rollback` subcommand -- that
is a hand-rolled version-control system, and it exists BECAUSE git was pointed
away from this directory, not in duplication of it.

### The "92-DAY-OLD INPUT" in the E5 row is wrong

The canonical harvest being replaced was dated 2026-09-02, promoted by v8. The
92-day figure came from PHASE6_REHARVEST_DEBT.md, which measured the harvest
BEFORE v8 refreshed it. The real age was 12 days. A stale number was carried
out of a document without checking whether the thing it described had moved.

### The four PASSes that were reading 2026-09-02

`build-reference-candidate` was run four times before the promotion. Every run
reported PASS at `topics=666 lines=29700 commands=462 args=2368`. After the
promotion, same tree, same flags:

    topics=669 lines=18566/18566 commands=464 args=2378 syscmd=212  status=PASS

manualgen was not at fault. `--harvest-workspace` says outright: *attached by
hash; NEVER PROMOTED.* It reads what it is given. Nothing else asked whether
what it was given matched the live store. `docpush_preflight.py` step 7 now
asks, against the CANONICAL workspace, and fails HARD.

### NEW FINDING -- 726 duplicate artifacts, 11.3% of the store

Two scans in `src/help/helpdata_source_miner.cpp` cover the same text:
`mine_usage_help_functions` (bodies of `*_usage` / `*_help` functions) and
`mine_usage_output_blocks` (any `cout << "...Usage|Syntax..."` to the next
`return`). When help text lives in a `*_help` function that prints "Usage",
both scans mine the same lines and both emit.

`add_unique` keys on `catalog|command|kind|name|text|EVIDENCE`. The two
evidence strings differ, so the key differs, so the dedup never fires.

    artifacts             10587
    distinct on content    9861
    duplicates              726   across 563 groups
    duplicate line rows    2096   of 18566   (11.3%)
    commands touched        114

Verified on `src/cli/cmd_pshell.cpp`: line 61 `void show_pshell_help(...)`
matches the function regex, line 83 `std::cout << "Usage:\n"` matches the block
regex, and lines 84-89 fall inside both. The harvest shows it as two contiguous
runs of artifact ids, 4917-4922 and 4924-4929, with a header at 4923.

This does NOT overturn the contract-drift work. That collapsed two FAMILIES
and reached `content in BOTH: 0`, correctly. It never claimed to collapse
duplicates WITHIN a family, and did not. This defect was underneath it.

Dropping `evidence` from the key collapses all 726, but first-writer-wins would
keep whichever scan runs first -- better provenance here, by accident of call
order rather than by decision. Recording both evidence strings, or having each
scan skip regions the other claimed, is the more honest shape. NOT PATCHED;
this needs a decision, and any fix is a source change carrying its contract.

### The 28 non-ASCII rows, re-measured against the promoted harvest

    harvest-ascii: 28 of 18566 harvest rows carry non-ASCII
      topics affected : 3
      22 right arrow   SOURCE_MINER   PSHELL, SQL HELP
       6 em-dash       SHARED_MSG

Same 28. The dedup removed 11,134 line rows and NOT ONE carried a non-ASCII
character, so all 28 live in the surviving family and are genuine defects.

The 22 arrows are **11 unique source lines seen twice** by the duplication
above -- 6 in `cmd_pshell.cpp`, 5 in `cmd_sql_help.cpp`. Fixing the miner
halves the reported count without touching source. Fixing the source removes
them. Both are needed.

They live in `std::cout <<` runtime output, not in `@usage` blocks: they are
what the engine PRINTS. Changing them changes displayed behaviour, so the house
rule applies -- the usage change carries its contract.

The 6 em-dashes are SHARED_MSG runtime warnings carrying `{orig}` / `{actual}`
/ `{safe}` placeholders. Different lane from the usage contracts.

The tool is `tools/manualgen/check_harvest_ascii.py`. An earlier note in this
run reported it ABSENT from the tree; that was a search of
`tools/fullstack_docs/` only, reported as a conclusion. It is tracked and
present.

### Where the run stands

    E1 PROVEN   E2 CLEARED   E3 CLEARED   E4 CLEARED   E5 CLEARED
    E7 SATISFIED FOR THE STORE ONLY -- see the correction above
    E6 open     E8 open

Next: E6, `command-catalog.mdx` regenerated with fallback 0.

### One input still chosen by fallback

`manualgen validate` reports `selection_mode=explicit` for the harvest and
`selection_mode=legacy_default` for the assembly workspace. The input that cost
this run is now pinned by flag; its neighbour is still chosen by fallback. Same
shape, different input. Pin it before anything projects toward a published
surface.

`validation_review_rows=1` is unexamined. "Review" is the category that quietly
became "accepted" in v9.

## Status 2026-09-14T04:40Z -- THE CENSUS GAP IS SETTLED

The section above headed "AN UNRECONCILED COUNT, named before anyone quotes
it" is now reconciled. It decomposes completely, with no remainder.

Applying each tool's OWN inclusion rule and listing every file one counts and
the other does not:

    census only     22
    inventory only   0

    16   @dottalk.usage.voluntary
     6   the marker appears, but as PROSE

`INVENTORY ONLY = 0` is the half to read first. Every mined contract is inside
census scope. The tools differ in extensions (9 vs 5) and in method (a
filesystem walk under src/include/bindings vs `git ls-files`), and neither
difference produces a single file in practice. The whole gap is the marker.

### The sixteen are correct and documented

`contract_inventory` excludes `@dottalk.usage.voluntary` deliberately, and says
why in its own source: the literal `"@dottalk.usage.voluntary v1"` does not
CONTAIN `"@dottalk.usage v1"`, so `helpdata_source_miner.cpp` never sees them
either. They reach no store, no topic, no page. Fourteen of the sixteen are the
SET family.

### The six are sentences, not contracts

    src/cli/cmd_rpg.cpp:24        //   handler and a @dottalk.usage contract IN THE SAME COMMIT as the handler.
    src/cli/cmd_trigger.cpp:24    //   (identical boilerplate)
    src/cli/cmd_ttestapp.cpp:24   //   (identical)
    src/cli/cmd_vmware.cpp:24     //   (identical)
    src/cli/cmd_vt200.cpp:24      //   (identical)
    src/cli/shell_commands.cpp:133  // inline it had no source file, so no @dottalk.usage contract, so no SYSCMD

Five are one boilerplate sentence repeated in five file headers -- a rule ABOUT
contracts, quoted in the files it governs. The sixth is a comment explaining
that a command HAS NO contract, counted as having one.

`source_census` matches `@dottalk\.usage\b` anywhere in the file, including the
middle of an English sentence. IT CANNOT TELL AN INSTANCE OF THE MARKER FROM A
CITATION OF ONE -- the defect class already recorded as
FINDING_A_CHECKER_CANNOT_TELL_AN_INSTANCE_FROM_A_CITATION_OF_ONE, recurring in
a tool nobody had pointed at this question.

### What this retires

The candidate explanation recorded above -- "16 voluntary + 9 plain-form = 25,
so the arithmetic does NOT close" -- had the second term wrong. It is 6, not 9,
and the gap is 22, not 24. Those figures were taken over trees carrying another
session's uncommitted work, which is why they drifted between readings. The
decomposition is stable regardless.

`contract BLOCKS 241`, cited three times in this envelope, stays retired as a
quotable figure.

### What each number means, for E8

    source_census        how many source files MENTION a usage marker
    contract_inventory   how many tracked files carry a contract the engine
                         will actually MINE

`source_census`'s command-file count is OVERSTATED BY SIX. Both tools are
internally correct; they answer different questions. E8 must not put the census
number on a page as a contract count.

### Fix direction, recorded not taken

A contract marker is the first token of a comment line. A citation sits
mid-sentence. Anchoring `USAGE_RE` to that position separates them without a
keyword list. That is a change to a measuring instrument and wants a decision
and a test, not a patch at the end of a long run.

The decomposition is reproducible: `tools/fullstack_docs/census_gap_decompose.py`,
read-only, reimplements both inclusion rules from their own sources and names
every file in the difference with the reason it is there.
