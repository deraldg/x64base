# DEV-31 The AI Portal and Frontal Memory

```yaml
page_id: DEV-31
title: The AI Portal and Frontal Memory
status: STUB_EXTERNAL_AUTHORITY
last_verified: 2026-09-16
```

## Status of this chapter

**This chapter is a stub.** The published website is the current authority for
this topic and nothing here restates it yet.

It was created 2026-09-16 so that `ANCHOR-FRONTAL-MEMORY` and `ANCHOR-AI-PORTAL` have a target that exists.
A deferred anchor pointing at a chapter title that resolves to nothing cannot be
checked by any instrument -- the gap is real but invisible, which is the failure
the anchor rule exists to prevent. A stub that names its own emptiness is worse
reading and better engineering than a promise with no file behind it.

**Do not cite this chapter as evidence** until the `status` field above changes.

## Current authority

- `x64base-site/content/memory/overview.mdx`
- `x64base-site/content/memory/roadmap.mdx`
- `x64base-site/content/memory/team-model.mdx`

`ANCHOR-AI-PORTAL` currently cites `content/portal/schemas.mdx`, which the site
repository excludes with the comment *"Maintainer-only very early alpha report
preview. Do not publish."* Nothing under `content/portal/` is tracked, so that
citation points at something no reader can reach. **That anchor needs different
evidence before this chapter can use it.** The three `content/memory/` pages above
are tracked and are the usable authority today.

## What this chapter must cover

Derived 2026-09-16 from the section structure of the page(s) above. It is an
inventory of what the site already says, not an outline anyone has agreed to.

1. The problem: the amnesiac collaborator
2. Why the memory is human-side
3. The architecture, and a taxonomy of memory classes
4. Triage: the value function
5. Synapses: the associative recall graph
6. The substrate: the AI-BBS
7. Registered portal schemas and the identity crosswalk

## Anchor

`ANCHOR-FRONTAL-MEMORY` and `ANCHOR-AI-PORTAL` in `docs/manuals/anchors/manual_generation_anchor_map_v1.csv`.

Both anchors stay at their recorded state. Creating a file does not create
evidence, and this chapter asserts nothing that could be proved or disproved.
