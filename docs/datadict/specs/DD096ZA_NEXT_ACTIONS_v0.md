# DD096Z-A Next Actions

1. Install the drop-in package.
2. Run the design generator after DD096YQ and DD096Z are green.
3. Inspect preconditions, apply steps, file-family plan, table map, compatibility decisions, and future gates.
4. If accepted, DD096Z-B should create backups and an inactive candidate-root staging path only.
5. Do not replace the active Data Dictionary catalog from DD096Z-A.
