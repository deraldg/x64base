# DD-089G Next Actions

1. Install DD-089G.
2. Run preflight without `--apply-cmd-ddict`.
3. Inspect candidate marker ledger, helper artifact ledger, and protected-file ledger.
4. Apply with `--apply-cmd-ddict` if clean.
5. Do not build yet if CMake wiring is still pending.
6. Next lane: DD-089H guarded build wiring package.
