# DD-038 Next Actions

1. Install DD-038 from Downloads into the repo root.
2. Smoke `baseline_pointer.py --help`.
3. Run `baseline_pointer.py` against `current_baseline.json`.
4. Smoke `.\tools\datadict\dd-status.ps1`.
5. Run `.\tools\datadict\dd-status.ps1`.
6. If only accepted baseline/proof artifacts appear, run `.\tools\datadict\dd-status.ps1 -AcceptBaselineArtifacts`.
7. Preserve the result in `docs/datadict/runlog/`.
