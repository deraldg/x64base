# DD096Z-D2W SELECT-Area SMARTLIST Candidate Rebuild v0

Created UTC: `2026-05-29T16:10:32+00:00`

## Purpose

DD096Z-D2W supersedes DD096Z-D2V's listing style.

It keeps the correct SELECT-area repair and changes generated listing/verification output from `LIST` to `SMARTLIST` / `SL`.

## Boundary

No source edits, no build edits, no active catalog replacement, no active DBF writes, no active CDX/LMDB rebuild, no workspace mutation, no HELP/CMDHELPCHK mutation, no manual mutation.
