# DD096Z-D2ZL DDICT FIELDS/TAGS Source Context v0

Created UTC: `2026-05-29T19:34:54+00:00`

## Purpose

DD096Z-D2ZL extracts actual `cmd_ddict.cpp` source windows around FIELDS/TAGS before a real logic patch.

D2ZK applied safe scaffolding and include wiring, but did not rewrite the actual command logic. D2ZL is a review/context package so D2ZM can patch precise code instead of guessing.

## Boundary

No source edits, no build edits, no active catalog replacement, no active DBF/CDX/LMDB mutation, no HELP/CMDHELPCHK mutation, and no manual mutation.
