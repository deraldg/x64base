# DD-063R Next Actions

1. Install DD-063R.
2. Run without `--write-acceptance`.
3. Inspect gate, accepted command, accepted test, and boundary ledgers.
4. Run with `--write-acceptance` if green.
5. Proceed to DD-064 guarded runtime implementation plan only after explicit authorization.
