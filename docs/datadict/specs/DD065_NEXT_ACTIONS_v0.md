# DD-065 Next Actions

1. Install DD-065
2. Run preflight first
3. Inspect generated source candidates
4. Install source files only with `--apply-source-files --replace-existing`
5. Install smoke test with `--install-smoke-test`
6. Do not expect `DDICT` to run until DD-066 registration/build integration
