# DD096Z-D2ZO Next Actions

1. Install the drop-in.
2. Run preview.
3. Review focus windows and candidate diff.
4. Run with `--write-smoke-script`.
5. Only use `--apply-recognized-patch` if the manifest reports candidate changes and safe anchors.
