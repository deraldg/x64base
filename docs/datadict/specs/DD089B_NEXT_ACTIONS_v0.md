# DD-089B Next Actions

1. Install DD-089B.
2. Run preflight without `--apply-install`.
3. Inspect install ledger and protected-file ledger.
4. Apply with `--apply-install` if clean.
5. Do not build-wire yet.
6. Next lane: DD-089C guarded implementation extraction preview.
