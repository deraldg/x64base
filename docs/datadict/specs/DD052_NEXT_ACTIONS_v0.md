# DD-052 Next Actions

1. Install DD-052.
2. Run preflight without `--prepare-staging`.
3. If preflight is green, run with `--prepare-staging --replace-target`.
4. Run the generated DotTalk++ script manually.
5. Run DD-052 `--verify-after-runtime`.
6. Proceed to DD-053 pydottalk/readback verification.
