# DD095 Next Actions

1. Install DD095.
2. Run after DD093C and DD094 are green.
3. Inspect layout policy ledger, catalog artifact policy, gate ledger, and no-mutation boundary ledger.
4. Optionally run with `--write-policy` to write `docs/datadict/policy/DD095_DATADICT_LAYOUT_POLICY.md`.
5. If green, proceed to DD096 schema promotion/catalog policy or DD092D apply planning only after explicit authorization.
