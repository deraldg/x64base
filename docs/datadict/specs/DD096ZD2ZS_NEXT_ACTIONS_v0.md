# DD096Z-D2ZS Next Actions

1. Install the drop-in.
2. Run with `--stage-review-copy --write-smoke-script`.
3. Edit `docs/datadict/reviews/DD096ZD2ZS/cmd_ddict.cpp.review_candidate`.
4. Validate with `--candidate-source`.
5. Apply with `--candidate-source ... --apply-reviewed-source`.
6. Rebuild and run the D2ZS smoke.
