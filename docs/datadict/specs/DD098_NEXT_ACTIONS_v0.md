# DD098 Next Actions

1. Install DD098.
2. Run after DD097 is green.
3. Inspect the green chain ledger, baseline status ledger, gate ledger, and no-mutation boundary ledger.
4. Optionally run with `--write-closeout` to write the milestone report into docs/datadict/reports.
5. Then choose DD096A candidate catalog-row design or DD092D guarded apply planning with explicit authorization.
