# DD-040 Next Actions

1. Install DD-040 from Downloads into repo root.
2. Run `catalog_row_projection.py --help`.
3. Run the projection against `DDBASE-stable-v2`.
4. Review row counts for all planned catalog DBFs.
5. Do not create DBFs yet.
6. If projection is green, decide whether to explicitly authorize DD-041 sandbox DBF creation/readback.
