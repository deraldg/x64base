# DD-041 Next Actions

1. Install DD-041 from Downloads.
2. Run the help gate.
3. Run without `--execute-sandbox-write` if you want to see the guard.
4. Run with `--execute-sandbox-write --replace-existing-sandbox` after confirming authorization.
5. Inspect `dd041_table_readback_ledger.csv`.
6. Do not promote sandbox DBFs to active catalog.
7. Next likely package: DD-042 sandbox catalog query/readback through x64base or promotion gate planning.
