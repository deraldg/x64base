# DD096A-R Next Actions

1. Install DD096A-R.
2. Run after DD096F reports 8 missing references to the DDICT root object.
3. Rerun DD096B/C/D/E/F using the repaired candidate directory and repaired manifest directory overrides.
4. Do not write active DBFs.
