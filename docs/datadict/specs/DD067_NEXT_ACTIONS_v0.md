# DD-067 Next Actions

1. Install DD-067.
2. Run preflight without `--apply-patch`.
3. Inspect `dd067_patch_preview.diff`.
4. Run with `--apply-patch` only if preview is sane.
5. Build DotTalk++.
6. Run `DDICT HELP` smoke.
