# DD-089H Next Actions

1. Install DD-089H.
2. Run preflight without `--apply-build-wiring`.
3. Inspect CMake wiring ledger and generated diff.
4. Apply with `--apply-build-wiring` if clean.
5. Build dottalkpp.
6. Run DD-089I parity closure after successful build and DDICT smoke proof.
