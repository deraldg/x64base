# DD-007 Next Actions

## Recommended next step

Proceed to **DD-008: Source Contract + MetaFact Manifest Extension**, report-only.

DD-008 should extend the extractor lane to include:

- `@dottalk.usage v1` command contracts
- central shell registry rows
- `dt::meta::MetaFact` domain references
- HELP/message/catalog source anchors
- profile-surface classification seeds

## Later step

Proceed to **DD-009: Runtime DBF Fixture Scan** when a runtime data directory or DBF/CDX/memo sample pack is provided or when the user explicitly authorizes scanning a local runtime estate.

DD-009 should not open DotTalk++ by default. It should first parse files read-only and only import runtime transcript evidence if separately supplied.
