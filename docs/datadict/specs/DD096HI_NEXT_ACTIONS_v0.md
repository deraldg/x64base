# DD096H/DD096I Next Actions

1. Generate authorization, runtime DTS, and runner.
2. Inspect the preapply table checks, duplicate blockers, field length blockers, and generated DTS.
3. Run the generated PowerShell runner only if the package status is READY_TO_RUN.
4. Capture the runtime proof.
5. Continue to DD096J post-apply validation/closeout.
