# DD-064 Next Actions

1. Install DD-064.
2. Run without `--scan-source` first.
3. Inspect plan, component map, read-only service contract, runtime test plan, and boundary ledger.
4. Rerun with `--scan-source` for source hook inventory.
5. DD-065 implementation still requires explicit authorization.
