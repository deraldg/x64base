# DD-045 Next Actions

1. Install DD-045 from Downloads.
2. Run `active_catalog_promotion_execute.py --help`.
3. Run the guard pass without `--execute-promotion`.
4. Run the execution pass with `--execute-promotion`.
5. Inspect gate, backup, hash, readback, rollback, and boundary ledgers.
6. Do not create CDX or LMDB as part of DD-045.
7. Proceed to DD-046 active catalog post-promotion runtime readback.
