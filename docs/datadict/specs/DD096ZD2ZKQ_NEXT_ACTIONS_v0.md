# DD096Z-D2ZKQ Next Actions

1. Install the drop-in.
2. Run preview.
3. Apply include repair with `--apply-include-repair`.
4. Rerun D2ZN preview.
5. Run D2ZN with `--apply-safe-marker --write-smoke-script`.
