# DD-037 Next Actions v0

1. Install the repo drop-in from Downloads into `D:\code\ccode`.
2. Run `baseline_status_closure.py --help`.
3. Run DD-036 accepted mode if not already run.
4. Run DD-037 against the DD-034 final v2 check and DD-036 accepted closure.
5. If DD-037 returns `PASS_WITH_ACCEPTED_BASELINE_ARTIFACTS`, write a runlog savepoint.
6. Next package should be DD-038: Accepted Baseline Registry / Current Baseline Pointer, if desired.
