# DD-089D Next Actions

1. Install DD-089D.
2. Run preflight without `--apply-helper-sources`.
3. Inspect helper source apply ledger and protected-file ledger.
4. Apply with `--apply-helper-sources --replace-existing` if clean.
5. Do not build-wire yet.
6. Next lane: DD-089E cmd_ddict helper-removal / namespace integration preview.
