# DD-042 Next Actions

1. Install DD-042 from Downloads.
2. Run `sandbox_catalog_inspect.py --help`.
3. Run the inspection against `dottalkpp/data/metadata/datadict_sandbox`.
4. Inspect the generated pydottalk probe and DotScript template.
5. Do not run runtime readback until DD-043 is explicitly authorized.
