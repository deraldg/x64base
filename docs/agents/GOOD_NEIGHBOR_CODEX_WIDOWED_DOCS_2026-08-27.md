# Good Neighbor -- four AIF-135/136 documents committed on Codex's behalf, and two deliberately NOT

    Date        : 2026-08-27
    Acting      : member.ai.claude.cowork, run COWORK-20260827-001
    On behalf of: member.ai.codex, run CODEX-20260826-014
    Authorized  : member.derald -- "codex ran out of credit in 24 hours,
                  so he is dead until next week, push those through for
                  him and do good neighbor"
    Status      : review-needed. Codex is the steward of this material
                  and has not seen this note.

## WHY THIS HAPPENED AT ALL

The cited-paths gate reported six widows -- files cited by a tracked document,
present on disk, not tracked -- all of them Codex's, cited by
`docs/ai-friendly/AI_INTERACTION_INTAKE_QUEUE_V1.md` rows AIF-135 and AIF-136.

**They did not appear because anything broke. They appeared because the intake
queue entered the gate's scope for the first time.** Claude edited that file on
2026-08-27 to add intake rows for AIF-137 and AIF-138, which pulled it into the
change set; the gate's cited-path count went from 266 to 511 in one commit.

**That is R75 running in the other direction: a gate that inspects only the
change set is silent about every file nobody happens to touch, and its silence
is not evidence.** These six had been widowed since 2026-08-26 and no commit
had given the gate an occasion to say so.

## WHAT WAS COMMITTED -- FOUR DOCUMENTS

    docs/maintenance/AIF135_MONOTONIC_AIF_ALLOCATOR_ALIGNMENT_V1.md
    docs/maintenance/AIF136_AI_PORTAL_MEMORY_TIERING_AND_RETENTION_PLAN_V1.md
    docs/maintenance/AIF136_M3_MEMORY_PILOT_RULING_PACKET_V1.md
    docs/maintenance/AIF136_M4_COGNITIVE_DEMOTION_PROOF_V1.md

CHECKED BEFORE STAGING, not assumed:

- All four are hand-authored prose, ASCII-clean (zero non-ASCII bytes), and
  carry `Owner: member.derald` / `Steward: member.ai.codex` headers.
- No `.gitignore` rule matches any of them (`git check-ignore` returns nothing).
- **Two of them say "commit pending" in their own Status line** -- the
  AIF-135 alignment doc ("implemented locally; owner review before commit")
  and the AIF-136 tiering plan ("commit pending; no artifact moved or
  deleted"). Committing them is what their authors intended next.
- None carries an `ai_report_audit` envelope, and none needs one: enforcement
  is scoped to closeouts and portal reports, not to every
  `docs/maintenance/*.md`. Measured -- Claude's own AIF-137 and AIF-138
  finding documents and the R129 ruling committed the same day added zero to
  `enforced`, while the one closeout added one.

**NOTHING IN THEIR CONTENT WAS READ FOR CORRECTNESS AND NOTHING WAS EDITED.**
Their claims are Codex's and are not endorsed here. What was verified is that
they are committable, not that they are right.

## WHAT WAS *NOT* COMMITTED, AND WHY -- THIS IS THE PART THAT NEEDS A DECISION

    labtalk/reports/portal/memory_storage_classification_latest.md
    labtalk/reports/portal/memory_storage_inventory_latest.md

**These two are still widows on purpose. Three reasons, in order of force:**

**1. THEY WOULD HARD-BLOCK THE COMMIT.** Both carry CRLF line endings -- 62
and 117 CR bytes respectively. `check_house_style.py` is a HARD gate on added
documentation lines (`prepush_gate.py:588`, exit 2: "BLOCKED -- non-ASCII in
added documentation lines"). Staging them as they stand fails the gate, and
the fix is to rewrite someone else's generated output, which is worse than
leaving them untracked.

**2. THEY ARE GENERATED AND SAY SO.** Line 3 of the inventory reads:

    Generated by AIF-136 from a read-only, no-reparse-point scan.
    Do not hand-edit.

Generated catalogs are report-only under this house's standing rules. Claude
does not commit them without the naming lane's instruction, and "push those
through" from the owner is not the generator lane speaking about its own
output.

**3. `_latest` IS A MOVING POINTER.** A file named `_latest` is regenerated on
every run, so tracking it makes every regeneration a diff and every scan a
commit. That is a decision about the report pipeline, not a housekeeping fix,
and it is Codex's to make.

**THE REAL FIX IS PROBABLY NOT TO TRACK THEM.** It is for the AIF-136 intake
row to stop citing a generated `_latest` artifact and cite the GENERATOR
instead, or cite a dated snapshot that does not move. Claude did not make that
edit: rewriting another steward's intake row to change what it points at is a
content change, not housekeeping, and Codex cannot review it this week.

## WHOSE AREA

`docs/maintenance/AIF135*` and `AIF136*` and the AI Portal memory lane are
`member.ai.codex`'s, run `CODEX-20260826-014`. Claude's own lanes this session
were AIF-078 / AIF-120 (R129, AIF-137, AIF-138) and touched none of this
material's content.

## HOW TO VERIFY

    git log --oneline -1 -- docs/maintenance/AIF136_M4_COGNITIVE_DEMOTION_PROOF_V1.md

and re-run any commit: the cited-paths advisory should now list two widows
from the intake queue instead of six.

## HOW TO UNDO

    git rm --cached docs/maintenance/AIF135_MONOTONIC_AIF_ALLOCATOR_ALIGNMENT_V1.md
    git rm --cached docs/maintenance/AIF136_AI_PORTAL_MEMORY_TIERING_AND_RETENTION_PLAN_V1.md
    git rm --cached docs/maintenance/AIF136_M3_MEMORY_PILOT_RULING_PACKET_V1.md
    git rm --cached docs/maintenance/AIF136_M4_COGNITIVE_DEMOTION_PROOF_V1.md

The files stay on disk. Nothing was edited, so there is nothing to revert
inside them.

## ADDENDUM, SAME DAY -- THE FOUR DOCUMENTS EXPOSED THE WHOLE IMPLEMENTATION

Committing the four documents put them in the cited-paths gate's scope, and it
immediately reported **19 further widows**. They are not housekeeping.

`AIF136_AI_PORTAL_MEMORY_TIERING_AND_RETENTION_PLAN_V1.md` says *"M4 cognitive
pilot locally implemented and verified"*, and the implementation it names was
**not in the repository at all**:

    5 scripts  build_memory_storage_classification.py     29,722 B
               build_memory_storage_inventory.py          32,945 B
               build_portal_history_summary.py             3,681 B
               validate_memory_pilot_manifest.py           7,684 B
               verify_memory_pilot_recall.py               2,999 B
    3 schemas  portal_memory_{classification,inventory,pilot_manifest}_v1
    5 tests    ~23 KB
    1 registry labtalk/registries/aif136_memory_pilot_manifest_v1.json

**One disk, and the author is out until next week.** All 14 were committed on
the owner's authorization.

**The split is the tell.** Codex's AIF-132 tests ARE tracked; only the AIF-136
ones are not, which matches "commit pending" in the documents' own status
lines. He was mid-landing when the credits ran out.

**VERIFIED PER PATH, not assumed:** all 14 exist, ZERO CRLF, ZERO non-ASCII,
no `.gitignore` match (`check-ignore` rc=1), every `.py` parses under
`ast.parse`, every `.json` loads under `json.load`. `house-style` could not
have blocked them regardless -- `CHECKED_SUFFIXES` is `(".md",)` -- but they
are clean on that axis anyway.

**NOT DONE, SAID PLAINLY: THE TESTS WERE NOT RUN.** `pytest` is not installed
on the reviewing side, and running another steward's build scripts would
generate reports into his lane, which is his to do and was not authorized.
**A parse is not a pass.** Nothing here claims this implementation works; the
claim is that it is committable and should not sit on one disk for a week.

**A REVIEWER MISS, RECORDED RATHER THAN QUIETLY FIXED.** The first commit
verified the four documents were committable -- ASCII, headers, ignore rules,
envelopes -- and **never checked what they cited**. That is not
content-correctness, which was explicitly declined above; it is the same
committability check the reviewer believed was underway. The gate found in one
second what the review should have found first, and a plan document was
committed while pointing at nothing. The same shape as R75, one turn later and
by the same reviewer who had just written R75 into another document.

**STILL WIDOWED ON PURPOSE**, now five rather than two, on the same reasoning
as the section above: `PORTAL_HISTORY_SUMMARY_V1.md` (57 CRLF bytes), the
`memory_storage_*_latest.md` pair, and the `memory_storage_*_latest.json`
pair. Generated, self-declared do-not-hand-edit, and named for a moving
pointer.

## FOR CODEX, WHEN YOU ARE BACK

Two things are waiting for you and neither was decided in your absence:

1. **The five generated artifacts** -- the `_latest` .md and .json pairs and
   `PORTAL_HISTORY_SUMMARY_V1.md`. Track them (the three .md files need the
   CRLF fixed first), stop citing them, or cite something that does not move.
   The reasoning is above; the decision is yours.
1a. **Your implementation is now committed and was never run by the person who
   committed it.** 14 files, parse-checked and JSON-validated only. If any of
   it was mid-edit when your credits ran out, it is in the repository in that
   state. Check it before you build on it.
2. **AIF-135 is your lane and it is now more urgent than it was.** Its subject
   is aligning `claim-aif` and `next_aif` on monotonic high-water allocation.
   Claude allocated AIF-137 and AIF-138 through `claim-aif` on 2026-08-27 with
   no `--number`, both landed cleanly, and the claim files were verified
   present afterward -- because a `CLAIMED AIF-nnn` line that left NO claim
   file was observed on 2026-08-26 and is what your lane exists to fix. Two
   clean allocations are not evidence the defect is gone; they are two
   samples.
