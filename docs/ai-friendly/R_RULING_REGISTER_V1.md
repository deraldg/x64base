# R-NUMBER REGISTER V1

**The allocator of record for R-numbers.** Created 2026-08-24 after a near-miss:
a ruling was about to be stamped `R7` on the assumption that each AIF lane
carried its own R1..Rn series. It does not. **The R-space is one flat global
sequence**, and `R7` has been taken since 2026-08-06 -- the owner ruling on
AIF-090 ("CONVERT -- develop and document, it is our thesis").

Nothing detected that. There was no register, no allocator, and no gate; the
number was checked only because someone happened to grep. That is the same
shape this project keeps naming: **a claim that decays without ever going red.**

Run the allocator. Do not pick a number by eye. This file records WHAT each
number means; it deliberately does not record which number is next, because a
document cannot stay correct about that and the tool cannot be wrong about it.

```
$py12 tools\coordination\next_r.py
```

---

## THE ONE RULE THAT SURPRISES PEOPLE

**Doctrine and rulings share the sequence.** `R1` ("derivation runs downward
only"), `R6` ("an absent value must not be representable among present ones")
and `R7` (an owner ruling converting a single lane) are all in the same series.
Two different kinds of thing, one number space.

That was ruled deliberate rather than fixed. Splitting the space retroactively
would renumber citations that are already compiled into code comments, and a
citation that silently means something new is worse than an untidy sequence.
**So the KIND is a column here, not a range.** Read the column; do not infer
the kind from the magnitude.

| kind | meaning |
|---|---|
| `doctrine` | a general rule, expected to be cited far from where it was made |
| `ruling` | a decision about one lane or one artifact |
| `reserved` | cited somewhere in the tree, not yet attributed -- **burned, never reusable** |

**And one marker, which the gate reads:** a row that records what an
ALREADY-PASSED number meant must contain the token **`backfill`**. A row that
ALLOCATES a number now must be above the register's declared high-water and
needs no marker, because taking max+1 *is* the statement. This is the only
thing separating "R7 is a new ruling I just made" from "R7 is what that old
number always meant" -- citation evidence is identical in both cases, so the
human states it once, here.

---

## ALLOCATION RULES

1. **NEXT FREE IS max + 1, NEVER the lowest gap.** Gaps are reported by the
   allocator so a human can rule on them; they are never handed out. A reused
   number makes two decisions share an identity in a permanent record.
2. **A citation burns a number even when nobody can say what it meant.** If
   `R44` appears in the tree and no row here explains it, it is `reserved`, not
   free. You cannot safely reuse a number whose meaning you cannot find.
3. **Padding is display, not identity.** `R095` and `R95` are the SAME number.
   The register's earliest rows are zero-padded and later ones are not; the
   allocator normalises `R0*(\d+)` for exactly this reason. A tool that missed
   it would report a taken number as free -- the AIF-118 shape (the same answer
   for "absent" and "fine") inside the instrument meant to prevent it.
4. **Back-filling a `reserved` row into a real one is always welcome** and
   never changes the number. Mark it `backfill` so the gate can tell it from
   an allocation.
5. **An allocation is above the high-water. A back-fill is at or below it.**
   The gate enforces exactly that and nothing more -- it cannot read meaning,
   so it asks you to declare intent instead of inferring it.

---

## DECLARED

| R | date | kind | lane | ruled by | what it says |
|---|---|---|---|---|---|
| R1 | -- | doctrine (backfill) | AIF-078 | `member.derald` | Derivation runs downward only. |
| R2 | -- | doctrine (backfill) | AIF-078 | `member.derald` | A RUNG needs a PRODUCER, a COMPARATOR and a PERSISTER. |
| R3 | -- | doctrine (backfill) | AIF-078 | `member.derald` | Failure travels in the return value. |
| R5 | -- | doctrine (backfill) | AIF-078 | `member.derald` | One tree, one ladder -- two answers to one question IS the defect. |
| R16 | 2026-07-29 | ruling (backfill) | AIF-147 | `member.derald` | **SQL STATEMENT ORTHOGONALITY, AND IT IS A FAMILY.** `R16` -- a statement reads the table named in FROM and nothing else; `R16b` -- statements ignore session state, every cursor saved and restored around a statement (`sqlsel_statement.cpp:321`); `R16d` -- an expression that cannot be projected is REPORTED, never silently mis-projected (`:88`, `:390`). One ordering model, shared with relation equality (`:103`). Declared 2026-08-30, THIRTEEN MONTHS-EQUIVALENT after the fact: it has governed shipped source since 2026-07-29 (`sqlsel_statement.hpp:16`, first commit `38a9631fe`) and had NO REGISTER ROW, which AIF-147 flagged on 2026-08-28 without a direction being chosen. **IT DECIDED A DESIGN QUESTION WHILE UNDECLARED**, which is why it is being rowed now rather than tidily: asked on 2026-08-30 which surface should carry workspace relations, the answer fell out of R16 itself -- a workspace is SESSION STATE, and R16b says statements ignore session state, so making SQLSEL workspace-aware would break the one property it is built around. REL is the opposite by construction (current parent, current area, cursors that move), and measurement agrees: `set_relations.cpp`, `cmd_relations.cpp` and `cmd_ersatz.cpp` all know about workspaces and NO SQLSEL FILE MENTIONS ONE. **RULED THE SAME DAY: NEITHER SURFACE TAKES IT -- THE WORKSPACE JOINS THE NAME**, resolved by the relation managers as `<alias> IN <ws>` (the spelling R131 already shipped for `SET PATH <slot> <path> IN <ws>`, and which `USE <t> IN FREE` also uses; a dotted `<ws>.<alias>` was rejected because it collides with `alias.field`). So SQLSEL can cross workspaces later WITHOUT ever reading session state, because the statement still names everything it touches -- R16 survives by construction rather than by exemption. Ships review-needed. |
| R132 | 2026-08-30 | doctrine | AIF-147 | `member.derald` | **A LANE MAY OWN ITS OWN R-NAMESPACE, AND THE AIF-120 GUI LANE DOES.** The R-space is one flat global sequence (this register's own founding sentence), and the GUI lane has been numbering independently inside it: `docs/maintenance/AIF120_BOUND_WIDTH_RULING_V1.md` and `AIF120_COMPOSITION_RULE_V1.md` cite TWELVE numbers -- R6, R7, R12, R13, R16, R17, R20, R22, R24, R25, R29, R30 -- measured 2026-08-30. **TWO OF THEM, R6 AND R7, ALREADY CARRY DOCTRINE ROWS IN THIS REGISTER**, and a third, R16, is the SQL orthogonality family declared in the row above. **THE COLLISION IS NOT ONE NUMBER AND MUST NOT BE FIXED ONE NUMBER AT A TIME.** Renumbering the one someone tripped over is the half-fix this house keeps naming; renumbering all twelve edits a lane that is mid-flight and buys stale citations. So the lane owns its namespace instead -- the pattern already shipping for catalogs, which the normalization gate prints every commit (`edref owns its namespace`, `pshell_ref owns its namespace`) and does not fail. **DATING SETTLED WHICH SIDE MOVES:** SQL's R16 is 2026-07-29 in shipped source; the GUI's is 2026-08-19, three weeks later. **NOT YET ENFORCED, AND THAT IS STATED RATHER THAN IMPLIED:** `next_r.py` still counts GUI citations in its 129, so the advisory backlog is unchanged by this row. The scan change is the follow-up and was deliberately NOT made in the same pass -- editing the allocator everyone depends on, at the end of a long session, to make a number tidy is how a gate breaks for everybody. Ships review-needed. |
| R133 | 2026-08-30 | doctrine | AIF-120 | `member.derald` | **A SUBSET OF AN EXISTING LANE IS GRANDFATHERED INTO ITS NUMBER. DO NOT MINT A NEW ONE.** Owner: *"we need to stop creating new aif numbers for things that are subsets of things that have aif numbers -- they are grandfathered."* Ruled while deciding where a five-part MINIDB hydration defect should land: the steward had offered a new lane or an amendment, and the amendment is now the rule rather than the choice. **THE TEST IS OWNERSHIP, NOT NOVELTY.** A finding is a subset when the files it lives in already declare a lane -- `include/dottalk/minidb_hydrate.hpp` and `include/dottalk/minidb.hpp` both carry `lane: AIF-120` in their own headers -- or when an existing row already charters the surface. Being newly discovered does not make a thing a new lane. **WHY THIS RULE ARRIVED TODAY AND NOT EARLIER, and the timing is not a coincidence:** AIF-135 was ruled the same morning and made allocation `max+1`, which removes the last friction from minting a number. A cheap allocator needs a rule about when NOT to allocate, or the register fills with rows that are paragraphs of a document nobody assembled. The AIF register is a map of LANES; a lane with one finding in it is not a lane, it is a sentence in somebody else's. **AND IT HAS A COUNTERWEIGHT THAT MUST BE STATED, because grandfathering can hide a defect as easily as it can file one:** a subset amends the parent row, it does not vanish into it. The amendment carries its own measurements, its own citations and its own NOT-FIXED line, so a reader of the parent lane finds the finding rather than a reference to a conversation. **THE FAILURE MODE TO WATCH:** a parent lane whose charter FORBIDS what the subset needs. Measured the same hour -- `minidb_hydrate.hpp:56` and `cmd_workspace.cpp:3204` cite **AIF-108** for an engine behaviour, and AIF-108's status column reads *chartered -- NO engine change proposed*. A subset grandfathers into a lane that can CARRY it; when the nearest lane forecloses the work, that is the one case where a number is the honest answer. Ships **review-needed** -- the author does not self-approve. |
| R6 | -- | doctrine (backfill) | AIF-078 | `member.derald` | An absent value must not be representable among present ones. |
| R7 | 2026-08-06 | ruling (backfill) | AIF-090 | `member.derald` | CONVERT -- "develop and document, it is our thesis". Skill programme retired unbuilt after P0 falsified its premise; the four defects P0 found were repaired instead (`79888dfaa`). |
| R110 | -- | ruling (backfill) | AIF-120 | `member.derald` | Workspace path depth: keep `WorkspacePath`, and strike the reason given for keeping it. |
| R112 | -- | ruling (backfill) | AIF-120 | `member.derald` | The measured-zero gate; first-wins-plus-warning admissible ONLY as instrumented behaviour. |
| R113 | -- | ruling (backfill) | AIF-120 | `member.derald` | Order and functions (with R114). |
| R114 | -- | ruling (backfill) | AIF-120 | `member.derald` | Order and functions (with R113). |
| R120 | 2026-08-24 | ruling | AIF-078 | `member.derald` | **The GUI's positional rung IS the engine slot.** Step 3 of the slot lane. The GUI's ordinal was the index into its own area list while the CLI's was the engine slot, so one area had two positional addresses and the posture line `AREA <n>` meant a different thing depending on which surface wrote it. The ordinal is now the slot: sparse, stable across another area's close, and the same number both surfaces print. Costs recorded rather than discovered: the survivor of a close KEEPS its number instead of being renumbered, the vacated slot becomes a hole the next open falls into, and GUI-written postures from before this change carry list indices that will now be read as slots. |
| R124 | 2026-08-24 | ruling | AIF-078 | `member.derald` | **THE RELATION WIRE RECORD LIVES IN `xbase`.** Implementing R122. The posture grammar `RELATION <p> <c> ON <csv> [TO <csv>]` already exists, round-trips, and is held by `dottalkpp_relation_merge_test` -- the format needed no design. The obstacle was PLACEMENT: the formatter is `relation_parse.cpp` in `src/gui`, and `dottalkpp` does not link `dottalk_gui_core`, so the producer could not reach it. Ruled to a neutral home both targets already link, rather than cherry-picked into the CLI (which would invert the dependency direction R122 just ruled and grow the cherry-pick list `area_alloc.hpp` refused to grow on 2026-08-23) or given its own target (correct but ceremony). The unit is std-library-only end to end -- `relation_parse.cpp` -> `relation_parse.hpp` -> `model.hpp` -> `<cstdint> <filesystem> <optional> <string> <vector>` -- and the build ALREADY proves it portable: `src/tests/CMakeLists.txt:43` compiles it into a target that deliberately does not link `dottalk_gui_core`. Same precedent as `area_alloc.hpp` and `workspace_membership.hpp`; `src/xbase` globs its sources so neither consumer needs a build change. |
| R125 | 2026-08-24 | ruling | AIF-078 | `member.derald` | **THE WIRE RECORD CARRIES THE WORKSPACE HANDLE; A NAME IS A RENDERING.** This CLOSES step 2 of `claude/GUI_LAYER_DECISION_OUTLINE.md` -- the NAME-versus-HANDLE seam that has been live since I1.2 partitioned the relation store by handle while the GUI carried a name in six places with nothing converting. The outline asked for the conversion to happen ONCE, IN A NAMED PLACE. The wire record is that place: the producer emits the handle, and a name is produced for DISPLAY by `xbase::workspace::name_of()`, which already exists and which the GUI already calls in exactly one place (`session.cpp:508 owning_workspace_now`). R6 is satisfied by construction and not by a new convention: the handle space already reserves 0 as not-a-legal-handle (`workspace_membership.hpp`, `kDefaultHandle = 1` "precisely so that 0 can mean" absent), so an absent workspace cannot be spelled as a present one. Ruled together with R124 rather than separately, because a record shipped before this was decided would have had a workspace field meaning whatever its first writer put there -- the GUI's existing defect, relocated one layer out. |
| R122 | 2026-08-24 | ruling | AIF-078 | `member.derald` | **`src/gui` DOES NOT DEPEND ON `src/cli`. THE PROCESS BOUNDARY IS THE INTERFACE.** Seam B of `claude/GUI_LAYER_DECISION_OUTLINE.md`, ruled NO. The GUI reaches the engine by SPAWNING a `dottalkpp.exe` (`gui_shell_runtime.cpp:287` CreateProcessW, `gui_cli_bridge.cpp:242` _popen) and parsing its stdout -- there is no in-process command path at all, which the outline did not know when it framed the choice. So the fix for defects 1-4 of its bill (the ` TO ` clause, the posture round trip, the flattened tree, `n/a` becoming a measured `0`) is NOT to link the engine but to make the producer EMIT STRUCTURED DATA across the boundary that already exists, and to consolidate the three parse sites into one DELIBERATE boundary rather than three accidents. B1 was measured and does not collapse -- `set_relations.cpp` takes its engine by injection -- but it costs a `workarea_util` split, a `shell_engine` shim, `command_output` and `db_tuple_stream`, and buys a dependency direction that is hard to walk back. |
| R123 | 2026-08-24 | ruling | AIF-078 | `member.derald` | **THE GUI'S MATCH COUNTER IS DELETED; ABSENT IS SHOWN AS ABSENT.** `count_relation_matches` (`session.cpp`) diverged from `relations_api::match_count_for_child` four ways -- it counted DELETED rows, scanned unbounded, walked physical order instead of the active index, and refused multi-field keys -- so one relation had two match counts and nothing on screen said which one a cell held. Under R122 it cannot be fixed in place: the count is a computation over the GUI's OWN open areas at its OWN cursor positions, which no subprocess can answer without replicating the state. A number that disagrees with the engine is worse than no number. `MaybeMatchCount` and the `n/a` state ALREADY EXIST, so this needs no new type and no link -- R6 was already satisfied and the code was declining to use it. |
| R121 | 2026-08-24 | ruling | AIF-123 | `member.derald` | **ADDRESSING IS ABSOLUTE, TRAVERSAL IS FILTERED.** `GO <n>` names a record and must land on it under any setting: a `GO` that skipped forward would make the one command whose purpose is reaching a specific record unable to reach it, and would close the only door to navigating onto a flagged row to `RECALL` it singly. `SKIP` / `TOP` / `BOTTOM` name a POSITION IN A SET -- next, first, last -- and that set must be the visible one, so they honour `SET DELETED` as they already honoured `SET FILTER`. Ruled on principle and NOT on dialect precedent: the xBase family is honoured but not binding (steward, 2026-08-24), and the only document in the tree answering the `GO` question -- `include/foxpro_go.hpp` -- turned out to be unreviewed generated prose that contradicts itself. The defect was ONE PREDICATE: `navsel::resolve_mode` chose the logical view by asking only whether a SET FILTER was active, so `SET DELETED` -- the second reason the logical view differs from the raw order -- never reached traversal. Everything downstream was already wired. |
| R119 | 2026-08-24 | ruling | AIF-078 | `member.derald` | **`autoq_next` is RESERVED AND UNWIRED.** The x64 header sequence slot stays load-only; the catalog keeps `max(WS_ID)+1` under a FLOCK. Stamped R119 and not R7 because R7 was already taken -- the near-miss that caused this register to exist. |

R4, R8-R109 excluding those above, R111 and R115-R118 are **cited in the tree
and not yet attributed here.** They are reserved by rule 2. Back-fill them as
they are identified; the allocator reports them every run so they do not go
quiet.

---

## RESERVED BY CITATION

**This section deliberately states NO numbers.**

Every `Rnnn` citation anywhere in the scanned tree burns that number, whether or
not a row above explains it. The allocator derives that set on every run. It is
not copied here, and it must not be: a hand-copied population is a hardcoded
denominator, and this project has already paid for one --
`ENTRY_PATH_BASELINE = 127704` in `recall.py`, a frozen number that made a
metric flattering and made the bound written to catch it unable to fire
(AIF-090 D2).

The first draft of this file did exactly that. It stated "highest taken R118,
next free R119" and listed the gaps by hand. **The allocator's first run
contradicted all three claims** -- it found R0, R90, R91, R94, R97, R98, R100,
R104 and R106 cited, which the hand-count had called gaps. The instrument was
right and the document was wrong, roughly ninety seconds after the document was
written. That is the whole argument for deriving it, and it is left here rather
than quietly corrected.

To see the current population:

```
$py12 tools\coordination\next_r.py
```

**A known artifact, stated so nobody re-investigates it:** the scan matches
`\bR0*(\d{1,3})\b`, so `R0` and some low numbers may be incidental text
rather than real citations. They are still burned. Over-reserving costs
integers, which are free; under-reserving costs a collision in a permanent
record, which is not.

---

## GATE

`tools/coordination/r_collision_gate.py`, wired into the pre-push gate beside
the AIF collision gate.

- **HARD:** a duplicate R in the DECLARED table. Two rulings cannot share one
  identity.
- **HARD:** a newly declared number **at or below the register's own declared
  high-water with no `backfill` marker.** That is a decision made now claiming
  a number that has already passed -- the R7 shape exactly.
- **ADVISORY:** numbers cited in the tree with no declared row. There are
  roughly a hundred of these and they predate the register. A gate that blocked
  on them would be switched off within a day -- the same reasoning that keeps
  `check_open_items.py` advisory and that kept `check_aif_claimed.py` to ADDED
  rows only.

**The hard check was wrong twice before it was right, and both are recorded in
the gate's own docstring.** The first cut compared a new number against the
whole working tree, which would have failed the correct flow of declaring and
citing in one change. The second compared against files the change does not
touch -- and blocked this register's own seeding commit, because
**citation cannot separate theft from back-fill at all.** R7-declared-fresh and
R7-declared-historically produce identical evidence. The check was measuring
something that does not carry the answer. Hence the marker: the gate stopped
guessing and asked.

`tools/coordination/test_r_gate.py` -- 11 fixtures, five of which must go red
before their green means anything.

---

**Owner:** `member.derald` -- **steward:** `member.ai.claude.cowork`
**Status:** review-needed. The author does not self-approve.
| R126 | 2026-08-25 | ruling | AIF-128 | `member.derald` | **AN IDENTITY NUMBER IS AN INTEGER; THE ZERO PADDING IS DISPLAY.** Readers match loosely (`AIF-0*(\d+)`, any width, any padding) and normalise to int, so `AIF-43` and `AIF-043` are ONE number; writers keep `%03d`, which is a MINIMUM width and widens by itself past 999; NOTHING in the corpus is re-padded and every existing citation stays valid. Ruled after AIF-128 found `\d{3}` in the collision gate. REJECTED: six-digit padding (answers a display question, costs a rewrite of every citation plus 60 claim filenames, and still needs the loose matcher during any transition) and a real (wrong type for an identity, and there is nowhere to put it -- no DBF has an AIF column; identity needs equality and max() and nothing else). **THE FORMATTER WAS NEVER THE CEILING AND I SAID OTHERWISE:** `%03d` renders 1000 as AIF-1000 quite happily -- the real stop was `AIF_LO, AIF_HI = 6, 999` in session_coordinator, one line from the formatter that had been checked, where `claim-aif` would have returned no candidate at AIF-999. Raised to 999999 with a LAZY candidate generator so the wider bound costs nothing. **NINE READERS MEASURED AGAINST AIF-1000:** seven NO MATCH, one correct (next_aif, which already carried this exact reasoning in a comment and was the only one to have it), and seed_tracking returned `AIF-100` -- not a decline, a DIFFERENT ALREADY-TAKEN NUMBER. The sibling R sequence had the same ceiling and is CLOSER to it (R125 against AIF-128). **THE WIDENING'S OWN FALSE POSITIVE WAS MEASURED, NOT ASSUMED:** widening can only ADD matches, so every newly-matchable token was enumerated -- zero for R across all ten SCAN_DIRS, and exactly one for AIF, which was real. A 2026-08-15 closeout writes `AIF-11{6,7}.claim` as brace shorthand for the PAIR 116 and 117; `{` is a non-word character so `\b` matched `AIF-11` and resolved it to AIF-011. The old pattern missed it BY ACCIDENT; the new one declines it ON PURPOSE via `(?!\{)` on the three PROSE scanners only -- row-anchored patterns need it not, a row id being line-anchored and pipe-followed. **A NEAR-FINDING THE MEASUREMENT REFUTED:** the two allocators do use different rules (next_aif max+1 and never a gap; session_coordinator lowest-free) but `taken()` returns 128 numbers with NO gaps below the high-water mark, so both answer 129 today. Latent, not active; recorded, not filed. Full record `docs/maintenance/R126_IDENTITY_NUMBERS_ARE_INTEGERS_RULING_V1.md`. Ships **review-needed** -- the author does not self-approve. |
| R127 | 2026-08-25 | ruling | AIF-068 | `member.derald` | **WHAT A COMMAND-REFERENCE PAGE IS COMPOSED OF.** Three parts. **(a) CATALOGS: DOT + FOX + UI + DEV compose a developer command page; EDU, EXT, INTERNAL and ED do not** -- they are separate surfaces (`EDU\|BIBLETALK` is 125 lines of lesson material) and are cross-referenced rather than silently dropped. FOX earns its place despite three-line stubs: `FOX\|AVERAGE` carries the full xBase SYNTAX form that `DOT\|AVERAGE` lacks entirely. Asked because the generator keys each page on ONE TOPICKEY while a command's evidence is split across catalogs -- **not two subjects, one command mined twice**: `DOT\|BOOLEAN` cites `edu_boolean.cpp:79 usage_output_block`, `EDU\|BOOLEAN` cites `edu_boolean.cpp:18 usage_contract`, same file, two collectors. Measured: 160 DOT topics have a same-name sibling, 29 have a sibling with MORE lines; BROWSETV would have shipped 3 of 47. **(b) A SOURCE CONTRACT MAKES A COMMAND SUPPORTED -- UNLESS ITS OWN status= SAYS OTHERWISE** (owner: "once a source file has a contract it is supported"). Measured: 215 topics carry `pattern=usage_contract`, 186 marked SUPPORTED, **29 not -- and FIFTEEN of those declare `status=supported` or `supported-conditional` in their own contract while HELP_TOPIC.SUPPORTED says false.** Two answers to one question in one store, the R5 shape; the rule is not policy being imposed, it is the store made to agree with itself. But a contract is not a blanket -- `experimental` (DOT\|TRANSACTION), `sample-extension` (EXT\|STUDENT*), `backend-helper` (EDU\|IDX), `implementation-present` (EDU\|SIX) are the contract's OWN words for not-that. **So the contract makes the MARKER authoritative; the marker, not the contract's presence, decides.** **(c) `developer` IS AN AUDIENCE, NOT A DENIAL OF SUPPORT** (owner: "but that doesn't mean its not dev though"). UI\|BROWSETV, UI\|GENERIC, DEV\|HIER and the four INTERNAL\|*_BUFFER topics all carry contracts and all say developer: supported AND developer, orthogonal axes. A page absorbing a developer-marked sibling must carry that forward -- a developer tool presented as general-audience reference is a worse defect than a thin page. **NOT RULED, AND NAMED AS A DEPENDENCY: the `status=` vocabulary is uncontrolled -- NINETEEN distinct spellings** (supported, supported-conditional, supported-stub-mixed, active, implementation-present, implementation-shim, implementation-helper, dev-tool, developer, dev-canary, experimental, stub, deprecated, deprecated-compat, compatibility-alias, document-control-readonly, backend-helper, sample-extension, review-needed). Part (b) makes that string load-bearing, so it now needs a closed set; separate lane. Also not ruled: whether HELP_TOPIC.SUPPORTED is repaired at source or the contract read at render time. **SCOPE: rule now, apply to the 20** -- the other 26 sibling-richer cases are a measured backlog inheriting the answer; the 191 accepted pages are NOT reopened. **The renderer is NOT touched** -- `_render_page` derives its header from the topic dict it is handed, so composition is the generator's job and `command_reference_candidate.py`, shared with all 191 accepted pages, is unchanged. Full record `docs/maintenance/R127_COMMAND_PAGE_COMPOSITION_RULING_V1.md`. Ships **review-needed**. |
| R128 | 2026-08-26 | ruling | AIF-078 | `member.derald` | **LOAD IS ADDITIVE, AND CLOSE AND SAVE ARE SCOPED TO ONE WORKSPACE.** Opening a workspace no longer closes the one you were in: LOAD from a saved posture and OPEN of a directory both stop being replacement-style, and two directories opened are two workspaces, each holding its own areas. CLOSE and SAVE scope to ONE workspace by default, with an explicit form for everywhere. **THIS ENDS F2 BY DECISION, NOT BY MEASUREMENT.** R110 kept `WorkspacePath` and STRUCK its stated reason because carrier and payload were measured never to coexist -- which held only because LOAD replaced. From here two workspaces can be populated at once on both surfaces, and the type R110 left justified by nothing has its reason back, from THIS decision and not from container nesting (the justification R110 examined and rejected). It also settles the ambiguity `claude/THREE_LANES_INTEGRATION.md` sec 3 left open -- whether the log line `Workspace: mcc_db` named the CATALOG ROW or a RUNTIME HANDLE: **it is both**, a load names the workspace it loads into. **MOST OF THE ENGINE HALF ALREADY SHIPPED** at AIF-078 stage 3: NEW / SWITCH / REGISTRY / DESTROY; bare CLOSE already scoped via `close_workspace_tree` rather than a `MAX_AREA` sweep, so **selective close is done at the CLI**; `WORKSPACE ADD` already additive; `find_free_area_for_workspace` and `join(handle, slot)` already workspace-aware. **AND IT COSTS NO NEW DEPENDENCY:** the whole membership API (`create`, `set_current_handle`, `find_by_name_ci`, `members`, `destroy`) is header-only in `include/xbase/workspace_membership.hpp` and both targets already link `xbase`, so the GUI reaches it without linking the CLI -- **R122 is not touched**, same precedent R124 used for the relation wire record. **FOUR THINGS THIS MAKES WRONG THAT ARE NOT WRONG TODAY.** **(1) SAVE HAS NO WORKSPACE DISCRIMINATOR, AND IT IS LIVE NOW:** `schema_save_to_string()` (`cmd_workspace.cpp:1828`) enumerates `for (int area0 = 0; area0 < xbase::MAX_AREA; ++area0)` with no handle filter anywhere in it. Stage 3 shipped 2026-08-24, so a CLI session can ALREADY hold areas in two workspaces -- which means `WORKSPACE SAVE mcc_db` today writes the other workspace's areas into a posture named `mcc_db`. THE COUNT DISCIPLINE exactly: a number taken from an authority holding more than one KIND with no discriminator applied. The ruling does not create this defect; it removes the last reason it was rarely reached. It is ONE place -- file writer, memo carrier and MINIDB container all pass through that single serializer, so scoping it once scopes all three. **(2) THE GUI CANNOT BE IN A WORKSPACE AT ALL:** `src/gui` reads `xbase::workspace::current_handle()` in three places (`session.cpp:511`, `:577`, `:1664`) and calls `workspace::create` or `set_current_handle` in NONE. So `WS: DEFAULT` on every area after a load is a TRUE report for a stronger reason than sec 3 knew -- not that the load forgot to enter a handle, but that the GUI has no way to be anywhere else. And there are THREE close-all-and-clear sites, not the two sec 4 cited: `session.cpp:1797` (mirror OPEN dir), `:2051` (posture mirror), `:2760` (the Command box `workspace close`). The third closes every GUI area unconditionally while the CLI verb it shadows has been scoped since stage 3 -- two surfaces, one verb name, two meanings. **(3) THE ADDITIVE VERB CANNOT TAKE A DIRECTORY:** `WORKSPACE ADD` is single-table by construction and refuses the case in as many words at `:4851` -- **and the grammar this raised was RULED THE SAME DAY: OPEN ITSELF BECOMES ADDITIVE** (owner: *"open should be additive or it will kill the other workspaces, if a person wants it open by itself then they can close all of the other workspaces first like a sane person"*). Replace is not re-spelled anywhere, it is COMPOSED -- `WORKSPACE CLOSE ALL` then `WORKSPACE OPEN <dir>` -- because **the two mistakes are not equally recoverable**: an additive open the user wanted exclusive costs one extra command, while a replacing open the user wanted additive closes every area in every other workspace unasked, and a dirty one is not recoverable by typing anything. When one error is a keystroke and the other is data, the default belongs on the keystroke side. `ADD` is NOT deleted: OPEN and ADD stop differing in ADDITIVITY (both add) and differ only in GRANULARITY (directory vs one table), which is a better seam -- a verb pair split on *does this destroy your session* is a trap, one split on *how much do you want* is a menu. **A NEW CONSEQUENCE, not ruled:** the second OPEN of an already-open directory. `WORKSPACE NEW` refuses a duplicate name outright (`find_by_name_ci(nm) != 0`, `:4480`), so a second `OPEN` would either refuse or uniquify, and neither is right -- refusing makes a harmless act an error, uniquifying puts two workspaces on one directory. It should RE-ENTER the workspace that directory already has, a second open of the same place being a navigation rather than a creation; what it reconciles in the CONTENTS is the open question. **(4) TWO SURFACES SAY A NAME CANNOT BE RECLAIMED, AND IT CAN:** the header block at `:175-179` and the USAGE output a user actually reads at `:4310-4311` both say there is no DROP verb. Stale since `WORKSPACE DESTROY` landed 2026-08-23 (D10.3, `:4535`, calling `xbase::workspace::destroy` at `:4623`), with `WSL_T4` proving reclamation directly -- destroy `WSLADR1`, create it again, second WS_ID must be GREATER; in-suite 2026-08-24 at 156 -> 157. **And the correction for the line four rows above it DID land:** `:4307-4308` self-corrects the NEW claim and is dated 2026-08-24, so someone walked this exact block that day, fixed one claim and left the DROP claim standing beside it. Third contract drift in the comment block that exists to prevent contract drift, which records the previous two in its own text. Load-bearing, not trivia: under additive open a workspace is created per opened directory, and whether closing one RETIRES it decides whether the second open of the same directory gets a fresh workspace or silently ADOPTS the first one's identity. **NOT RULED, named so they are not settled by drift:** what a second OPEN of an already-open directory reconciles (above); what NAMES a workspace on `OPEN <dir>`, given names must be unique and a directory leaf collides across roots; whether a per-workspace close also DESTROYs; how SAVE spells everywhere (CLOSE's grammar argues `ALL`); relations, still cleared GLOBALLY on a scoped close (`:180`), which additive load makes reach across workspaces routinely rather than rarely; and the UI shape THREE_LANES sec 5 proposed. **NO CODE WAS WRITTEN.** `src/cli/**` and `src/xbase/**` are engine and want an explicit go; `src/gui` waits on the grammar, because the grammar decides what the GUI is mirroring. **NOTHING HERE WAS RUN:** there is no current build on the authoring side, so every claim is source read at HEAD ad34e9145 and none is runtime-proven. Full record `docs/maintenance/R128_ADDITIVE_LOAD_AND_SCOPED_CLOSE_SAVE_RULING_V1.md`. Ships **review-needed** -- the author does not self-approve. |
| R129 | 2026-08-27 | ruling | AIF-078 | `member.derald` | **THE WORKSPACE CURSOR: A CURSOR THAT NOTHING ELSE OBEYS IS NOT A CURSOR.** Owner ruling, part 1 only: *"there needs to be a workspace cursor, we have an area cursor and the table/row cursor already I think"*. **ALL THREE CURSORS ALREADY EXIST AND THEY DO NOT NEST THE SAME WAY.** Row cursor `DbArea::recno()` (`include/xbase.hpp:353`) is a member, one per area, and nests correctly. Area cursor `Engine::_current` (`:623`) is ONE `int`; `selectArea()` (`:615`) range-checks `0 <= idx < MAX_AREA` and assigns, which is the entire body. Workspace cursor is `WorkspaceTable::current_` (`include/xbase/workspace_membership.hpp:163`, forwarder `:469`). **SO THE RULING IS NOT "BUILD ONE", IT IS "FINISH THE ONE THAT IS THERE"** -- its own comment states the half it has: *"The workspace a newly opened area joins,"* which answers WHERE DOES THE NEXT THING GO and not WHICH ONE AM I LOOKING AT. **THE ASYMMETRY: the row cursor nests under the area cursor; the area cursor does NOT nest under the workspace cursor.** **NAME RESOLUTION IS WORKSPACE-BLIND ON FOUR PATHS** -- `find_open_area_by_name_ci` (`workarea_util.hpp:53`, **36 call sites**, 21 depending on first-match-wins, and the only INSTRUMENTED one), `find_open_area_by_alias` in `cmd_use.cpp:471` and again in `cmd_codasyl.cpp:287`, and `resolve_area_by_alias` in `browser_builders.cpp:159`. `cmd_select.cpp` scans and calls `selectArea()` at `:200` with no filter of its own. **THE R5 DIVERGENCE, RUNTIME-PROVEN 2026-08-27:** two authorities answer "which workspace am I in" -- `current_handle()` and `area(currentArea()).wsHandle()` (`xbase.hpp:300`, stamped via `dbf_file.cpp:231`) -- and they disagree after `WORKSPACE SWITCH` (`cmd_workspace.cpp:5151`), after `WORKSPACE OPEN` (`:4593`, `:4625`) and after a scoped `close_workspace_tree` (`:1524`), none of which calls `selectArea()`. **MEASURED LIVE: `SELECT students` standing in WSX32 (handle 3) selected area 8, which is WSX64's table, while WSX32's own STUDENTS sat at area 21, and the session said nothing.** After that command `current_handle()` reads 3 and `area(currentArea()).wsHandle()` reads 2. **R112 ALREADY RULED THE AMBIGUITY HALF AND R128 ARMED ITS TRIPWIRE.** R112 sec 6a admits first-wins-plus-warning only as an instrumented phase whose counter must reach a measured zero (`workarea_util.hpp:41-52`); `record_ambiguity()` (`workarea_util.cpp:102`) latches per (name, site) and records **`ws_handles` beside `engine_slots`** (`:120`), and `WORKSPACE REGISTRY` prints it even at zero (`cmd_workspace.cpp:4740-4757`) so that "no collision" and "not instrumented" cannot look alike. `cmd_regression.cpp:465` called that ledger **"a TRIPWIRE for AIF-078 stage 4, not a migration counter,"** STRUCTURALLY ZERO until two workspaces could be open at once -- **R128 landed 2026-08-26, the tripwire was armed for ONE DAY, and it fired on the first attempt.** **THE RESOLVER ALREADY HOLDS THE WORKSPACE DATA AND DOES NOT FILTER ON IT.** **THE RUN ALSO FOUND A DEFECT THAT IS NOT THIS RULING'S SUBJECT AND IS NOW AIF-137:** the ledger fired on the second `WORKSPACE OPEN` BEFORE ANY TABLE NAME WAS TYPED, resolving BUILDING from handle 3 to workspace 2, with `REL LIST` reporting an EMPTY store -- the relation STORE is partitioned by workspace (I1.2) and the relation PARENT is not. Separate finding document; R129 cites it and stops carrying it. **AND THE INSTRUMENT IS BLIND TO HALF OF WHAT IT MEASURES:** the STUDENTS ledger row is tagged `REL refresh parent`, not a select site, because `cmd_select.cpp` records nothing -- so had SELECT crossed on a name the refresh did not touch, the counter would read zero while a crossing occurred. **A counter blind to one of its own crossing paths cannot retire the migration phase, and its zero would mean "not instrumented" while looking like "clean"** -- the AIF-118 shape reappearing one level inside the instrument whose print-even-at-zero rule exists to prevent it. One `SELECT` caused four cross-workspace resolutions, and per the above that four is a FLOOR. **RECONCILED WITH AN EXTERNAL OWNER-ACCEPTED PACKET RATHER THAN COMPETING WITH IT.** `WORKSPACE_IDENTITY_AND_CATALOG_PRECEPTS_V1.md` (AIPR-20260827-GROK-001, co-authored member.derald + xAI Grok, owner-accepted 2026-08-27) P6 rules unqualified names to the current workspace, with `SALES:STUDENTS.FNAME` as the qualified form. **THE SECTION OF THIS RULING THAT SAID THE SAME THING IS STRUCK, NOT KEPT-AND-AGREED:** two authorities that agree today diverge silently the first time one is amended, which is the R5 shape one layer up from the code. **P6 ALSO CLOSES Q8**, open in `include/reference/data_address.hpp` since 2026-07-30 -- *"does an unqualified name walk up ancestors? proposed: NO ... must be ruled before any depth > 1 is resolved"* -- whose four named blockers (runtime registry, containment invariant, cycle guard, depth cap) all landed at AIF-078 stage 3. **WHAT P6 LEAVES FOR THIS RULING: P6 governs RESOLUTION and secs 6.1/6.2 govern NAVIGATION, so P6 IS LOAD-BEARING ON A CURSOR MODEL IT DOES NOT STATE** -- if a bare name resolves in the current workspace while the area cursor may sit in a foreign one, "the current workspace" has two computable answers at the moment you ask. **BOTH REMAINING QUESTIONS WERE RULED BY THE OWNER ON 2026-08-27, AFTER THE MEASUREMENT AND AFTER THE EXTERNAL ANSWERS** -- *"I think all in 6.x are valid, especially allowing an empty workspace, there will be times we want to open and add to it. You have to have a place to start."* and *"6.2 is valid"*, with the risk accepted in as many words: *"If it turns [out] wrong we will find out quickly."* **THE ORDERING IS THE POINT: the first cut of 6.2 proposed a blanket refusal and would have been ruled WRONG, because the transcript showed the case was a LOCAL name being ignored rather than a boundary policy being absent. A ruling made before its measurement would have shipped the wrong rule with the same confidence.** **6.1** `WORKSPACE SWITCH` moves `_current` to the target's lowest member so the two authorities agree, and an EMPTY workspace is a legal position -- this ruling's proposed refusal of such a switch is WITHDRAWN as over-engineering. **TWO ARGUMENTS WERE GIVEN FOR THE EMPTY WORKSPACE AND THE OWNER'S IS THE LOAD-BEARING ONE:** the external reviewer argued from the invariant (**I1 forbids a NULL workspace, not an EMPTY one**), the owner from use (*"you have to have a place to start"*) -- so an empty workspace is not a tolerated edge case to be survived, it is **the intended starting state for building one up**, which is what moves AIF-138 from LATENT to ON THE PATH: the engine has no value for a position a user is now INVITED to occupy. **6.1a rules that the position EXISTS and is LEGAL and deliberately does NOT rule how the cursor SAYS it** -- sentinel, `optional`, or a `hasCurrent()` predicate -- because choosing a representation before counting the `currentArea()` readers is exactly what produced the two withdrawn answers. **AND THE CONSEQUENCE THE OWNER WILL SEE FIRST IS NAMED RATHER THAN LEFT TO BE DISCOVERED: in an EMPTY workspace 6.2's arm 1 can NEVER apply**, since nothing is present locally, so every name refuses or misses -- correct under the ruling, and the section to reopen if it reads badly in use; **6.2** `SELECT` resolves to the current workspace's member per P6, refusing only when the name is ABSENT here and PRESENT elsewhere -- this ruling's proposed blanket REFUSE is **WITHDRAWN**, having reasoned about the crossing when the question was resolution, and the measured transcript is the local-name case rather than a boundary-policy case. **THE REFUSAL ARM DOES NOT MEAN "PRINT AN ERROR" ON THE ENGINE'S OWN LOOKUPS:** P6 is a RESOLVER rule, so it governs internal resolutions too, but an internal refusal has nobody to tell -- for `refresh_from_parent_name()` the correct behaviour is simply to find NO parent and return, which is what closes AIF-137. Said explicitly because "refuse" reads as "diagnose", and a diagnostic on the refresh path would be wrong. **A NEW QUESTION THE ANSWERS SURFACED -- 6.1a -- AND IT TURNED OUT NOT TO BE A WORKSPACE QUESTION AT ALL, WHICH IS THE FINDING.** An empty workspace has no lowest member, so SWITCH-into-empty leaves `_current` pointing at a foreign OPEN area, which `infer_parent_from_workarea()` reads and hands into the AIF-137 path -- **the state the answer makes legal is the state that keeps that defect alive.** Chasing it found that **`Engine::_current` CANNOT EXPRESS "NOTHING SELECTED" AND NEVER COULD: slot 0 means area 0 (a real addressable slot, and where the 2026-08-27 run's BUILDING landed), the STARTUP POSITION (`shell.cpp:528` calls `eng.selectArea(0)` by decision), AND "NO ENGINE AT ALL"** -- `workareas::current_slot()` (`workareas.hpp:120`) reads `if (!eng) return 0;`, which is the AIF-118 shape sitting in the very accessor the AIF-137 path calls. Absent is spelled with a present value, which is R6, and it predates multi-workspace entirely; AIF-078 made it reachable and did not cause it. **SEPARATED OUT AS AIF-138** with its own finding document, exactly as AIF-137 was separated from this ruling: R129 keeps the question and defers the defect. **THE AUTHOR ANSWERED 6.1a WRONGLY TWICE BEFORE ANSWERING IT, AND BOTH WRONG ANSWERS ARE RECORDED IN SEC 5 OF AIF-138 SO THEY ARE NOT RETRIED.** First: park `_current` at a closed slot the workspace will fill -- rejected, because that means parking at SOME slot, which slot depends on what OTHER workspaces hold, so the cursor becomes a function of unrelated state. Second, and worse because it was written into this ruling before it was caught: an invariant stated as *`area(currentArea()).wsHandle()` is either `current_handle()` or 0* -- **which silently accepts a MISSED MEMBERSHIP STAMP as a legal empty position, since an OPEN unregistered area and a CLOSED area both read 0**, the former being a registration defect `reconcile_unregistered_areas()` (`:1578`) exists to name and `compute_save_scope` already counts as `skipped_unregistered` (`:1963`). **An invariant with the AIF-118 shape inside it, in a ruling about that shape.** The invariant survives only when stated over the PAIR: an OPEN current area must carry `current_handle()` and never another handle AND NEVER 0, and "nothing selected" must be said by the cursor IN ITS OWN VALUE rather than by pointing at a slot that happens to be closed. **THE METHOD ERROR IS WORTH MORE THAN THE CONCLUSION AND IS RECORDED AS SUCH:** the author checked whether CLOSE produced a safe READING and never checked whether the cursor could SAY "nothing" -- verifying the consequence instead of the representation, when R6 is a rule about representation. **ALSO NOT RULED:** whether `USE <table> IN <workspace>` is PLACEMENT or QUALIFICATION given `cmd_workspace.cpp:138` states "SWITCH-then-open, never open-then-assign"; `WorkspaceIdentity::profile_path`, a declared field with **NO writer and NO reader anywhere in the tree** and the natural home for per-workspace environment; the CLI `LOAD` half of R128 (`schema_close_all()` at `:2405`); and what retires the R112 instrument under P6 -- a measured zero, or a refusal counter that may legitimately never be zero. **FOUR CORRECTIONS TO THE AUTHOR'S OWN FIRST CUT ARE RECORDED IN SEC 10 RATHER THAN SILENTLY AMENDED**, including a withdrawn criticism of the external packet's directory layout: it is an established convention six packets deep since 2026-07-24, and the check that "found" it off-contract was `git ls-files` inside a tree the packets do not live in -- **R75, a gate sees the shape it was built to see, and its silence about a class of thing is not evidence the class is clean.** The rule was known and used as though absence were measurement. **NO CODE WAS WRITTEN AND NO BUILD WAS RUN:** `src/cli/**` and `src/xbase/**` are engine and want an explicit go; the only runtime act was a read-only measurement whose residue (two superseded catalog rows under D10.1/D10.3) is stated in the ruling. Full record `docs/maintenance/R129_WORKSPACE_CURSOR_RULING_V1.md`; findings `docs/maintenance/AIF137_FINDING_RELATION_PARENT_IS_WORKSPACE_BLIND_V1.md` and `docs/maintenance/AIF138_FINDING_AREA_CURSOR_CANNOT_SAY_NOTHING_V1.md`; closeout `docs/maintenance/SESSION_CLOSEOUT_R129_AIF137_WORKSPACE_CURSOR_2026-08-27.md`. Ships **review-needed** -- the author does not self-approve. |
| R130 | 2026-08-27 | ruling | AIF-078 | `member.derald` | **A POSTURE RECORDS A KEY, NOT AN ADDRESS.** Owner ruling: *"regarding LOAD, no problem, we don't save slots, we allocated them as they are available, do we need a slot provider??"* -- and on the brute close, *"now we use workspace close as the brute solution -- everybody has database fun until they shutdown their app and all of the workspaces close at one time."* **THIS IS THE ITEM R129 PARKED BY NAME** ("the CLI `LOAD` half of R128, `schema_close_all()` at `:2405`"), and the function has since been renamed `workspace_close_all()` (`f60d2d70d`), so R129's citation now points at a name that does not exist. **A posture's `AREA <n>` is a KEY inside the posture, not an engine address.** On load, slots are allocated as they are available. **THE CLOSE WAS NOT THE DEFECT; IT WAS THE ONLY GUARANTEE AVAILABLE TO A LOADER THAT REPLAYS ADDRESSES.** MEASURED: `workspace_load_from_stream()` replays `AREA 0` -> `open_into_area(0, ...)`, and `open_into_area` (`cmd_workspace.cpp:1310`) calls `get_area_0based(n)` and `A.close()` on that exact slot -- so the loader can only work if slot `n` is free, which `workspace_close_all()` at `:2405` guaranteed. Closing every workspace at once is CORRECT at shutdown and that function exists for it; the defect was LOAD borrowing shutdown's hammer as a precondition. **SO LOAD IS NOT OPEN.** R128 made OPEN additive by DELETING its close, because OPEN allocates. **Deleting LOAD's close alone would be WORSE than the defect being fixed** -- the loader would open into whatever occupies the recorded slots, which is precisely where another workspace's areas live. Scoping the close and adding the map are ONE change, not two. **NO NEW PROVIDER IS BUILT.** `cli::find_free_area_for_current_workspace(bool&)` (`workarea_util.hpp:155`, defined `workarea_util.cpp:223`, forwarding to `xbase::find_free_area_for_workspace`) is workspace-scoped, grows a workspace contiguously and REPORTS when it breaks contiguity. Callers today: `cmd_use.cpp:765` (USE ... IN FREE) and `cmd_workspace.cpp:1223` (R128's additive OPEN). **LOAD is the only opener in the tree that does not use it**, and IN FREE is already guarded against regressing -- `USE_ARGS` arm `U_T4` exists so that "if U_T4 ever reads 1 again, IN FREE has gone back to workspace-blind." **THE LOAD-BEARING HALF IS THE KEY-TO-SLOT MAP, NOT THE ALLOCATOR.** Two v3 payload lines reference the recorded numbers -- `CURSOR <area> <recno>` (`:2528`, into `get_area_0based(n)`) and `CURRENT <area>` (`:2539`, applied at `:2569` via `select_engine_area(n)`) -- so the loader must carry `old_n -> allocated_slot` built during the AREA loop and consulted by both handlers. **IF THE MAP IS OMITTED THE RESULT IS AIF-137's SHAPE AS A WRITE:** with LOAD allocating past an occupied range, a bare `CURSOR 8` reaches engine slot 8, which now belongs to whichever workspace was already there, and MOVES ITS CURSOR -- on a user's data, driven by a saved file, and with no instrument at all, where AIF-137 was at least found by one. **THIS DISSOLVES R129 SEC 6.2's SLOT-ARM DEPENDENCY.** 6.2 as ruled governs NAME resolution; LOAD addresses by slot, and R121 (ADDRESSING IS ABSOLUTE, TRAVERSAL IS FILTERED) argues `SELECT <n>` should reach slot `n` across any workspace, which left LOAD with no refusal to catch a bad remap. Under this ruling **LOAD never addresses a slot it did not just allocate**, so the hazard stops existing rather than needing a guard. 6.2's slot arm remains open for `SELECT` itself; it is no longer a prerequisite for LOAD. **NO FORMAT VERSION BUMP, AND THAT WAS NOT OBVIOUS:** a posture's `AREA` numbers need only be INTERNALLY CONSISTENT, not zero-based, so a posture saved from a workspace holding slots 13..25 loads correctly, its numbers being labels. NOT CLAIMED: that an OLD loader reads a NEW posture identically -- nothing about the FILE changes here, but the question would arise the moment anyone proposed renumbering saves, and it is named so nobody does it thinking it is free. **NOT RULED:** whether LOAD is RE-ENTRANT in R128's sense (a second OPEN of the same directory adds only what is new; whether a second LOAD of the same posture does likewise, or always allocates fresh, is unanswered); whether `WORKSPACE LOAD <name> AS <ws>` should exist, a plain LOAD now being additive; what LOAD does with the allocator's `broke_contiguity` report, which USE announces; and R129 sec 6.2's slot arm per above. **PROVEN SPEC-FIRST, WATCHED TO FAIL** -- the order R128 and AIF-137 both used and which caught a false green in each. Three arms, one of them honest about being green today for the wrong reason: **T1** the other workspace survived the LOAD (RED today -- `workspace_close_all()` closed it); **T2** the loaded workspace's cursor was restored (**GREEN TODAY FOR THE WRONG REASON**, stated as such -- with the slot space emptied first, recorded number and allocated slot coincide by accident; it discriminates against a NEW implementation whose map is wrong, not against today's build); **T3** the other workspace's cursor was NOT moved (RED today because that workspace is closed, and RED under a missing map because its cursor is driven by a stale `CURSOR <n>`) -- **T3 is the arm the map exists for.** Full record `docs/maintenance/R130_POSTURE_RECORDS_A_KEY_NOT_AN_ADDRESS_V1.md`. Ships **review-needed** -- the author does not self-approve. |
| R131 | 2026-08-29 | ruling | AIF-078 | `member.derald` | **A WORKSPACE OWNS ITS ENVIRONMENT, AND `SET PATH` IS HOW IT GETS ONE.** Owner ruling: *"allow workspace `new` for parallel workspaces, without the parent, or child annotation. Once we have a new named workspace, we switch to it and then we can set the environment etc. before an open"* -- so the sanctioned sequence is NEW / SWITCH / SET PATH / OPEN, and **THE NEW RULE IS THE THIRD LINE: `SET PATH` retargets the CURRENT WORKSPACE, not the session.** **WHAT IT DOES NOT CHANGE, stated first so it is not credited:** `WORKSPACE NEW <name> [UNDER <parent>]` already makes parallel siblings the default -- measured 2026-08-29, `workspace new workspaces` gave `parent 0 depth 0 WS_ID 267`. No code is needed to allow it; the change is that THE ENVIRONMENT ACQUIRES AN OWNER. **THE HOLE:** the three path slots are GLOBAL and `WORKSPACE SWITCH` moves MEMBERSHIP without moving them, so with MCC and CASCERP both open `SET ORDER TO TAG SID` on MCC's STUDENTS answered `openCdx: LMDB env missing: ...SYSTEMS\CASCADE_ERP\LMDB\STUDENTS.cdx.d` -- an MCC table resolved under the Cascade bundle because Cascade was opened last. Both systems stay READABLE; what breaks is anything that must RESOLVE a container or an LMDB env, which makes a switch FOUR LINES rather than one. **THE ENGINE ALREADY NAMED THE ANSWER TWICE AND DEFERRED IT:** `cmd_workspace.cpp:4773` -- *"THE MEMBERS CANNOT ANSWER IT. Deriving the origin from where member 0's file lives works UNTIL THE WORKSPACE IS EMPTY ... R6, the same defect one layer down"* -- and `:4779` -- *"WORKSPACES.dbf carries DBF_ROOT and is the place a durable answer belongs; wiring that is not this change."* R129 named a third home in its ALSO NOT RULED list. **THE EMPTY WORKSPACE IS WHAT FORCES IT:** the owner's sequence switches into a workspace with no members, and a workspace with no members has nothing to derive an environment from, which is the same reason the origin map RECORDS rather than DERIVES, one level up. **THREE CANDIDATE HOMES, NOT THE SAME THING AND NOT RECONCILED:** `WorkspaceIdentity::profile_path` (an ADDRESSING type), `DBF_ROOT`/`IDX_ROOT` (DURABLE catalog columns, already on every row), and the live `Entry` in `workspace_membership.hpp` (RUNTIME). **`profile_path` RE-MEASURED 2026-08-29 and R129 is corrected:** ONE declaration, ZERO writers, TWO readers -- `unspecified()` and `operator==` (`data_address.cpp:45,50`) -- so it is not unread, it PARTICIPATES IN EQUALITY, and two workspaces with different environments compare EQUAL today. R129's "no reader" becomes "read only by code that asks whether it is empty, which it always is". **THIS RULING MOVES AIF-138 FROM LATENT TO BLOCKING.** R129 sec 6.1 ruled that SWITCH moves `_current` to the target's lowest member so the two authorities agree; measured 2026-08-29 the whole body is `set_current_handle(h)` plus a print -- R129 said NO CODE WAS WRITTEN and none has been. So between SWITCH and OPEN the session sits in a workspace with NO members while `currentArea()` still points at the PREVIOUS workspace's table, and anything reading the current area acts on a FOREIGN one (LIST, DBAREA, and `infer_parent_from_workarea()`, the AIF-137 path). An empty workspace has no lowest member to move to, so 6.1 cannot be implemented until the area cursor can say "nothing" in its own value -- and this sequence requires occupying that state DELIBERATELY, every time a workspace is built. **THE INSTRUMENT FOR THAT WINDOW EXISTS BY ACCIDENT:** DBAREA and GPS gained an owning-workspace line beside the current-workspace line the same day on a separate owner instruction, built for the SELECT-across-workspaces case, and it names this divergence from the other side. **Q1 IS RULED AND THE GRAMMAR GAINS ONE FORM:** `SET PATH <slot> <value> IN <ws-or-handle>` binds explicitly; the bare form keeps today's behaviour and binds to current. That is the whole remedy for the misordering `NEW -> SET PATH -> SWITCH` -- name the target rather than rely on standing in it. `IN` is already the house word for targeting something other than current (`SET ORDER TAG <tag> IN <alias>`, `USE <table> IN <n>`), and after SET PATH it can only mean a workspace since paths have no per-area meaning. **A y/n PROMPT WAS PROPOSED AND WITHDRAWN THE SAME DAY ON MEASUREMENT:** this engine has NO script-mode prompt suppression -- `g_suppress_prompts` (`dirty_prompt.cpp:27`) is set only on the QUIT path and DOTSCRIPT never touches it -- so a prompt inside a `.dts` blocks on the console or eats the next script line as its answer, and `open_mcc_and_cascade.dts` sets the Cascade roots while MCC is open, so it would have prompted mid-script in a suite candidate. A gate existed (`g_dotscript_depth`, `cmd_dotscript.cpp:340`) and was declined: one grammar, no mode-dependent behaviour. **STILL OPEN: (Q2)** WHAT ARE A NEW WORKSPACE'S ROOTS BEFORE ANY SET PATH -- inherit the current ones (never unresolvable) or start empty (so `OPEN dbf` refuses until told); the ruling on the TREE does not decide it, since "no parent, no child" is about NESTING and this is a different kind of inheritance. **ALSO NOT RULED:** which of the three homes carries the roots and whether live and durable are one thing or two; whether DEFAULT's roots are the INIT slots by definition; whether `OPEN <dir> AS <name>` should record the three roots it resolved against, since it knows them for free at that moment. **NO CODE WAS WRITTEN:** `src/cli/**` and `src/xbase/**` are engine and want an explicit go. Full record `docs/maintenance/R131_WORKSPACE_ENVIRONMENT_RULING_V1.md`. Ships **review-needed** -- the author does not self-approve. |
| R134 | 2026-08-31 | ruling | AIF-078 | `member.derald` | **THE ALIAS IS THE PATH, AND THE PATH IS COMPOSED, NOT STORED.** Owner, in two parts: *"if we use the alias to be global it must be unique, the alias can be the path which should be both unique and self-identifying"* and, after the reader count below, *"i love the compose option"*. **WHAT IT RESOLVES.** Two routes to opening a table gave two different answers about what namespace an alias lives in, and the disagreement is MEASURED rather than argued: `USE` uniquifies -- runtime-proven 2026-08-31 by `dottalkpp/data/scripts/derive_distinct_alias_workspace_probe.dts`, six markers, full count, the engine printing *"USE: alias 'DDAT' is held by area 0; this instance is named 'DDAT2'"* for a name taken only in ANOTHER workspace -- while `WORKSPACE OPEN <dir>` does not, which AIF-137's own transcript proves by the R112 ledger firing on a name open in two areas at once (it could not fire if either had been renamed). One route treats the alias namespace as process-global, the other as per-workspace. **THE RULING PICKS GLOBAL AND MAKES THE KEY CARRY THE SCOPE**, which is the same move D10.4 made for the relation store: `WSA:DDAT` and `WSB:DDAT` cannot collide, so nothing needs filtering after the fact. **AND IT REJECTS THE SYNTHETIC DISAMBIGUATOR ON THE HOUSE'S OWN GROUNDS:** `DDAT2` says *"this is the second one"*, which is an ordinal; the path says *"this is B's"*, which is an identity. This register keeps finding that a count is a worse answer than a name. **COMPOSED, NOT STORED, AND THE COUNT IS WHY.** Measured 2026-08-31 over 1,233 files: `logicalName()` is declared exactly once (`include/xbase.hpp:284`, a `DbArea` member) and read at **58 sites across 40 files in 9 subsystems** -- `src/cli` 29 files, `src/workspace` 3, `src/gui/core` 2, and one each in `src/xbase`, `src/tv`, `src/tuple`, `src/edu`, `src/browser` and `include`. Storing the qualified form changes what every one of those sees, including four subsystems with no stake in workspaces at all. Composing it on demand touches the composer and the three alias resolvers. **THE METHOD IS THE POINT AS MUCH AS THE NUMBER:** R129 sec 10 records the author choosing a representation before counting the readers and being wrong twice, so the count came first this time and it is stated with its denominator. **FOUR WRITERS, AND THEY ARE FOUR DECLARATIONS OF ONE OPERATION.** `src/workspace/schema_workspace.cpp:96`, `src/workspace/workarea_slot.cpp:27`, `src/cli/cmd_workspace.cpp:870` and `src/cli/cmd_use.cpp:367` each carry their OWN copy of the `has_setLogicalName_t` detection shim, the last under a different name, with one call site each. That is the four-declarations-of-a-field-name shape this register already cites, living in the alias namespace, and it is the mechanical reason the two routes disagree: two routes, two writers, no shared decision. **WHAT THIS SUPERSEDES.** R129 sec 8 bullet 1 calls `find_open_area_by_alias` workspace-blind and reads that as the defect. Under a globally unique key a global resolver is CORRECT, so the bullet's implied fix direction is withdrawn: the correctness fix is at the WRITER, `populate_dbarea_metadata()` (`src/cli/cmd_use.cpp:501`), which decides *"the name this instance answers to"* and asks the blind resolver whether a stem is taken. One site, and it makes `USE` agree with `WORKSPACE OPEN` without changing a single reader. The bullet's MEASUREMENT stands; its diagnosis moves. **THE ASYMMETRY WITH D9.3 SEC 9a IS DELIBERATE AND IS STATED SO IT IS NOT READ AS DRIFT.** The relation store rejected a composite `RelKey{ws, name}` in one flat map in favour of per-workspace maps -- *"THE PARTITION IS THE MAP"* -- and this ruling takes the opposite shape for aliases. One principle, two surfaces: **internal identity keys on an opaque interned handle because no user ever types one; user-facing identity keys on a self-describing path because users do.** **WHAT SURVIVES.** `derive_distinct_alias()` is not retired; its question narrows to genuine WITHIN-workspace collisions, where two different files are opened under one name in one workspace and an ordinal is the only honest answer. Its own comment claims *"Deterministic and announced; never silent"* and the probe proved that claim TRUE, so it remains a wrong-but-LOUD path and must be triaged as a different class from AIF-137's silent crossings. **NOT RULED, and the first one bites persistence.** (a) The path is unique WITHIN A SESSION only: `WORKSPACE DESTROY` frees a name and a recreated name mints a fresh WS_ID (MWX_T9), so `WSA:DDAT` can name two different workspaces across a destroy/recreate cycle. A composed alias is therefore safe as a runtime address and NOT safe as a persisted one -- the same session-handle-versus-durable-id question the workspace parent raises from the other side, and it must be answered before any path is written into a posture. (b) The accessor's name and whether the bare form remains the default return of `logicalName()`. (c) The separator is `:` per P6 and is not re-decided here. **NO CODE WAS WRITTEN:** `src/cli/**` and `src/xbase/**` are engine and want an explicit go; the only act was a read-only scan and one probe run whose residue is stated in its commit. Ships **review-needed** -- the author does not self-approve. |
| R135 | 2026-08-31 | doctrine | AIF-078 | `member.derald` | **FOUR KINDS OF IDENTITY, SORTED BY WHERE THE TRUTH LIVES -- AND A COMPOSITION INHERITS DURABILITY, IT NEVER CONFERS IT.** Owner: *"session handles are interesting, there are durable id, derivable id, composition id"*. **EXTENDS R1, DOES NOT SUPERSEDE IT.** R1 is one line -- *"Derivation runs downward only"* -- and this row gives the ladder it governs a fourth rung and an inheritance rule. **THREE OF THE FOUR WERE ALREADY IN THE TREE AND ONE IS NEW; MEASURED 2026-08-31 over the governance corpus (1,223 files):** `durable id`/`durable identity` in 9 and 8 files, `session handle` in 4, `derivable` in 14 -- but in all 14 as a PROPERTY of some other thing ("X is derivable from Y") and never as a KIND of identity -- and `composition id`/`composite id` in **zero**. The vocabulary is the contribution; the structure was already load-bearing. **THE FOUR RUNGS.** (1) **SESSION HANDLE** -- truth lives in the process's own table, lifetime is the process, monotonic and NEVER reused after destroy so a stale handle resolves to *gone* and never to *somebody else*. **NOT PERSISTABLE**: written into a file it is not merely useless, it is a number the next process will hand to something else. (2) **DURABLE ID** -- truth lives in the catalog row, specifically the ROOT of its PREV_ID chain (`WS_ID`), and a chain root does not change (D10.2). **IT IS THE PERSISTENCE**, so persisting it is not a question. Note its legal zero: `include/xbase/workspace_membership.hpp` states 0 means *"no durable identity yet"*, real and legal for exactly one workspace -- DEFAULT, allocated LAZILY so a bare launch never touches the catalog. (3) **DERIVABLE ID** -- truth lives somewhere else entirely, and the identity is a recomputation of it. **NOT DECLARABLE**: storing it makes a second authority, which is R5's definition of the defect. This is already doctrine and did not need inventing -- `docs/ai-friendly/ENTITY_LIFECYCLE_AND_THE_BRIDGE_V1.md` rules *"Declare only what cannot be derived"*, *"A field earns existence by being consumed"*, and *"An unauthored default is not information."* (4) **COMPOSITION ID** -- truth lives in ITS PARTS. R134's alias `WS:TABLE` is the first named instance. **ITS LIFETIME IS THAT OF ITS LEAST DURABLE PART, AND THAT IS THE WHOLE RULE:** a composition assembled from a session handle is a session-scoped identity wearing a durable-looking spelling. **WHY THIS RULE EARNS A NUMBER RATHER THAN A SENTENCE:** a composition id is the one kind whose durability is invisible in its own spelling. `WSA:DDAT` and `WSID271:DDAT` look equally permanent and are not, and nothing in the string says which you are holding. Every other rung announces itself. **THE TWO CONSEQUENCES THAT WERE THE OPEN QUESTION.** (a) **The alias.** R134 already rules the alias COMPOSED and not stored, and that choice -- made before this taxonomy existed -- is exactly what makes the durability question cheap: compose from the SESSION HANDLE for a runtime address, from the DURABLE ID for anything written into a posture. There is no stored form to migrate because there is no stored form. (b) **The workspace parent.** A `PARENT` line in a posture carries the DURABLE ID, never the handle. `ws_id_of()` is the named upward conversion R1 permits and **it can fail, returning 0**; that failure is the refusal -- *this workspace has no durable identity yet and cannot be named as a parent* -- rather than a silent zero, which is the determinacy property the owner names as the point of the system. **AND R1'S DIRECTION RULE EXTENDS TO COMPOSITION UNCHANGED:** a composition id is assembled DOWNWARD from parts the holder already has, and must never reach UP to look a part up. That is the same reason `ws_id` is a STAMP and not a lookup -- *"A handle knows its WS_ID because it was told; it cannot go and find out."* A composer that fetches a missing part has turned an address into a query and can fail in the middle of rendering a name. **WHAT THIS RE-DIAGNOSES, AND IT CORRECTS A CLAIM MADE EARLIER THE SAME WEEK:** the catalog's `DEPTH` was described as UNWIRED. It is sharper than that -- **DEPTH is a DERIVABLE STORED WITHOUT A DERIVATION.** `depth_of()` exists, its own comment ties itself to this column, and the writers put a literal there instead; 106 rows reading 0 is *"an unauthored default is not information"* measured. **THE QUALIFIER THAT SAVES THE COLUMN, because rung 3 read flatly would delete it:** a derivable MAY be stamped into a durable record, since the catalog is read WITHOUT hydrating and a snapshot of a computable fact is not a second authority -- **provided it is stamped from the single source at write time and never written by hand.** DEPTH fails on the stamp, not on the storage. Two writers each asserting a literal is two hand-declarations of a computable fact, which is the four-declarations shape at a count of two. **NOT RULED.** (i) Whether the runtime-composed and persisted-composed forms are spelled the SAME (`WSA:DDAT` vs `271:DDAT`), or whether a persisted composition should be visibly different so a reader cannot mistake one for the other -- the argument for same is readability, for different is that the spelling would then announce durability and this row's central hazard would not exist. (ii) What a composition means for a table in DEFAULT before the catalog is touched, since rung 2's legal zero says there is no durable part to compose from yet. (iii) The accessor's name, still open from R134. **NO CODE WAS WRITTEN:** `src/cli/**` and `src/xbase/**` are engine and want an explicit go; the only act was a read-only scan whose scope is stated above. Ships **review-needed** -- the author does not self-approve. |
| R136 | 2026-08-31 | doctrine | AIF-110 | `member.derald` | **A RESTRUCTURE NEEDS A JOURNAL WHEN IT TOUCHES MORE THAN ONE FILE, AND REPORTING IS ENOUGH ONLY WHERE THE FAILURE ANNOUNCES ITSELF.** Owner, on being shown that a table restructure is a transaction: *"convert is attractive, think wal and acid"*. This is the answer, and it is narrower than "build a WAL" in one direction and wider in another. **THE PARTITION.** A restructure that writes ONE file gets atomicity and durability from build-new-then-rename: the new file is completed and flushed under a temp name and the rename is the commit, which is what `append_rewrite_table` already does. **No write-ahead log is required and adding one would be machinery with no failure to protect against.** A restructure that must land TWO OR MORE files together gets nothing from rename, because rename is atomic per file and not across a set: a crash between renames leaves a new table addressing an old sidecar, or the reverse. **That is the case that requires write-ahead intent and replay, and it is not hypothetical** -- memo compaction is chartered (supersede never erases the old payload bytes; the workspace catalog's DTX is 3,125,392 bytes against a 196,577-byte table, roughly sixteen to one, and most of it is dead) and it rewrites the table and the sidecar together. **THE EVIDENCE IS BOTH HALVES, MEASURED 2026-08-31 AND RECORDED IN AIF-110'S ROW.** Unindexed: `FIELDMGR APPEND` on the live workspace catalog preserved every memo TOKEN verbatim and never opened the sidecar -- 273 rows, all 20 original column hashes identical, sidecar md5 byte-identical, three independent record counts agreeing. Indexed: `dottalkpp/data/scripts/fieldmgr_append_live_cdx_probe.dts` on a table with a live CDX and LMDB environment -- the data intact, the container DETACHED. So a field append is single-file even on a table with a memo, because the token is a value in the row and the sidecar is never addressed. **THE SECOND HALF OF THE RULE IS THE PART WORTH THE NUMBER, AND IT IS NOT ABOUT FILE COUNT AT ALL.** Reporting a consequence is an adequate response ONLY where the resulting state announces itself. Here it does: a CDX carries a fingerprint of the table it was built against -- kind, version, reclen, field count, content hash -- and `openCdx` compares before attaching, so a stale container REFUSES rather than answering. Measured: `[table reclen=21 fields=3]` against `[cdx reclen=17 fields=2]`. **A stale index that refuses is a recoverable state; a stale index that answers is a wrong one, and no amount of warning text substitutes for a guard.** So the test is not "how many files" alone but **"if this goes wrong, does the next reader find out?"** -- and where the answer is no, the operation must be PREVENTED or made atomic, never merely narrated. Field append passes because the guard exists; memo compaction fails it in both directions at once, which is why it is the first real customer for the journal. **THIS RULE IS WHY THE FIX IN AIF-110 IS A MESSAGE AND NOT A REBUILD.** The append leaves the table correct and the container detached, the detachment is self-announcing by design, and the operator is now told at the moment it happens rather than at `SET ORDER` in a later session. A rebuild would also be correct and is a bigger change across a layer boundary -- `fields::append` is `src/xbase` and rebuilding a CDX is xindex plus the CLI -- so it is a caller's job, not this layer's. **NOT RULED.** (a) What the journal actually is -- an intent log replayed on open, a two-phase rename with a marker file, or a single container format that makes the pair one file. (b) Which layer owns it: `src/xbase` writes tables and does not know about sidecar families, so a facility that must land a table and its DTX together sits above it, and naming that owner is the first act of the compaction lane. (c) Whether `FIELDMGR DELETE` and `MODIFY` are single-file -- DELETE is currently `not_implemented`, and a field removal shifts every record, which is the same single-file shape but has never been measured. **NO CODE WAS WRITTEN BY THIS RULING** beyond the AIF-110 message fix it explains. Ships **review-needed** -- the author does not self-approve. |
| R137 | 2026-08-31 | doctrine | AIF-078 | `member.derald` | **CONTAINMENT IS DECLARED, NEVER INFERRED, AND `PARENT_ID = 0` MEANS EXACTLY ONE THING.** Owner: *"this is the time to correct the design."* Ruled before any writer stamps the column. **THE RULE.** A workspace's container is a claim someone makes, established at declaration time and nowhere else. `NEW <name> UNDER <parent>` declares one -- stamp `ws_id_of(parent)`. `NEW <name>` declares its absence -- stamp 0. `OPEN` **cannot declare one** (measured: its grammar is `<dir>` / `<file.dbf>` plus index-flavour options, no UNDER) so it stamps 0 and must not read the current workspace. `LOAD` restores the payload's declaration, or 0 -- never the loader's position. **THE SENTINEL.** `parent = 0` is an absence; DEFAULT is a real occupant at `kDefaultHandle = 1`, chosen so 0 could mean "no such workspace", and its own `ws_id` stays 0 until something forces `ensure_durable_workspace`. Nesting under an undurable DEFAULT and storing 0 would make one value mean both "no container" and "container is DEFAULT". So a parent that exists without a durable id is **refused at declaration time**, beside `create()`'s absent-parent refusal and `would_cycle`. **THE STORED FORM IS THE DURABLE ID, NEVER THE HANDLE** (R135) -- a handle names something else next launch. **WHY INFERENCE AND NOT JUST WRONGNESS IS BANNED:** `close_workspace_tree` (`src/cli/cmd_workspace.cpp:1782`) recurses through `children(h)` with `recursive=true` and a mirror walk iterates `members(h)` -- the AREAS -- so an edge inferred from ambient position closes tables that were never part of the operation. And an inferred parent makes a restore non-deterministic with respect to its artifact: one posture, two ambient positions, two trees. **SCOPE, STATED PLAINLY BECAUSE IT IS SMALL:** the tree mechanics here are standard and already correct -- parent pointer, 0 for root, `would_cycle`, `depth_of`, `children`. This rules only the two things that are not tree questions: serialize by stable id rather than by session pointer, and do not let one sentinel carry two meanings. Owner, on an earlier and much longer draft: *"I hope we are not over-complicating this... how is our parent problem any different than the first leaf of a node"* -- it is not, and the draft is trimmed to say so. **COSTS NOTHING TODAY:** no writer stamps either column; the 273 converted rows read 0 and are already correct under this rule. **NOT RULED:** whether nesting under DEFAULT is wanted at all (if not, the refusal is permanent and simpler); the DTSHEMA 4 line shape, which belongs to **AIF-070**, not here; and whether an area's membership -- legitimately ambient, since an area joins whichever workspace is current when opened -- should share the word "parent" with containment. Ships **review-needed**. |
| R138 | 2026-09-01 | ruling | AIF-082 | `member.derald` | **A LOCK THAT CLEARS ON ITS OWN WAS NEVER WEDGING ANYTHING, AND A NON-EMPTY LOCK IS NOT A STALE LOCK.** **review-needed** -- the author does not self-approve. On 2026-09-01 the prepush gate hard-failed twice in three minutes on a 668857-byte `.git/index.lock` that was gone seconds later both times, byte-identical across runs, with `.git/index` untouched at 668794 bytes -- a live git mid-operation, not wreckage. **THE RULE.** `check_sandbox_git_guard.py` asks one question -- is this repository WEDGED -- and a wedged repository's lock does not vanish while you look at it. A non-empty lock is therefore SAMPLED across a settle window, not stat'd once: one that survives is real, one that does not is reported and passed over. The zero-byte case still fails hard on first sight, because that is the killed-git signature, it never clears by itself, and it is what cost an afternoon on 2026-07-31. **THE SECOND HALF, AND THE WORSE ONE.** The guard printed "NON-EMPTY lock. That is not the known-stale signature ... do not delete it blindly" and returned the SAME exit code as the stale case, which `prepush_gate.py` then summarized as "a stale index.lock is present. Remove it." The check proved NOT STALE; the summary asserted STALE and instructed the deletion the check had just warned against. This is R-shaped in the way this project keeps rediscovering -- **the condensed restatement loses the qualifier** (AIF-038, the `supported` backfill) -- except that here it INVERTED it, into an instruction to delete a live git's lock and corrupt an index mid-write. Two conditions, two exit codes: 2 STALE (hard), 5 PERSISTENT NON-EMPTY (advisory, and the remedy is WAIT). **A severity is a claim about a state, so two states may not share one code.** Falsified both directions before wiring: the pre-R138 single stat returns exit 2 on a transient lock (reproducing both failures), the sampled version returns 0; `tools/staging/test_git_guard_lock.py` covers all four states, 5/5 green, and the 11 existing gate tests stay green. Mechanism-agnostic on purpose -- WHICH git call writes the transient lock was NOT established, only that it is deterministic, byte-identical and self-clearing, and a guard that answers "is it wedged" does not need to know. **AMENDED THE SAME DAY, AND THE AMENDMENT MATTERS MORE THAN THE RULING.** The mechanism above is WRONG and the sentence declining to establish it is the reason it stayed wrong. This gate runs from `.git/hooks/pre-commit`, so it executes inside a commit git has already begun: the lock is GIT'S OWN, guaranteed present for the whole hook, sized to the pending index (668857 for a one-file change, 668961 with a second file staged). Not a race, not transient, not self-clearing -- deterministic and structural. The settle window can never clear it there. **AND THE CHECK WAS LOGICALLY DEAD IN THAT CONTEXT, not merely noisy:** a STALE lock cannot reach a hook, because git refuses the commit with "Unable to create .git/index.lock: File exists" BEFORE any hook runs -- so the only lock this check could ever see there was the guaranteed-innocent one. **A switch that moves nothing is not an open** (with `scan_capture_hook.hpp` and the `openCdx` refresh branch). The lock check is therefore UNWIRED from `prepush_gate.py` and kept as the standalone tool it always was, correct wherever no commit is in flight. **THE METHOD FAILURE, recorded because it is the transferable part:** the ruling declared the mechanism unestablished and justified not looking as a design principle -- "a guard that answers 'is it wedged' does not need to know". It was one `cat .git/hooks/pre-commit` away. Declining to look is not the same as the question not mattering, and dressing the decline up as principle is how a wrong model survives its own review. The severity split (2 STALE hard, 5 NON-EMPTY advisory) STANDS and is what stopped the gate instructing the maintainer to delete git's live commit lock mid-commit. |
