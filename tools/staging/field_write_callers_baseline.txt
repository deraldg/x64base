# AIF-156 -- direct DbArea field writes outside the engine that do NOT go
# through xbase::cli::replaceFieldStored / replaceFieldNull.
#
# ============================================================================
# MIGRATED 2026-09-19 FROM file:line TO A CONTENT KEY. COUNT UNCHANGED: 13.
#
# Each entry is now  file :: enclosing function :: call text :: label.
# An insertion above a site changes none of the three, so a relocation is
# silent; a genuinely new writer changes at least one, so it still speaks.
# The LINE NUMBER IS STILL PRINTED beside a NEW finding -- a reader needs it
# to navigate. It is simply no longer COMPARED, which is what drifted.
#
# WHY NOW, AND NOT AT ANY OF THE NINE EARLIER OCCASIONS. This file recorded
# nine anchor drifts and named this exact remedy at the seventh, then declined
# it each time because the checker belongs to AIF-156. A TENTH arrived
# 2026-09-18 -- cmd_workspace.cpp 3057 -> 3074, a WsLock struct inserted above
# an untouched call by the M3 workbench commit -- and it was the FIRST OUTSIDE
# cmd_commit.cpp, which removes the last reading in which this was one
# pathological file rather than the keying scheme.
#
# THE DECIDING FACT IS THIS FILE'S OWN: EVERY ONE OF THE TEN WAS A FALSE
# POSITIVE. Not one NEW line in this gate's history has been a genuine
# unrouted writer. A signal whose every firing to date is noise trains its
# reader to skim, and the real one then arrives in a channel nobody reads.
#
# FALSIFIED BEFORE MIGRATING, three claims:
#   python tools/staging/check_field_write_callers.py --selftest
#     1. the same call keys IDENTICALLY after lines are inserted above it
#     2. a different call in the same function keys DIFFERENTLY
#     3. the same text in another function keys DIFFERENTLY
# And the migration itself was checked by count: 13 sites in 11 files before,
# 13 sites in 11 files after. A migration that changed the count would be a
# migration that changed the measurement.
#
# A LEGACY BASELINE IS NOT COMPARED. The checker detects the old shape and
# prints the migration instruction rather than reporting 13 FIXED and 13 NEW
# in one run -- which would be the loudest instance yet of the noise this
# change ends.
#
# THE CALL TEXT IS THE STRIPPED FORM the scanner matches on, not source
# verbatim: string literals are blanked before matching. That is deterministic,
# so the key is stable; it is recorded here so nobody reads a blanked literal
# as a defect.
#
# cmd_importsql.cpp keys as <file-scope> because the definition heuristic found
# no enclosing definition line. Disclosed rather than hidden: a consistent
# wrong answer still keys correctly, and only a FLAPPING one would reintroduce
# drift.
# ============================================================================
#
# ADVISORY, AND THIS LIST MAY ONLY SHRINK. A NEW line means somebody added a
# direct write: route it through the funnel, or -- if it is a deliberate
# below-the-funnel writer like the key generator -- add the FILE to
# EXEMPT_FILES in the checker WITH ITS REASON, never to this baseline. A
# baseline that grows stops being a measurement.
#
# Regenerate: python tools/staging/check_field_write_callers.py --emit-baseline
#
# ---------------------------------------------------------------------------
# 2026-09-23 -- THE FIRST GENUINE SHRINK SINCE THE MIGRATION: 13 -> 11 sites,
# 11 -> 9 files, and both removals are ROUTINGS, not re-anchors. Owner ruling:
# interactive editors share ONE gated commit function. browse_edit.cpp's
# commit_staged() now asks gateFieldWrites() for every staged field before
# writing any (exempted BY ROUTE -- see EXEMPT_FILES); app_simple_browser.cpp
# lost its private commit lambda entirely and calls that shared door. The
# content-key scheme carried its first real signal in the shrink direction.
# ---------------------------------------------------------------------------
# 2026-09-23, LATER THE SAME DAY -- 11 -> 6 sites, 9 -> 6 files, by EXEMPTION
# of the engine-owned system stores, not by routing. Owner ruling: a primary
# key is assigned at record creation and never edited; bbs_store.cpp (3
# sites), message_catalog.cpp and identity_dbf_store.cpp are creation-only BY
# CONSTRUCTION -- every key engine-assigned at append under the table lock, no
# path aims a write at an existing key, and their SYS* tables never carry a
# user-declared PRIMARY designation. Verified at the call sites, recorded with
# the reasons in EXEMPT_FILES (a THIRD exemption shape: engine-owned system
# store). What remains below is the real frontier: cmd_autodbf and
# cmd_importsql (creation-time uniqueness of imported keys), cmd_commit
# (buffer-flush apply), cmd_copy and cmd_sort (does the PRIMARY stamp travel
# to the destination?), and cmd_workspace set_by_name.
# ---------------------------------------------------------------------------
# THE HISTORY BELOW IS KEPT VERBATIM. It is the evidence for the migration
# above, and the ten drifts are the reason the scheme changed.
# ---------------------------------------------------------------------------
# through xbase::cli::replaceFieldStored / replaceFieldNull.
#
# Frozen 2026-09-07 at 28 sites in 19 files; 27 after CALCWRITE was routed
# and 22 after REPLACE_MULTI and the LEGACY INSERT/UPDATE verbs were gated
# (see EXEMPT_FILES in the checker for why those three are exempt rather
# than listed -- they gate every field, then keep their own multi-field
# write, which is the correct shape and not a hole).
# onto the funnel in the same commit -- the gate reported that as FIXED, which is
# the shrink direction it exists to measure. ADVISORY, AND THIS LIST MAY ONLY
# SHRINK. A NEW line means somebody added a direct write: route it through the
# funnel, or -- if it is a deliberate below-the-funnel writer like the key
# generator -- add the FILE to EXEMPT_FILES in the checker WITH ITS REASON,
# never to this baseline. A baseline that grows stops being a measurement.
#
# Regenerate: python tools/staging/check_field_write_callers.py
# CORRECTED 2026-09-09 FROM 22 SITES TO 16, AND NOT ONE OF THE SIX WAS ROUTED.
# They were never field writes. The `set(` test discriminated by the shape of
# the first ARGUMENT and never looked at the RECEIVER, so every object in the
# tree with a set(x) method counted: five view_->set(lines) on a LinesView (a
# TScroller subclass) in src/tv/cmd_recordview.cpp, which performs no DbArea
# write of any kind and is a VIEWER, and one msgLine_->set(s) on a TMsgLine in
# src/tv/foxtalk_command_window.cpp, a file where the string DbArea does not
# occur. The checker now checks the receiver and PRINTS every exclusion. See
# R142 and the finding filed the same day.
#
# 2026-09-09, SECOND CORRECTION: hierarchy_service.cpp leaves as an EXEMPTION,
# not as a routing. It called DbArea::replaceFieldStored -- the ENGINE
# PRIMITIVE, a different function from the xbase::cli funnel of the same name
# -- so it reached disk with no constraint gate. It now asks gateFieldWrites()
# for a whole row before writing any of it, which is the cmd_replace_multi
# shape, and is exempted BY ROUTE like the others. 16 -> 14.
#
# 2026-09-09, THIRD: table_state.cpp leaves as an EXEMPTION on a different
# argument from every one before it. It is not that gating WAL replay would be
# slow; it is that gating it would be WRONG. Replay writes values the gate
# already accepted at COMMIT, and it removes the log when it finishes, so a
# refusal at replay would discard a committed record and say nothing. 14 -> 13.
#
# 2026-09-09, FOURTH -- AN ANCHOR MOVE AND NOT A CHANGE OF SUBSTANCE.
# cmd_commit.cpp:319 -> :320. AIF-159 added ONE include line near the top of
# that file, which pushed the site down by one. It is the same A.set(f1, v)
# inside apply_one_recno, doing the same thing for the same reason. Re-anchored
# here so the gate does not report it as one site FIXED and another NEW, which
# is what happened at d778bad5c when the same shift went unrecorded. The count
# is unchanged: 13 sites in 11 files.
#
# 2026-09-09, FIFTH -- the same file, one line further down again.
# cmd_commit.cpp:320 -> :321. AIF-159 section 3 added one more include
# (sqlsel_statement.hpp) for the native-COMMIT guard. Same A.set(f1, v) inside
# apply_one_recno. Count unchanged: 13 sites in 11 files.
#
# 2026-09-11, SIXTH -- THE SAME FILE, THE SAME CAUSE, THE THIRD TIME.
# cmd_commit.cpp:321 -> :322. The DBF durability fix added ONE include
# (xbase/durable.hpp) so the redo log is not deleted before the apply reaches
# stable media. Same A.set(f1, v) inside apply_one_recno, unchanged in every
# respect. Count unchanged: 13 sites in 11 files.
#
# AND THAT IS THREE ANCHOR MOVES IN ONE FILE FROM THREE UNRELATED CHANGES,
# EACH COSTING A MANUAL EDIT AND EACH PRINTING A NEW/FIXED PAIR A READER HAS TO
# RECOGNISE AS NOISE. The cause is not carelessness: A BASELINE KEYED BY
# file:line IS A SECOND DECLARATION OF WHERE THE CODE IS, and it goes stale on
# any insertion above the site -- an edit that changes nothing this gate
# measures. The house has the general form of this written down already (a
# marker count beside a file is a second declaration and it drifted four
# times). Keying each entry by file plus something CONTENT-derived -- the
# enclosing function, or the matched text -- would not drift, and the real
# signal (a NEW site that is genuinely a new writer) would stop sharing a
# channel with a line shift. NOT PROPOSED AS A CHANGE HERE, and recorded
# rather than silently re-anchored a third time.
#
# 2026-09-11, SEVENTH -- AND THE SIXTH ENTRY WAS WRONG WHEN IT WAS COMMITTED.
# The true move is cmd_commit.cpp:321 -> :336, not :322. The sixth entry
# measured the shift caused by 70aa4fa60's single include and re-anchored to
# 322 -- correct for the file AS IT STOOD WHEN THAT NUMBER WAS COMPUTED. The
# SAME COMMIT (1c0669ead) then expanded this file's notes: and risk: contract
# blocks by about fourteen lines, all of them ABOVE the site, so the re-anchor
# was stale before it landed. The gate caught it on the next run: NEW :336,
# FIXED :322. Nothing was added and nothing was routed. Count unchanged: 13
# sites in 11 files.
#
# THE COMMIT THAT SHIPPED THE WRONG NUMBER IS THE ONE WHOSE MESSAGE ARGUED THAT
# A file:line BASELINE DRIFTS ON ANY INSERTION ABOVE THE SITE. The author could
# not hold it correct for the length of a single commit while writing the
# paragraph explaining why it goes stale -- because the paragraph and the
# insertion were the same edit.
#
# THAT IS THE FOURTH DRIFT IN THREE DAYS AND THE FIRST CAUSED BY THE WARNING
# ITSELF. Recorded in full rather than quietly corrected, because a
# self-inflicted instance is the strongest evidence this list can carry for what
# the sixth entry could only assert: THE DEFECT IS NOT CARELESSNESS. Any scheme
# that makes a human re-derive a line number by hand, in the same edit that
# moves it, fails this way. Keying by file plus the enclosing function or the
# matched text would have required no edit at all on any of the four occasions.
# STILL NOT PROPOSED AS A CHANGE -- the checker belongs to AIF-156 -- but the
# evidence has moved and the owner should know that it moved.
#
# 2026-09-11, EIGHTH -- cmd_commit.cpp:336 -> :396. Caused by AIF-160's
# RecordLockGuard (29337dca2), about sixty lines inserted ABOVE the site.
# Nothing was added and nothing was routed: same call, same receiver, same
# field, moved by an edit this gate does not measure. Count unchanged.
#
# THE NUMBER ABOVE IS THE GATE'S OWN READING OF THE FINAL FILE, not the author's
# arithmetic. That distinction is not pedantry -- it is precisely how the sixth
# entry went wrong, in the commit that was explaining the hazard, and repeating
# it while writing the eighth entry about it would have been the second
# self-inflicted instance in three days.
#
# src/cli/group_log.cpp IS NOT LISTED HERE, and its five writes are not a
# shrink. They went to EXEMPT_FILES with their reason, per the rule at the top
# of this file: a new writer is routed or exempted, never added here, because a
# baseline that grows stops being a measurement.
#
# 2026-09-11, NINTH -- cmd_commit.cpp:396 -> :399. Caused by AIF-160's commit
# seam (dace4fb1a) adding THREE include lines above the site: group_log.hpp and
# <optional>, with a blank between them. Nothing was added and nothing was
# routed: same call, same receiver, same field. Count unchanged.
#
# 399 IS THE GATE'S READING OF THE COMMITTED FILE, not a hand count. The author
# had independently arrived at 399 by counting the inserted lines and the two
# agreed -- which is luck, not method, and is recorded as such because the sixth
# entry was wrong for exactly the reason that arithmetic is not the instrument.
#
# NINE DRIFTS, AND THE FOURTH IN THIS ONE FILE IN A SINGLE DAY. The seventh
# entry's diagnosis has now been confirmed five more times than it was when it
# was written: a baseline keyed by file:line is a second declaration of where
# the code is, and cmd_commit.cpp is the site that proves it, because it is the
# file every lane touching COMMIT adds an include to. The remedy named there --
# key by file plus the enclosing function, or by the matched text -- would have
# cost zero edits on all nine occasions.
#
# WHAT IS NEW IN THIS INSTANCE, and it is the reason this entry is longer than
# the drift deserves: EVERY ONE OF THE NINE HAS BEEN A FALSE POSITIVE. Not one
# NEW line in this gate's history has been a genuine unrouted writer. A signal
# whose every firing to date is noise is one a reader learns to skim, and the
# next instance -- the real one -- arrives in a channel that has trained its
# audience to dismiss it. That is a worse failure than the manual edits, and it
# is the cost the seventh entry priced only as "noise a reader has to
# recognise". STILL NOT PROPOSED AS A CHANGE HERE -- the checker belongs to
# AIF-156 -- but the evidence has moved again and this is the ninth time it has.
src/cli/cmd_autodbf.cpp :: import_rows :: if (!area.set(static_cast<int>(i + 1), converted[i])) { :: set
src/cli/cmd_commit.cpp :: apply_one_recno :: if (!A.set(f1, v)) { :: set
src/cli/cmd_copy.cpp :: logical_copy_to_as :: if (!dst.set(i, s.get(i))) { :: set
src/cli/cmd_importsql.cpp :: <file-scope> :: if (!area.set(static_cast<int>(i + 1), converted[i])) :: set
src/cli/cmd_sort.cpp :: cmd_SORT :: (void)out.set(out_i + 1, v); :: set
src/cli/cmd_workspace.cpp :: set_by_name :: if (!a.set(i + 1, v)) { err = std::string(" ") + col; return false; } :: set
