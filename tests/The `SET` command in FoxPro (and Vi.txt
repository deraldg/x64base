The `SET` command in FoxPro (and Visual FoxPro) is incredibly versatile — it's used to configure nearly every aspect of the runtime environment. There are **dozens of variations**, each controlling a specific behavior, display format, or system setting.

Here’s a categorized breakdown of the most commonly used `SET` commands:

---

### 🧭 **Environment & Behavior Settings**
- `SET DELETED` – Controls visibility of records marked for deletion.
- `SET EXACT` – Determines string comparison behavior.
- `SET SAFETY` – Prompts before overwriting files.
- `SET TALK` – Controls display of system messages.
- `SET CONFIRM` – Requires Enter to exit a field.
- `SET ESCAPE` – Enables/disables Esc key to cancel READ.
- `SET MULTILOCKS` – Enables record locking in multi-user environments.
- `SET REPROCESS` – Retry time for locked records.

---

### 📁 **File & Path Management**
- `SET PATH TO` – Specifies search paths for files.
- `SET DEFAULT TO` – Sets the default working directory.
- `SET CLASSLIB TO` – Loads visual class libraries.
- `SET LIBRARY TO` – Loads external libraries or FLLs.

---

### 🖥️ **Display & Output**
- `SET CENTURY` – Controls date formatting (e.g., 2-digit vs 4-digit years).
- `SET DATE` – Specifies date format (e.g., MDY, DMY).
- `SET BELL` – Enables/disables the system bell.
- `SET COLOR` – Sets screen colors.
- `SET MESSAGE TO` – Directs messages to a specific window.
- `SET FORMAT TO` – Specifies report format file.
- `SET PRINTER TO` – Redirects output to a printer or file.

---

### 🧮 **Data & Indexing**
- `SET ORDER TO` – Sets the active index tag.
- `SET RELATION TO` – Establishes parent-child table relationships.
- `SET COLLATE TO` – Defines collation sequence for string comparisons.
- `SET FILTER TO` – Applies a filter to a table.
- `SET UNIQUE` – Controls duplicate index entries.

---

### 🧪 **Debugging & Development**
- `SET COVERAGE TO` – Enables code coverage logging.
- `SET ASSERTS ON|OFF` – Enables/disables ASSERT statements.
- `SET STEP ON` – Enables single-step debugging.
- `SET PROCEDURE TO` – Loads procedure files.

---

### 🌐 **Internationalization & Code Pages**
- `SET CPDIALOG` – Enables/disables code page dialog.
- `SET CPCOMPILE TO` – Sets code page for compilation.
- `SET LANGUAGE TO` – Specifies language for system messages.

---

### 🧰 **Miscellaneous**
- `SET AUTOSAVE` – Controls buffer flushing during READ.
- `SET AUTOINCERROR` – Handles autoincrement field errors.
- `SET BLOCKSIZE` – Sets memo field block allocation.
- `SET EXCLUSIVE` – Controls shared vs exclusive file access.
- `SET REFRESH` – Controls cache refresh rate.

---

For a full reference with syntax and examples, check out the [SET Command Overview from Yaldex](https://www.yaldex.com/fox_pro_tutorial/html/1334aa6b-38e0-4fcd-a174-3c4f5c964155.htm) or the [Visual FoxPro command reference on studylib.net](https://studylib.net/doc/5619760/some-of-visual-foxpro-s-commands).

If you’re working on a legacy FoxPro app or trying to modernize one, I can help you map these settings to equivalent behaviors in modern environments.
